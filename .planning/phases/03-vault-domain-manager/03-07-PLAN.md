---
phase: 03-vault-domain-manager
plan: 07
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06]
files_modified:
  - internal/vault/manager.go
  - internal/vault/entities.go
  - internal/vault/manager_test.go
  - internal/vault/validation_test.go
autonomous: true
requirements:
  - QUERY-02
  - VAULT-01

must_haves:
  truths:
    - Can search secrets by query (excludes sensitive fields)
    - Can list favorites using DFS traversal
    - Atomic save commits all changes or none
    - All UAT criteria validated with tests
    - Package compiles and tests pass
  artifacts:
    - path: internal/vault/manager.go
      provides: Search and favorites methods
      exports: ["Buscar", "ListarFavoritos", "Salvar"]
    - path: internal/vault/manager_test.go
      provides: Integration tests for full workflows
      min_lines: 400
    - path: internal/vault/validation_test.go
      provides: Comprehensive UAT coverage
      min_lines: 800
  key_links:
    - from: manager.go Buscar
      to: entities.go Segredo
      via: "name and observação search (NOT sensitive campos)"
      pattern: "strings\\.Contains"
    - from: manager.go ListarFavoritos
      to: entities.go Cofre.PastaGeral
      via: "DFS traversal from root"
      pattern: "func.*dfs"
    - from: manager.go Salvar
      to: repository.go
      via: "atomic commit with two-phase pattern"
      pattern: "Salvar.*repository"
---

<objective>
Implement search (excluding sensitive fields per QUERY-02), favorites listing with DFS traversal, and comprehensive validation ensuring all Phase 3 UAT criteria are met.

Purpose: Complete final Manager API surface. Search respects sensitive field types (senha excluded). Favorites use efficient DFS. All 30 implementation decisions validated. Full integration tests prove Manager orchestrates entity logic correctly.

Output: Complete vault domain layer ready for Phase 4 (adapter integration).
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-vault-domain-manager/03-CONTEXT.md
@.planning/phases/03-vault-domain-manager/03-RESEARCH.md
@modelo-dominio.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Buscar with sensitive field exclusion</name>
  <read_first>
    - .planning/REQUIREMENTS.md (QUERY-02: passwords excluded)
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-19)
    - .planning/phases/03-vault-domain-manager/03-RESEARCH.md (Search Algorithm)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity method on Cofre: buscar(consulta string) []*Segredo. Case-insensitive search in: segredo.nome, segredo.observacao, non-sensitive campos (exclude tipo=senha per QUERY-02). Recursively search all folders from PastaGeral. Return only secrets with estadoSessao != Excluido.

Manager: Buscar(consulta string) calls cofre.buscar(), returns results. Read-only operation, no state changes.

Per D-19, search is case-insensitive using strings.ToLower. Per QUERY-02, sensitive field types (senha) are excluded from search.
  </action>
  <acceptance_criteria>
    - Cofre.buscar searches nome, observacao, non-sensitive campos
    - Excludes deleted secrets (estadoSessao == Excluido)
    - Excludes senha type fields from search (QUERY-02)
    - Case-insensitive matching
    - Manager.Buscar delegates to entity
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*Buscar" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>Buscar implemented with sensitive field exclusion per QUERY-02</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ListarFavoritos with DFS traversal</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-20)
    - .planning/phases/03-vault-domain-manager/03-RESEARCH.md (Favorites Traversal)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity method on Cofre: listarFavoritos() []*Segredo. DFS traversal starting from PastaGeral. For each pasta, iterate segredos and collect where favorito == true AND estadoSessao != Excluido. Recurse into subpastas. Return collected slice.

Manager: ListarFavoritos() calls cofre.listarFavoritos(), returns results. Read-only operation.

Per D-20, use DFS (not BFS) for traversal. Exclude deleted secrets.
  </action>
  <acceptance_criteria>
    - Cofre.listarFavoritos uses DFS from PastaGeral
    - Collects secrets where favorito == true
    - Excludes deleted secrets (estadoSessao == Excluido)
    - Manager.ListarFavoritos delegates to entity
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*ListarFavoritos" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>ListarFavoritos implemented with DFS per D-20</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write search and favorites validation tests</name>
  <read_first>
    - .planning/REQUIREMENTS.md (QUERY-02)
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-19, D-20)
  </read_first>
  <files>internal/vault/validation_test.go</files>
  <behavior>
    - Test search finds matches in nome, observacao, text campos
    - Test search excludes senha type campos (QUERY-02)
    - Test search excludes deleted secrets
    - Test search is case-insensitive
    - Test favorites lists only favorito=true secrets
    - Test favorites excludes deleted secrets
    - Test favorites uses DFS order
  </behavior>
  <action>
Add test functions: TestBuscarSensitiveExclusao, TestBuscarCaseInsensitive, TestBuscarExcluiExcluidos, TestListarFavoritosOrdem, TestListarFavoritosExcluiExcluidos. Create test data with various campo types, set some as favorito, mark some deleted. Verify search and favorites respect rules.
  </action>
  <acceptance_criteria>
    - TestBuscarSensitiveExclusao proves senha fields not searched
    - TestBuscarCaseInsensitive verifies case-insensitive matching
    - TestListarFavoritosOrdem verifies DFS traversal order
    - All tests validate exclusion of deleted secrets
    - go test ./internal/vault passes
  </acceptance_criteria>
  <verify><automated>go test ./internal/vault -v -run "TestBuscarSensitiveExclusao|TestBuscarCaseInsensitive|TestListarFavoritosOrdem"</automated></verify>
  <done>Search and favorites tests pass covering QUERY-02 and D-19/D-20</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write comprehensive integration tests</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 3 UAT criteria lines 52-82)
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (All 30 decisions)
  </read_first>
  <files>internal/vault/manager_test.go</files>
  <behavior>
    - Test full workflow: create vault → add folders/templates/secrets → modify → save → lock → unlock
    - Test state tracking: modificado flag, estadoSessao transitions
    - Test atomic save: all changes committed or none
    - Test cycle detection: move folder into descendant fails
    - Test promotion: delete folder promotes children with conflict resolution
    - Test duplication: creates independent copy with "(N)" name
    - Test validation: all business rules enforced
  </behavior>
  <action>
Add integration test functions covering end-to-end workflows. TestFullWorkflow: creates complete vault structure, performs all operations, validates state. TestAtomicSave: verifies two-phase commit pattern from D-17. TestCycleDetection: validates folder hierarchy protection. TestPromotion: validates automatic conflict resolution per D-27. TestDuplication: validates independent copy with name progression. Use subtests for organization.
  </action>
  <acceptance_criteria>
    - TestFullWorkflow exercises all Manager methods
    - TestAtomicSave validates two-phase commit (D-17)
    - TestCycleDetection validates hierarchy protection
    - TestPromotion validates conflict resolution (D-27)
    - TestDuplication validates independent state (D-15)
    - All integration tests pass
    - go test ./internal/vault -v passes
  </acceptance_criteria>
  <verify><automated>go test ./internal/vault -v -run "TestFullWorkflow|TestAtomicSave|TestCycleDetection|TestPromotion"</automated></verify>
  <done>Integration tests pass covering all Phase 3 workflows</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Write comprehensive UAT validation tests</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 3 UAT criteria)
    - .planning/REQUIREMENTS.md (VAULT-02, SEC-*, FOLDER-*, TPL-*)
  </read_first>
  <files>internal/vault/validation_test.go</files>
  <behavior>
    Test every UAT criterion from ROADMAP Phase 3:
    - VAULT-02: All entity types exist with correct structure
    - SEC-01 through SEC-09: All secret operations
    - FOLDER-01 through FOLDER-05: All folder operations
    - TPL-01 through TPL-06: All template operations
    - QUERY-02: Search excludes sensitive fields
    - All 30 implementation decisions (D-01 through D-30)
  </behavior>
  <action>
Add comprehensive test suite organized by requirement ID. TestVAULT02Structure: validates entity definitions. TestSEC01through09: all secret operations. TestFOLDER01through05: all folder operations. TestTPL01through06: all template operations. TestQUERY02: sensitive field exclusion. TestDecisionsD01throughD30: validates all implementation decisions. Use table-driven tests for multiple scenarios per requirement.
  </action>
  <acceptance_criteria>
    - Every requirement from ROADMAP has corresponding test
    - Every implementation decision from CONTEXT.md validated
    - Test coverage >90% for internal/vault package
    - All edge cases covered (empty names, conflicts, invalid states)
    - go test ./internal/vault -v passes with 0 failures
    - go test ./internal/vault -cover shows >90% coverage
  </acceptance_criteria>
  <verify><automated>go test ./internal/vault -v -cover</automated></verify>
  <done>All UAT criteria validated with comprehensive test suite</done>
</task>

<task type="auto">
  <name>Task 6: Final package validation and documentation</name>
  <read_first>
    - internal/vault/doc.go
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md
  </read_first>
  <files>internal/vault/doc.go, internal/vault/manager.go</files>
  <action>
Update doc.go with complete package documentation: API surface summary, key design decisions (D-01, D-05, D-11, D-17, D-29), usage examples. Add godoc comments to all exported Manager methods with parameter descriptions and return values. Verify package compiles cleanly with no warnings.

Run final validation:
- go build ./internal/vault
- go test ./internal/vault -v
- go vet ./internal/vault
- golangci-lint run ./internal/vault (if available)
  </action>
  <acceptance_criteria>
    - doc.go contains comprehensive package overview
    - All exported methods have godoc comments
    - go build ./internal/vault succeeds with 0 warnings
    - go test ./internal/vault passes all tests
    - go vet ./internal/vault reports 0 issues
    - Package ready for Phase 4 adapter integration
  </acceptance_criteria>
  <verify><automated>go build ./internal/vault && go test ./internal/vault && go vet ./internal/vault</automated></verify>
  <done>Package validated, documented, and ready for integration</done>
</task>

</tasks>

<verification>
Run full validation suite:
```bash
# Build check
go build ./internal/vault

# Test with coverage
go test ./internal/vault -v -cover

# Static analysis
go vet ./internal/vault

# All UAT criteria from ROADMAP Phase 3 met
```

Verify all requirements addressed:
- VAULT-02: Entity types complete
- SEC-01 through SEC-09: Secret operations complete
- FOLDER-01 through FOLDER-05: Folder operations complete
- TPL-01 through TPL-06: Template operations complete
- QUERY-02: Search excludes sensitive fields
- All 30 implementation decisions validated
</verification>

<success_criteria>
- Manager methods: Buscar, ListarFavoritos exist and work correctly
- Search excludes senha type campos per QUERY-02
- Favorites use DFS traversal per D-20
- Integration tests cover full workflows
- UAT tests validate all requirements
- Test coverage >90%
- Package compiles with 0 warnings
- All tests pass (go test ./internal/vault -v)
- Static analysis clean (go vet ./internal/vault)
- Documentation complete (doc.go + godoc comments)
- Phase 3 complete and ready for Phase 4
</success_criteria>

<output>
After completion, create `.planning/phases/03-vault-domain-manager/03-07-SUMMARY.md`
</output>

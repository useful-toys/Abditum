---
phase: 03-vault-domain-manager
plan: 06
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - internal/vault/manager.go
  - internal/vault/entities.go
  - internal/vault/validation_test.go
autonomous: true
requirements:
  - SEC-03
  - SEC-04
  - SEC-05
  - SEC-08
  - SEC-09

must_haves:
  truths:
    - Can rename secret (marks estadoSessao = Modificado)
    - Can edit field values (marks estadoSessao = Modificado)
    - Can change observação (marks estadoSessao = Modificado)
    - Can move secret to different folder
    - Can reposition secret within folder
    - Observação is structurally separate from campos
  artifacts:
    - path: internal/vault/manager.go
      provides: Secret content mutation methods
      exports: ["RenomearSegredo", "EditarCampoSegredo", "EditarObservacao", "MoverSegredo", "ReposicionarSegredo"]
    - path: internal/vault/entities.go
      provides: Secret content and structure
      exports: ["campos []ValorCampo", "observacao string"]
    - path: internal/vault/validation_test.go
      provides: Content mutation tests
      min_lines: 200
  key_links:
    - from: manager.go RenomearSegredo
      to: entities.go Segredo.estadoSessao
      via: "content mutation → Modificado"
      pattern: "estadoSessao.*Modificado"
    - from: manager.go EditarObservacao
      to: entities.go Segredo.observacao
      via: "direct field access (not in campos slice)"
      pattern: "observacao.*="
---

<objective>
Implement secret content mutations (rename, edit fields, edit observação) and structure operations (move, reposition) with estadoSessao tracking.

Purpose: Complete secret CRUD operations. All content changes mark estadoSessao = Modificado per D-11. Observação is structurally separate field per D-29 (not in campos slice). Move and reposition support folder hierarchy and drag-and-drop UI.

Output: Working secret content management with comprehensive tests covering UAT criteria.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-vault-domain-manager/03-CONTEXT.md
@.planning/phases/03-vault-domain-manager/03-RESEARCH.md
@modelo-dominio.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement RenomearSegredo with content mutation</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-11, D-12)
    - internal/vault/entities.go (Segredo struct)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity methods: validarRenomear (checks name unique in pasta), renomear(nome) returns (alterado bool, err). If name actually changed, also set estadoSessao = Modificado (content mutation per D-11). Manager: RenomearSegredo validates, mutates, uses alterado feedback per D-12. Only mark cofre.modificado if changed.
  </action>
  <acceptance_criteria>
    - Segredo.validarRenomear checks uniqueness in pasta
    - Segredo.renomear returns alterado, sets estadoSessao = Modificado if changed
    - Manager.RenomearSegredo uses alterado feedback
    - No-op rename (same name) doesn't mark modified or change state
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*RenomearSegredo" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>RenomearSegredo implemented with estadoSessao tracking per D-11</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement EditarCampoSegredo with validation</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-11, D-13)
    - modelo-dominio.md (ValorCampo struct)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity methods: validarEditarCampo(index, valor) checks index valid, valor matches campo type. editarCampo(index, valor) returns (alterado bool, err). If value actually changed, set estadoSessao = Modificado. Manager: EditarCampoSegredo validates, mutates, uses alterado feedback.

Per D-13, campos is slice indexed by position. Types: texto, senha, url, numero, data, booleano.
  </action>
  <acceptance_criteria>
    - Segredo.validarEditarCampo validates index and type compatibility
    - Segredo.editarCampo returns alterado, sets estadoSessao = Modificado if changed
    - Manager.EditarCampoSegredo uses alterado feedback
    - ErrCampoInvalido for bad index, ErrTipoIncompativel for type mismatch
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*EditarCampoSegredo" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>EditarCampoSegredo implemented with type validation per D-13</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement EditarObservacao (separate field)</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-11, D-29)
    - modelo-dominio.md (Segredo.observacao field)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity method: editarObservacao(texto string) returns (alterado bool, err). If observacao actually changed, set estadoSessao = Modificado. Manager: EditarObservacao validates (length ≤1000 chars), mutates, uses alterado feedback.

Per D-29, observacao is separate string field on Segredo struct (NOT in campos slice). This ensures structural impossibility of manipulation.
  </action>
  <acceptance_criteria>
    - Segredo.editarObservacao returns alterado, sets estadoSessao = Modificado if changed
    - Manager.EditarObservacao validates length
    - observacao field is string (not in campos slice)
    - ErrObservacaoMuitoLonga for length >1000
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*EditarObservacao" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>EditarObservacao implemented as separate field per D-29</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement MoverSegredo between folders</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-16)
    - internal/vault/entities.go (Pasta struct)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity methods: validarMover(destino *Pasta) checks destino not nil, name unique in destino. mover(destino *Pasta, posicao int) removes from origem.segredos, inserts at posicao in destino.segredos. Manager: MoverSegredo(segredo, destino, posicao) validates, mutates, marks cofre.modificado (but NOT estadoSessao per D-16 - move is structural, not content).
  </action>
  <acceptance_criteria>
    - Segredo.validarMover checks destino valid, name unique
    - Segredo.mover removes from origem, inserts at posicao in destino
    - Manager.MoverSegredo marks cofre.modificado but NOT estadoSessao (D-16)
    - ErrPastaInvalida, ErrNameConflict returned appropriately
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*MoverSegredo" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>MoverSegredo implemented without estadoSessao change per D-16</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement ReposicionarSegredo within folder</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-16)
    - internal/vault/entities.go (Pasta.segredos slice)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity methods: validarReposicionar(novaPosicao) checks novaPosicao valid within pasta.segredos. reposicionar(novaPosicao) moves within slice using Go slice idiom (remove, insert). Manager: ReposicionarSegredo validates, mutates, marks cofre.modificado (but NOT estadoSessao per D-16).
  </action>
  <acceptance_criteria>
    - Segredo.validarReposicionar validates position within bounds
    - Segredo.reposicionar uses slice manipulation to move
    - Manager.ReposicionarSegredo marks cofre.modificado but NOT estadoSessao (D-16)
    - ErrPosicaoInvalida for out of bounds
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*ReposicionarSegredo" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>ReposicionarSegredo implemented without estadoSessao change per D-16</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Write secret content mutation validation tests</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-11, D-16, D-29)
    - .planning/ROADMAP.md (Phase 3 UAT criteria)
  </read_first>
  <files>internal/vault/validation_test.go</files>
  <behavior>
    - Test rename → estadoSessao = Modificado
    - Test edit field → estadoSessao = Modificado
    - Test edit observação → estadoSessao = Modificado
    - Test move folder → cofre.modificado true, estadoSessao unchanged
    - Test reposition → cofre.modificado true, estadoSessao unchanged
    - Test observação is separate field (not in campos)
    - Test no-op mutations don't mark modified
  </behavior>
  <action>
Add test functions: TestRenomearSegredoEstado, TestEditarCampoEstado, TestEditarObservacaoEstado, TestMoverSemEstadoMudanca, TestReposicionarSemEstadoMudanca, TestObservacaoSeparada. Use table-driven tests. Verify D-11 (content vs structure) and D-16 (move doesn't affect estadoSessao).
  </action>
  <acceptance_criteria>
    - TestRenomearSegredoEstado verifies estadoSessao = Modificado on rename
    - TestEditarCampoEstado verifies estadoSessao = Modificado on edit
    - TestEditarObservacaoEstado verifies estadoSessao = Modificado on observação change
    - TestMoverSemEstadoMudanca proves move doesn't change estadoSessao
    - TestObservacaoSeparada proves observação not in campos slice
    - go test ./internal/vault passes
  </acceptance_criteria>
  <verify><automated>go test ./internal/vault -v -run "TestRenomearSegredoEstado|TestEditarCampoEstado|TestMoverSemEstadoMudanca|TestObservacaoSeparada"</automated></verify>
  <done>Secret content mutation tests pass covering UAT criteria</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
go test ./internal/vault -v
```

All secret CRUD UAT criteria covered:
- SEC-03: Rename marks estadoSessao = Modificado
- SEC-04: Edit fields marks estadoSessao = Modificado
- SEC-05: Observação is separate field (structural enforcement)
- SEC-08: Move folder doesn't change estadoSessao (D-16)
- SEC-09: Reposition doesn't change estadoSessao (D-16)
</verification>

<success_criteria>
- Manager methods: RenomearSegredo, EditarCampoSegredo, EditarObservacao, MoverSegredo, ReposicionarSegredo exist
- Content mutations set estadoSessao = Modificado (D-11)
- Structure operations (move, reposition) do NOT set estadoSessao (D-16)
- Observação is separate string field, not in campos slice (D-29)
- All validation tests pass
- go build ./internal/vault succeeds
- go test ./internal/vault passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-vault-domain-manager/03-06-SUMMARY.md`
</output>

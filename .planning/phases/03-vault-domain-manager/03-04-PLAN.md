---
phase: 03-vault-domain-manager
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - internal/vault/manager.go
  - internal/vault/entities.go
  - internal/vault/validation_test.go
autonomous: true
requirements:
  - TPL-01
  - TPL-02
  - TPL-03
  - TPL-04
  - TPL-05
  - TPL-06

must_haves:
  truths:
    - Can create template with unique name
    - Templates ordered alphabetically by name
    - Cannot create template named "Observação"
    - Can add/remove/reorder fields in template
    - Template deletion requires explicit confirmation
  artifacts:
    - path: internal/vault/manager.go
      provides: Template management methods
      exports: ["CriarModelo", "RenomearModelo", "ExcluirModelo"]
    - path: internal/vault/entities.go
      provides: Template field management
      exports: ["AdicionarCampo", "RemoverCampo", "ReordenarCampo"]
    - path: internal/vault/validation_test.go
      provides: Template business rule tests
      min_lines: 200
  key_links:
    - from: manager.go CriarModelo
      to: entities.go Modelo
      via: "alphabetical insertion"
      pattern: "sort\\.Slice"
---

<objective>
Implement template (modelo) management with alphabetical ordering, "Observação" name prohibition, and field structure operations.

Purpose: Complete template CRUD and field management. Templates are sorted alphabetically after every name change. Field operations support reordering for UI drag-and-drop. Observação prohibition enforced per D-29.

Output: Working template management with comprehensive validation tests covering UAT criteria.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-vault-domain-manager/03-CONTEXT.md
@.planning/phases/03-vault-domain-manager/03-RESEARCH.md
@modelo-dominio.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CriarModelo with alphabetical insertion</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-05, D-23, D-29)
    - .planning/phases/03-vault-domain-manager/03-RESEARCH.md (Alphabetical Insertion)
    - internal/vault/entities.go (Cofre struct)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Add template creation to entities.go and manager.go. Entity validation: validarCriacaoModelo checks name non-empty, length ≤255, not "Observação" (case-insensitive), unique within cofre.modelos. Entity mutation: criarModelo creates Modelo, appends to modelos, sorts slice alphabetically by nome. Manager: CriarModelo delegates, marks cofre.modificado, updates timestamp. Return *Modelo or error.

Use sort.Slice with strings.ToLower for case-insensitive alphabetical sort per D-23.
  </action>
  <acceptance_criteria>
    - Cofre.validarCriacaoModelo validates name, uniqueness, not "Observação"
    - Cofre.criarModelo inserts and sorts modelos alphabetically
    - Manager.CriarModelo delegates to entity methods
    - ErrNomeReservado returned for "Observação" (any case)
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*CriarModelo" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>CriarModelo implemented with alphabetical ordering per D-23</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement RenomearModelo with re-sort</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-12, D-24)
    - internal/vault/manager.go (CriarModelo pattern)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity methods: validarRenomear (checks name unique, not "Observação"), renomear returns (alterado bool, err). After successful rename, re-sort cofre.modelos alphabetically. Manager: RenomearModelo validates, mutates, uses alterado feedback per D-12. Only mark modified if actual change.
  </action>
  <acceptance_criteria>
    - Modelo.validarRenomear validates uniqueness, not "Observação"
    - Modelo.renomear returns (bool, error)
    - After rename, cofre.modelos re-sorted
    - No-op rename doesn't mark modified
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*RenomearModelo" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>RenomearModelo implemented with re-sort and change detection</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement ExcluirModelo with in-use check</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-26)
    - modelo-dominio.md (Modelo.EmUso logic)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity method: EmUso() bool checks if any segredo.modelo == this modelo pointer. Entity method: excluir() removes from cofre.modelos slice. Manager: ExcluirModelo checks EmUso() → ErrModeloEmUso, otherwise validates and mutates. Mark cofre.modificado.

Per D-26, deletion check uses pointer equality (segredo.modelo == modelo).
  </action>
  <acceptance_criteria>
    - Modelo.EmUso() iterates cofre secrets checking pointer equality
    - Modelo.excluir() removes from cofre.modelos
    - Manager.ExcluirModelo protects in-use templates
    - ErrModeloEmUso returned if any secret references template
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*ExcluirModelo" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>ExcluirModelo implemented with in-use protection</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement field management (add/remove/reorder)</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-29)
    - modelo-dominio.md (Campo struct)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity methods on Modelo: validarAdicionarCampo (name unique, not "Observação"), adicionarCampo(nome, tipo, posicao), validarRemoverCampo (index valid), removerCampo(index), validarReordenarCampo (from/to valid), reordenarCampo(from, to).

Manager methods: AdicionarCampo, RemoverCampo, ReordenarCampo delegate to entity, mark cofre.modificado. All methods update timestamp.

Per D-29, "Observação" prohibited in campo names (same as template names).
  </action>
  <acceptance_criteria>
    - Modelo.adicionarCampo inserts at position
    - Modelo.removerCampo removes by index
    - Modelo.reordenarCampo moves field from index to index
    - ErrNomeReservado for "Observação" in field name
    - ErrCampoInvalido for invalid indices
    - Manager methods delegate and mark modified
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*AdicionarCampo" internal/vault/manager.go && grep -q "func.*ReordenarCampo" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>Field management implemented with validation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Write template management validation tests</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-23, D-26, D-29)
    - .planning/ROADMAP.md (Phase 3 UAT criteria)
  </read_first>
  <files>internal/vault/validation_test.go</files>
  <behavior>
    - Test alphabetical ordering after create and rename
    - Test "Observação" prohibition (template and field names, all cases)
    - Test template name uniqueness
    - Test template deletion protection when in-use
    - Test field add/remove/reorder operations
    - Test position validation for fields
  </behavior>
  <action>
Add test functions: TestCriarModeloOrdenacao, TestModeloNomeReservado, TestExcluirModeloEmUso, TestCampoOperacoes. Use table-driven tests for multiple scenarios. Verify error types match expected.
  </action>
  <acceptance_criteria>
    - TestCriarModeloOrdenacao verifies alphabetical sort
    - TestModeloNomeReservado covers "Observação" in template and field names
    - TestExcluirModeloEmUso verifies in-use protection
    - TestCampoOperacoes covers add/remove/reorder with edge cases
    - go test ./internal/vault passes
  </acceptance_criteria>
  <verify><automated>go test ./internal/vault -v -run "TestCriarModeloOrdenacao|TestModeloNomeReservado|TestExcluirModeloEmUso|TestCampoOperacoes"</automated></verify>
  <done>Template validation tests pass covering UAT criteria</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
go test ./internal/vault -v
```

All template UAT criteria covered:
- TPL-01: Templates created with unique names
- TPL-02: Templates alphabetically ordered
- TPL-03: "Observação" name prohibited
- TPL-04: Field add/remove/reorder operations work
- TPL-05: In-use templates cannot be deleted
- TPL-06: Field structure changes tracked in modificado
</verification>

<success_criteria>
- Manager methods: CriarModelo, RenomearModelo, ExcluirModelo exist
- Entity methods: adicionarCampo, removerCampo, reordenarCampo exist
- Templates sorted alphabetically after create/rename
- "Observação" prohibition enforced for templates and fields
- In-use templates protected from deletion
- All validation tests pass
- go build ./internal/vault succeeds
- go test ./internal/vault passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-vault-domain-manager/03-04-SUMMARY.md`
</output>

---
phase: 03-vault-domain-manager
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - internal/vault/manager.go
  - internal/vault/entities.go
  - internal/vault/validation_test.go
autonomous: true
requirements:
  - FOLDER-01
  - FOLDER-02
  - FOLDER-03
  - FOLDER-04
  - FOLDER-05

must_haves:
  truths:
    - Can create folder with unique name in parent
    - Cannot rename Pasta Geral or move it
    - Moving folder into descendant returns ErrCycleDetected
    - Deleting folder promotes children with automatic conflict resolution
    - Name conflicts resolved with numeric suffix "(N)"
  artifacts:
    - path: internal/vault/manager.go
      provides: Folder management methods
      exports: ["CriarPasta", "RenomearPasta", "MoverPasta", "ExcluirPasta"]
    - path: internal/vault/validation_test.go
      provides: Business rule validation tests
      min_lines: 300
  key_links:
    - from: manager.go MoverPasta
      to: entities.go Pasta
      via: "cycle detection algorithm"
      pattern: "func.*detectarCiclo"
---

<objective>
Implement folder management with cycle detection, Pasta Geral protection, and promotion-with-conflict-resolution on deletion.

Purpose: Complete folder hierarchy operations including complex algorithms (full ancestor walk for cycles, recursive promotion with name conflict resolution). All operations validate via entity methods, Manager orchestrates and updates global state.

Output: Working folder CRUD with comprehensive validation tests covering UAT criteria.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-vault-domain-manager/03-CONTEXT.md
@.planning/phases/03-vault-domain-manager/03-RESEARCH.md
@modelo-dominio.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CriarPasta with validation</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-04, D-05, D-22, D-25)
    - .planning/phases/03-vault-domain-manager/03-RESEARCH.md (Two-Phase Validation Pattern)
    - internal/vault/entities.go (Pasta struct)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Add folder creation with position parameter to entities.go and manager.go. Entity validation methods (private): validarCriacaoSubpasta, contemSubpastaComNome, criarSubpasta. Manager method (public): CriarPasta(pai, nome, posicao). Validate name non-empty, length ≤255, unique within parent, valid position (0 to len inclusive). Insert at position using slice idiom. Mark cofre.modificado, update timestamp. Return *Pasta or error.
  </action>
  <acceptance_criteria>
    - Pasta.contemSubpastaComNome method exists (private)
    - Pasta.validarCriacaoSubpasta validates name, uniqueness, position
    - Pasta.criarSubpasta inserts at position (cannot fail after validation)
    - Manager.CriarPasta delegates to entity methods
    - Manager updates cofre.modificado and timestamp
    - ErrNomeVazio, ErrNomeMuitoLongo, ErrNameConflict, ErrPosicaoInvalida returned appropriately
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*CriarPasta" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>CriarPasta implemented with two-phase validation per D-05</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement RenomearPasta with Pasta Geral protection</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-12 change detection, D-24 timestamps)
    - internal/vault/manager.go (CriarPasta pattern)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity methods: validarRenomear (checks not Pasta Geral, name unique in parent), renomear returns (alterado bool, err). Manager: RenomearPasta checks if pasta == cofre.PastaGeral → ErrPastaGeralProtected. Validate, mutate, use alterado feedback to conditionally update global state per D-12. Only update timestamp if actual change.
  </action>
  <acceptance_criteria>
    - Pasta.validarRenomear checks not Pasta Geral, uniqueness
    - Pasta.renomear returns (bool, error) signaling actual change
    - Manager.RenomearPasta protects Pasta Geral
    - No-op rename (same name) doesn't mark modified per D-12
    - ErrPastaGeralProtected, ErrNameConflict returned correctly
    - go build ./internal/vault
  </acceptance_criteria>
  <verify><automated>grep -q "ErrPastaGeralProtected" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>RenomearPasta with Pasta Geral protection and change detection</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement MoverPasta with cycle detection</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-RESEARCH.md (Cycle Detection section — full ancestor walk)
    - .planning/ROADMAP.md (Phase 3 pitfall watch)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity method detectarCiclo: walk from destino.pai up to root, comparing each ancestor with pasta. Return true if match found (cycle). Manager.MoverPasta: validate not Pasta Geral, not moving to self, no cycle, name unique in destination. Remove from old parent (removerDePai), add to new parent (adicionarAoPai). Update timestamps, mark modified.
  </action>
  <acceptance_criteria>
    - detectarCiclo method walks FULL ancestor chain
    - MoverPasta rejects Pasta Geral
    - MoverPasta rejects move to self
    - MoverPasta returns ErrCycleDetected for descendant destination
    - Name conflict in destination returns ErrNameConflict
    - go build ./internal/vault
  </acceptance_criteria>
  <verify><automated>grep -q "detectarCiclo" internal/vault/entities.go && go build ./internal/vault</automated></verify>
  <done>MoverPasta with full-ancestor-walk cycle detection</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement repositioning methods</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-23 repositioning semantics, D-12 no-op edge cases)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Methods: ReposicionarPasta(pasta, novaPosicao), SubirPastaNaPosicao(pasta), DescerPastaNaPosicao(pasta). Validate position range. Edge cases per D-12/D-23: Subir at position 0 → no-op (return nil, don't mark modified). Descer at last position → no-op. Reposicionar to current position → no-op. Only mark modified if actual reposition occurs.
  </action>
  <acceptance_criteria>
    - ReposicionarPasta, SubirPastaNaPosicao, DescerPastaNaPosicao methods exist
    - Edge case: Subir at 0 returns nil, doesn't mark modified
    - Edge case: Descer at last returns nil, doesn't mark modified
    - Edge case: Reposicionar to current position no-op
    - Invalid position returns ErrPosicaoInvalida
    - go build ./internal/vault
  </acceptance_criteria>
  <verify><automated>grep -q "SubirPastaNaPosicao" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>Repositioning methods with no-op edge case handling</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement ExcluirPasta with promotion and conflict resolution</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-RESEARCH.md (Folder Deletion with Promotion section)
    - .planning/REQUIREMENTS.md (FOLDER-05)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
ExcluirPasta returns ([]Renomeacao, error). Protect Pasta Geral. For each subpasta: if name conflict in parent → merge contents recursively (mesclarPastas). For each segredo: if name conflict in parent → generate suffixed name gerarNomeSufixado, record Renomeacao. Remove pasta from parent. Return list of renames for TUI to display. Important: StateDeleted secrets retain state when promoted (FOLDER-05).
  </action>
  <acceptan

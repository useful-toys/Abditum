---
phase: 03-vault-domain-manager
plan: 05
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - internal/vault/manager.go
  - internal/vault/entities.go
  - internal/vault/validation_test.go
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-06
  - SEC-07

must_haves:
  truths:
    - Can create secret in folder with template
    - Can delete secret (marks estadoSessao = Excluido)
    - Can restore deleted secret (marks estadoSessao = Normal)
    - Can favorite/unfavorite secret (does NOT change estadoSessao per D-11)
    - Duplicate secret with name "(2)", "(3)" progression
  artifacts:
    - path: internal/vault/manager.go
      provides: Secret lifecycle methods
      exports: ["CriarSegredo", "ExcluirSegredo", "RestaurarSegredo", "FavoritarSegredo", "DuplicarSegredo"]
    - path: internal/vault/entities.go
      provides: Secret state machine
      exports: ["estadoSessao field", "favorito field"]
    - path: internal/vault/validation_test.go
      provides: State machine and lifecycle tests
      min_lines: 200
  key_links:
    - from: manager.go ExcluirSegredo
      to: entities.go Segredo.estadoSessao
      via: "state transition Normal → Excluido"
      pattern: "estadoSessao.*Excluido"
    - from: manager.go FavoritarSegredo
      to: entities.go Segredo.favorito
      via: "toggle favorito without estadoSessao change"
      pattern: "favorito.*="
---

<objective>
Implement secret lifecycle operations (create, delete, restore, favorite) and state machine with independent tracking for estadoSessao and favorito.

Purpose: Complete secret state management with D-11 two-flag design (estadoSessao for content changes, favorito for UI preference). Duplication creates clones with name conflict resolution. Delete/restore transitions are reversible until Salvar.

Output: Working secret lifecycle with comprehensive state machine tests covering UAT criteria.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-vault-domain-manager/03-CONTEXT.md
@.planning/phases/03-vault-domain-manager/03-RESEARCH.md
@modelo-dominio.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CriarSegredo with state initialization</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-05, D-11, D-13, D-28)
    - .planning/phases/03-vault-domain-manager/03-RESEARCH.md (State Machine)
    - internal/vault/entities.go (Segredo struct, EstadoSessao enum)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Add secret creation to entities.go and manager.go. Entity validation: validarCriacaoSegredo checks pasta not nil, nome unique within pasta, modelo not nil. Entity mutation: criarSegredo creates Segredo with estadoSessao = Normal, favorito = false, campos initialized from template structure, appends to pasta.segredos.

Manager: CriarSegredo(pasta, modelo, nome) delegates, marks cofre.modificado = true AND sets segredo.estadoSessao = Modificado per D-11 (new content). Update timestamp. Return *Segredo or error.

Per D-13, campos slice is initialized by copying template.campos structure (names, types, order) with empty values.
  </action>
  <acceptance_criteria>
    - Pasta.validarCriacaoSegredo validates pasta, nome uniqueness, modelo
    - Pasta.criarSegredo initializes estadoSessao = Normal, favorito = false
    - campos slice mirrors template structure with empty values
    - Manager.CriarSegredo marks cofre.modificado AND estadoSessao = Modificado
    - ErrPastaInvalida, ErrModeloInvalido, ErrNameConflict returned appropriately
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*CriarSegredo" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>CriarSegredo implemented with two-flag state initialization per D-11</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ExcluirSegredo state transition</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-11, D-14)
    - modelo-dominio.md (EstadoSessao enum)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity method: excluir() sets estadoSessao = Excluido. Manager: ExcluirSegredo checks segredo.estadoSessao != Excluido → ErrSegredoJaExcluido, calls excluir(), marks cofre.modificado, updates timestamp.

Per D-11, favorito flag is NOT changed. Per D-14, deletion is reversible until Salvar.
  </action>
  <acceptance_criteria>
    - Segredo.excluir() sets estadoSessao = Excluido
    - Manager.ExcluirSegredo validates not already deleted
    - favorito flag unchanged
    - cofre.modificado marked
    - ErrSegredoJaExcluido returned if already Excluido
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*ExcluirSegredo" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>ExcluirSegredo implemented with state transition per D-11</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement RestaurarSegredo state transition</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-14)
    - internal/vault/manager.go (ExcluirSegredo pattern)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity method: restaurar() sets estadoSessao = Normal. Manager: RestaurarSegredo checks segredo.estadoSessao == Excluido → ErrSegredoNaoExcluido, calls restaurar(), marks cofre.modificado, updates timestamp.

Per D-14, restore returns to Normal (not Modificado - no content change, just undo delete).
  </action>
  <acceptance_criteria>
    - Segredo.restaurar() sets estadoSessao = Normal
    - Manager.RestaurarSegredo validates is deleted
    - ErrSegredoNaoExcluido returned if not Excluido
    - cofre.modificado marked
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*RestaurarSegredo" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>RestaurarSegredo implemented per D-14</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement FavoritarSegredo (no estadoSessao change)</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-11, D-12)
    - .planning/phases/03-vault-domain-manager/03-RESEARCH.md (State Machine diagram)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity method: favoritar(valor bool) returns alterado bool. Sets favorito = valor, returns true if changed. Manager: FavoritarSegredo calls favoritar(), uses alterado feedback per D-12. If changed, mark cofre.modificado (but NOT estadoSessao - per D-11 favorito is independent).

CRITICAL per D-11: Favoriting does NOT change estadoSessao. Only content mutations do.
  </action>
  <acceptance_criteria>
    - Segredo.favoritar(bool) sets favorito and returns alterado
    - Manager.FavoritarSegredo only marks cofre.modificado if changed
    - estadoSessao NOT modified (D-11 independence)
    - No-op favorite (same value) doesn't mark modified
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*FavoritarSegredo" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>FavoritarSegredo implemented with independent flag per D-11</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement DuplicarSegredo with name progression</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-15, D-27)
    - .planning/phases/03-vault-domain-manager/03-RESEARCH.md (Name Conflict Resolution)
  </read_first>
  <files>internal/vault/manager.go, internal/vault/entities.go</files>
  <action>
Entity method: duplicar(nome string) creates deep copy with new name, estadoSessao = Normal, favorito = false, campos deep copied. Manager: DuplicarSegredo generates unique name using "(N)" progression per D-27 (check pasta for "nome (2)", "nome (3)", etc), calls duplicar, marks cofre.modificado AND new segredo.estadoSessao = Modificado.

Per D-15, duplication creates independent secret with fresh state.
  </action>
  <acceptance_criteria>
    - Segredo.duplicar() creates deep copy with new name
    - Manager.DuplicarSegredo finds unique name with "(N)" suffix
    - New secret has estadoSessao = Modificado, favorito = false
    - campos deep copied (not shared references)
    - go build ./internal/vault succeeds
  </acceptance_criteria>
  <verify><automated>grep -q "func.*DuplicarSegredo" internal/vault/manager.go && go build ./internal/vault</automated></verify>
  <done>DuplicarSegredo implemented with name conflict resolution per D-27</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Write secret lifecycle validation tests</name>
  <read_first>
    - .planning/phases/03-vault-domain-manager/03-CONTEXT.md (D-11, D-14, D-15)
    - .planning/ROADMAP.md (Phase 3 UAT criteria)
  </read_first>
  <files>internal/vault/validation_test.go</files>
  <behavior>
    - Test create → estadoSessao = Modificado, favorito = false
    - Test delete → estadoSessao = Excluido, favorito unchanged
    - Test restore → estadoSessao = Normal
    - Test favorite toggle → favorito changes, estadoSessao unchanged
    - Test duplicate → new secret with "(2)" name, independent state
    - Test state transitions (Normal ↔ Excluido)
  </behavior>
  <action>
Add test functions: TestCriarSegredoEstadoInicial, TestExcluirRestaurarCiclo, TestFavoritarIndependencia, TestDuplicarProgresso, TestEstadoTransicoes. Use table-driven tests for state machine. Verify flag independence per D-11.
  </action>
  <acceptance_criteria>
    - TestCriarSegredoEstadoInicial verifies Modificado + not favorito
    - TestExcluirRestaurarCiclo verifies delete/restore transitions
    - TestFavoritarIndependencia proves favorito doesn't affect estadoSessao
    - TestDuplicarProgresso verifies "(N)" name generation
    - TestEstadoTransicoes covers all valid state transitions
    - go test ./internal/vault passes
  </acceptance_criteria>
  <verify><automated>go test ./internal/vault -v -run "TestCriarSegredoEstadoInicial|TestExcluirRestaurarCiclo|TestFavoritarIndependencia|TestDuplicarProgresso"</automated></verify>
  <done>Secret lifecycle tests pass covering UAT criteria</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
go test ./internal/vault -v
```

All secret lifecycle UAT criteria covered:
- SEC-01: Secrets created with estadoSessao = Modificado
- SEC-02: Delete marks Excluido, restore returns to Normal
- SEC-06: Favorite toggle independent from estadoSessao
- SEC-07: Duplicate creates independent copy with "(N)" name
</verification>

<success_criteria>
- Manager methods: CriarSegredo, ExcluirSegredo, RestaurarSegredo, FavoritarSegredo, DuplicarSegredo exist
- State machine: estadoSessao transitions (Normal ↔ Excluido, → Modificado)
- Flag independence: favorito changes don't affect estadoSessao (D-11)
- Name progression: Duplication uses "(N)" suffix (D-27)
- All validation tests pass
- go build ./internal/vault succeeds
- go test ./internal/vault passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-vault-domain-manager/03-05-SUMMARY.md`
</output>

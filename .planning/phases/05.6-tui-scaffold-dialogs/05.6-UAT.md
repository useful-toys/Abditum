---
status: complete
phase: 05.6-tui-scaffold-dialogs
source: [05.6-SUMMARY.md]
started: 2026-04-06T00:00:00Z
updated: 2026-04-06T02:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Dialog box renders with correct border characters
expected: Run the app and press key 1. A dialog box appears with rounded border characters: ╭ at top-left, ╮ at top-right, ╰ at bottom-left, ╯ at bottom-right, │ on the sides. Title is embedded in the top border: "╭── [title] ─────╮"
result: pass

### 2. Severity symbols in title
expected: Tecla 1 (Destrutivo) → ⚠ no título, borda amarela. Tecla 4 (Erro) → ✕ no título, borda vermelha. Tecla 7 (Alerta) → ⚠ no título, borda amarela. Tecla a (Informativo) → ℹ no título, borda azul. Tecla d (Neutro) → sem símbolo, borda neutra.
result: pass

### 3. Acknowledge layout — single action, no Esc
expected: Press key 1 (Acknowledge/Neutral). The action bar at the bottom shows only "Enter OK" right-aligned: "╰────────── Enter OK ──╯". There is no "Esc Cancelar" on the left side.
result: pass

### 4. Confirm layout — default left, cancel right
expected: Press key 2 (Confirm/Neutral, 2-action). The action bar shows default action on the left and cancel on the right with fill dashes between: "╰── Enter [label] ──────────── Esc Cancelar ──╯"
result: pass

### 5. 3-action confirm layout
expected: Press key 3 (Confirm/Neutral, 3-action). The action bar shows three tokens: default on left, middle action in center, cancel on right, all separated by "── ".
result: issue
reported: "Falta espaço depois do último token não-cancel antes dos fill dashes. Ex: '╰── Enter Recuperar ── A Abrir backup──────── Esc Cancelar ──╯' — 'backup' encosta direto nos dashes, deveria ser 'backup ──────── Esc'."
severity: minor

### 6. Long body text wraps inside dialog
expected: Press a key that triggers a dialog with a long body message. The body text wraps to multiple lines within the dialog width, never overflowing the │ borders. Each line is padded to the same width.
result: pass

### 7. Enter key triggers default action and closes dialog
expected: Open any dialog (press key 1). Press Enter. The dialog disappears and the app returns to its normal state (PoC main screen).
result: pass

### 8. Esc key triggers cancel and closes dialog (Confirm intent)
expected: Open a Confirm dialog (press key 2). Press Esc. The dialog disappears and the app returns to its normal state.
result: pass

### 9. Tab/arrows cycle focus on Confirm dialogs
expected: Open a 2-action Confirm dialog (press key 2). Press Tab (or right arrow). Focus should visually shift from the default action to the secondary action. Press Tab again — focus wraps back. (Focus indicator may be subtle — key/label color change or bold.)
result: pass
note: "Spec does not define Tab focus cycling. Bug was diagnosed and fixed: removed focus int field, advanceFocus(), focusableCount(), and Tab/arrow handlers. Enter now always executes the action with Default:true. Commit: 8cd4c3d."

### 10. PoC keys 1–9 and a–f each trigger a dialog
expected: Press keys 1 through 9 in sequence — each should push a different dialog (different title, body, severity). Then press keys a through f — each should push another dialog. Total 15 unique dialogs should be reachable.
result: pass

## Summary

total: 10
passed: 9
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Enter sempre executa a ação Default:true; Esc executa Cancel:true; teclas explícitas (M, A, etc.) executam a ação correspondente. Não há conceito de foco navegável por Tab entre ações."
  status: resolved
  resolution: "Removed focus int field, advanceFocus(), focusableCount() from DecisionDialog. Rewrote triggerFocused() to iterate d.actions and execute the one with Default:true. Removed Tab/arrow key cases from Update(). Removed TestDecisionDialog_ArrowCyclesFocus test. Commit: 8cd4c3d."
  severity: major
  test: 9

- truth: "Action bar: tokens são separados por ' ── ' e deve haver um espaço após o último token não-cancel antes dos fill dashes. Layout correto: '╰── Enter Recuperar ── A Abrir backup ──────── Esc Cancelar ──╯'."
  status: resolved
  root_cause: "renderActionBar() em decision.go adicionava ' ── ' apenas entre segmentos (i < len(segs)-1), mas não adicionava espaço trailing após o último segmento. O fill de dashes encostava direto no texto do último token."
  resolution: "Added trailing space after last non-cancel segment in renderActionBar() loop (decision.go). All 44 tests pass. Fix applied but not yet committed."
  severity: minor
  test: 5
  artifacts:
    - path: "internal/tui/decision.go"
      issue: "renderActionBar() loop faltava espaço trailing após último segs[i] quando i == len(segs)-1"

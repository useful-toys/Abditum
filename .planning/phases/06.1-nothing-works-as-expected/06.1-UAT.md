---
status: testing
phase: 06.1-nothing-works-as-expected
source: SUMMARY.md
started: 2026-04-06T22:00:00Z
updated: 2026-04-06T23:30:00Z
---

## Current Test

number: [done]
awaiting: [none — all tests concluded]

## Tests

### 1. F5/F6 Keys — Welcome Screen
expected: Launch the app. Command bar at the bottom shows "f5 Novo · f6 Abrir" (not the old "o"/"n" keys).
result: issue — F5/F6 keys worked but order was wrong (f6 before f5). Fixed: swapped priorities so f5 Novo appears before f6 Abrir. Committed 6733a16.

### 2. F6 — Open Vault launches file picker
expected: Press F6 on the welcome screen. A file picker modal opens to select a .abditum file.
result: pass — file picker opens correctly. Note: file picker has formatting issues, to be addressed separately.

### 3. F5 — New Vault launches file picker
expected: Press F5 on the welcome screen. A file picker modal opens to choose where to save the new vault.
result: pass — file picker opens correctly. Note: file picker has formatting issues, to be addressed separately.

### 4. Open Vault — Error recovery (bad file)
expected: Press F6, select a file that is NOT a valid vault (any non-.abditum file). An error message appears and the file picker re-opens so you can choose a different file (instead of the flow ending).
result: blocked — file picker not functional; to be addressed separately.

### 5. Create Vault — Overwrite "Voltar" returns to picker
expected: Press F5, pick a path where a file already exists. The overwrite confirmation dialog appears. Press Esc (or "Voltar"). The file picker re-opens so you can choose a different location (instead of ending the flow).
result: blocked — file picker not functional; to be addressed separately.

### 6. Create Vault — Weak password dialog
expected: Press F5, pick a new path, enter a short/simple password (e.g. "abc"). A "Senha fraca" dialog appears with "Revisar" (default) and "Prosseguir" options instead of saving immediately.
result: blocked — file picker not functional; to be addressed separately.

### 7. Create Vault — Weak password "Revisar" returns to password input
expected: In the "Senha fraca" dialog, press Enter (or R) to choose "Revisar". The password creation modal re-opens so you can enter a stronger password.
result: blocked — file picker not functional; to be addressed separately.

### 8. Create Vault — Weak password "Prosseguir" saves anyway
expected: In the "Senha fraca" dialog, press Esc to choose "Prosseguir". The vault saves and the app transitions to the vault area despite the weak password.
result: blocked — file picker not functional; to be addressed separately.

### 9. Ctrl+Q — Exit confirmation when no vault open (Fluxo 3)
expected: On the welcome screen (no vault open), press Ctrl+Q. A "Sair do Abditum?" dialog appears with "Sim" and "Não" options. The app does NOT quit immediately.
result: pass (after fix dd8b013)

### 10. Ctrl+Q — "Não" cancels exit
expected: In the "Sair do Abditum?" dialog, press Esc (Não). The dialog closes and you return to the app normally.
result: pass. Note: dialog is too wide for such a short message — to be addressed separately.

### 11. Ctrl+Q — "Sim" exits the app
expected: In the "Sair do Abditum?" dialog, press Enter (Sim). The application closes.
result: pass.

### 12. Ctrl+Q — Exit confirmation when vault open and clean (Fluxo 4)
expected: Open a vault successfully. Without making changes, press Ctrl+Q. The same "Sair do Abditum?" confirmation dialog appears (not an immediate quit).
result: blocked — file picker not functional; cannot open or create a vault.

### 13. Ctrl+Q — Unsaved changes dialog (Fluxo 5)
expected: Open a vault and make a change (if possible in current phase). Press Ctrl+Q. A "Alterações não salvas" dialog appears with "Salvar", "Descartar", and "Voltar" options.
result: blocked — file picker not functional; cannot open or create a vault.

### 14. Ctrl+Q — "Voltar" keeps app running
expected: In the "Alterações não salvas" dialog, press Esc (Voltar). The dialog closes and the app continues running with your unsaved changes intact.
result: blocked — file picker not functional; cannot open or create a vault.

## Summary

total: 14
passed: 5
issues: 2 (both resolved)
pending: 0
blocked: 8

## Gaps

- G1: File picker not functional — blocks tests 4, 5, 6, 7, 8, 12, 13, 14. To be addressed in a dedicated phase.
- G2: File picker formatting issues (tests 2, 3). To be addressed together with G1.
- G3: Decision dialog too wide for short messages (test 10). Fixed in Wave 4 (21ca1e9): boxWidth() now sizes to content, not 80% of terminal.

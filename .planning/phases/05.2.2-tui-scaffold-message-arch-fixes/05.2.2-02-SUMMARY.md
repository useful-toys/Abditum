---
phase: 05.2.2-tui-scaffold-message-arch-fixes
plan: 02
subsystem: tui
tags: [bubbletea, help-modal, keyboard-dispatch, lipgloss]

# Dependency graph
requires:
  - phase: 05.2
    provides: ActionManager, helpModal, modal stack plumbing
provides:
  - F1 toggles help modal (open→close, close→open) without stacking
  - ESC closes help modal when it is the topmost modal
  - Help modal displays bottom action bar with "Esc Fechar"
affects: [05.1, 06, 07, 08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Type-check topmost modal before ActionManager.Dispatch for intercept behavior
    - Bottom action bar within dialog border using lipgloss.Width() for fill calculation

key-files:
  created: []
  modified:
    - internal/tui/root.go
    - internal/tui/help.go

key-decisions:
  - "Help modal intercepts ALL keys when active (not just F1/ESC) — simpler than selective key filtering"
  - "Action bar rendered inside dialog border, not in command bar — per spec D-17/D-18"

patterns-established:
  - "Modal type-check before dispatch: check if topmost modal is specific type before ActionManager.Dispatch"
  - "Action bar within dialog: content + actionBar stacked, then wrapped in border"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-04-04
---

# Phase 05.2.2 Plan 02: Help Modal Dispatch Fix + Bottom Action Bar Summary

**Fix help modal F1 stacking bug (dispatch order intercept), ESC close failure, and add spec-required "Esc Fechar" bottom action bar**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-04T03:30:00Z
- **Completed:** 2026-04-04T03:35:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- F1 now toggles help modal (pushes when closed, intercepts when open to pop it)
- ESC closes help modal by intercepting key input before ActionManager.Dispatch
- Help modal displays "─ Esc Fechar ─" bottom action bar within dialog border
- Old "Esc or F1 close this help" footer text removed from buildContent()

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix F1/ESC dispatch — help modal must intercept keys when open** - `0aaaca5` (fix)
2. **Task 2: Add bottom action bar to help modal dialog** - `12e228a` (feat)

**Plan metadata:** TBD (docs: complete help-modal-dispatch-and-action-bar plan)

## Files Created/Modified

- `internal/tui/root.go` — Added helpModal type check before ActionManager.Dispatch in KeyPressMsg case
- `internal/tui/help.go` — Added bottom action bar in View(), removed old footer from buildContent()

## Decisions Made

- Help modal intercepts ALL keys when active (not just F1/ESC) — simpler than selective key filtering, ensures no other global actions fire while modal is open
- Action bar rendered inside dialog border, not in command bar — per spec D-17/D-18, the bar is part of the dialog itself
- Fill calculation uses `boxW - 4` (accounting for border + padding) with `lipgloss.Width()` for accurate character counting

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Help modal dispatch bug fixed — F1 toggles correctly, ESC closes correctly
- Bottom action bar implemented per design spec
- Ready for Phase 05.2.1 (help modal full dialog rewrite, command bar truncation, spinner frame fix, PoC modal pattern alignment)

---

*Phase: 05.2.2-tui-scaffold-message-arch-fixes*
*Completed: 2026-04-04*

---
phase: 05.2.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/messages.go
  - internal/tui/root.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Message bar shows semantic symbol + colored text + continuous border"
    - "Long messages truncate with ellipsis without breaking border"
    - "root.go uses RenderMessageBar() instead of raw styled text"
  artifacts:
    - path: "internal/tui/messages.go"
      provides: "RenderMessageBar with semantic text coloring and truncation"
      contains: "symStyle.Render(msg.Text)"
    - path: "internal/tui/root.go"
      provides: "renderFrame calls RenderMessageBar"
      contains: "RenderMessageBar(m.messages.Current(), m.width)"
  key_links:
    - from: "internal/tui/root.go"
      to: "internal/tui/messages.go"
      via: "RenderMessageBar call in renderFrame"
      pattern: "RenderMessageBar\\(m\\.messages\\.Current"
---

<objective>
Fix message bar rendering in root.go and add semantic text coloring + truncation to RenderMessageBar().

Purpose: root.go currently renders raw text without symbols, borders, or semantic colors (bugs 1, 2, 3, 7, 8). RenderMessageBar() exists but only colors the symbol, not the text, and lacks truncation.
Output: root.go uses RenderMessageBar(), messages.go applies semantic colors to full text and truncates with U+2026 ellipsis.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05.2.2-tui-scaffold-message-arch-fixes/05.2.2-CONTEXT.md

@internal/tui/messages.go — RenderMessageBar() (bugs 2, 3, 7, 8)
@internal/tui/root.go — renderFrame() line 314 (bug 1)
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/tui/messages.go:
```go
type MsgKind int
// MsgSuccess, MsgInfo, MsgWarn, MsgError, MsgBusy, MsgHint

type DisplayMessage struct {
    Text  string
    Kind  MsgKind
    Frame int // animation frame for MsgBusy (0-3)
}

func RenderMessageBar(msg *DisplayMessage, width int) string
```

From internal/tui/root.go:
```go
// Line 310-314 — CURRENT (broken):
var msgText string
if msg := m.messages.Current(); msg != nil {
    msgText = msg.Text
}
msgBar := msgBarStyle.Render("  " + msgText)

// Line 291:
const msgBarH = 1
```

Semantic color tokens (from CONTEXT.md D-05):
- MsgSuccess: `#9ece6a`
- MsgInfo: `#7dcfff`
- MsgWarn: `#e0af68`
- MsgError: `#f7768e` + Bold
- MsgBusy: `#7aa2f7`
- MsgHint: `#565f89` + Italic
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix RenderMessageBar — apply semantic color to text + add truncation</name>
  <files>internal/tui/messages.go</files>
  <read_first>
    - internal/tui/messages.go
    - .planning/phases/05.2.2-tui-scaffold-message-arch-fixes/05.2.2-CONTEXT.md
  </read_first>
  <action>
    In RenderMessageBar() (messages.go), make these changes:

    1. **Apply semantic color to text** (D-04, D-05): Change line 155 from:
       `content := symStyle.Render(symbol) + " " + msg.Text`
       to:
       `content := symStyle.Render(symbol) + " " + symStyle.Render(msg.Text)`
       This ensures symbol AND text use the same semantic color token.

    2. **Add truncation with U+2026 ellipsis** (D-06 through D-10):
       Before the fill calculation (before line 159), add truncation logic:
       - Calculate `prefixWidth = lipgloss.Width(prefix)` (currently ~3)
       - Calculate `suffixStartWidth = lipgloss.Width(suffixStart)` (currently ~2)
       - Calculate `symbolWidth = lipgloss.Width(symStyle.Render(symbol) + " ")` (symbol + 1 space per D-11)
       - Available text width = `width - prefixWidth - suffixStartWidth`
       - If available text width <= symbolWidth: show only symbol + border (no text)
       - Otherwise: truncate text to fit available width minus symbol width
       - Truncation algorithm: iteratively shorten text until `lipgloss.Width(symStyle.Render(symbol) + " " + symStyle.Render(truncated))` fits, then append `…` (U+2026) if text was actually truncated
       - Use `lipgloss.Width()` for ALL width calculations — NEVER `len(s)` (D-07)
       - Ensure border `─` remains continuous and closes correctly at any width (D-08)
       - Truncate at end of text, before suffix (D-09)

    3. **Ensure exactly 1 space between symbol and text** (D-11): The `" "` in the content string is already correct — verify no extra spaces exist.

    4. **Prefix padding**: Keep 2 spaces before the border prefix (`──`) to align with header text (D-12). The current `"──" ` prefix is correct.
  </action>
  <acceptance_criteria>
    - messages.go: `symStyle.Render(msg.Text)` appears in RenderMessageBar (text colored, not plain)
    - messages.go: truncation logic present using `lipgloss.Width()` (not `len()`)
    - messages.go: `…` (U+2026) character used as truncation suffix
    - messages.go: exactly 1 space between symbol and text in content construction
    - `go build ./internal/tui/` succeeds
    - `go test ./internal/tui/...` passes (existing tests)
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/tui/ && go test ./internal/tui/... -count=1</automated>
  </verify>
  <done>RenderMessageBar applies semantic color to full text (symbol + text same color), truncates long messages with U+2026 ellipsis, maintains continuous border, uses exactly 1 space between symbol and text</done>
</task>

<task type="auto">
  <name>Task 2: Wire root.go to use RenderMessageBar() instead of raw text</name>
  <files>internal/tui/root.go</files>
  <read_first>
    - internal/tui/root.go
    - internal/tui/messages.go
  </read_first>
  <action>
    In renderFrame() (root.go), replace the message bar section (lines 310-314):

    CURRENT:
    ```go
    var msgText string
    if msg := m.messages.Current(); msg != nil {
        msgText = msg.Text
    }
    msgBar := msgBarStyle.Render("  " + msgText)
    ```

    REPLACE WITH:
    ```go
    msgBar := RenderMessageBar(m.messages.Current(), m.width)
    ```

    Also remove the `msgBarStyle` variable declaration on line 286 since it's no longer needed:
    ```go
    msgBarStyle := lipgloss.NewStyle().Width(m.width).Foreground(lipgloss.Color("245"))
    ```
    Delete this line entirely — RenderMessageBar() handles all styling.

    Keep `const msgBarH = 1` (D-03) — the bar remains 1 line tall.

    Verify the `strings` import is still needed (it's used elsewhere in renderShortcuts and renderFrame). If not, remove it.
  </action>
  <acceptance_criteria>
    - root.go: `RenderMessageBar(m.messages.Current(), m.width)` present in renderFrame()
    - root.go: `msgBarStyle` variable does NOT exist (removed)
    - root.go: no raw `"  " + msgText` pattern in renderFrame()
    - `go build ./internal/tui/` succeeds
    - `go build ./cmd/abditum/` succeeds
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/tui/ && go build ./cmd/abditum/</automated>
  </verify>
  <done>root.go renderFrame() calls RenderMessageBar(m.messages.Current(), m.width) instead of raw styled text, msgBarStyle variable removed, full project compiles</done>
</task>

</tasks>

<verification>
- Both tasks compile cleanly
- PoC binary (`go run ./cmd/poc-mensagens/`) shows colored text in message bar (not just colored symbol)
- Long message (F10 in PoC) truncates with … ellipsis without breaking border
</verification>

<success_criteria>
- [ ] messages.go: RenderMessageBar colors both symbol AND text with same semantic token
- [ ] messages.go: RenderMessageBar truncates text with U+2026 when exceeding width
- [ ] messages.go: Truncation uses lipgloss.Width() — no len() for width calculation
- [ ] messages.go: Exactly 1 space between symbol and text
- [ ] root.go: renderFrame() calls RenderMessageBar() — no raw msgBarStyle usage
- [ ] root.go: msgBarStyle variable removed
- [ ] `go build ./...` succeeds
- [ ] `go test ./internal/tui/...` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05.2.2-tui-scaffold-message-arch-fixes/05.2.2-01-SUMMARY.md`
</output>

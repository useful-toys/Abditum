---
phase: 05.2.2-tui-scaffold-message-arch-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05.2.2-tui-scaffold-message-arch-fixes/05.2.2-03-PLAN.md
autonomous: true
requirements: []
gap_closure: true
---

<objective>
Formalize post-verification bug fixes already applied in commit 1f7b0ef — error icon and spinner frame order corrections — with documentation and automated verification.

Purpose: Close 2 gaps found after VERIFICATION.md passed, ensuring spec compliance with tui-design-system-novo.md is permanently verified.
Output: Gap closure plan with automated verification that both fixes match the design system spec.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2.2-tui-scaffold-message-arch-fixes/05.2.2-CONTEXT.md
@.planning/phases/05.2.2-tui-scaffold-message-arch-fixes/05.2.2-VERIFICATION.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/tui/messages.go
@tui-design-system-novo.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/tui/messages.go:
```go
// Spinner frames (line 127)
spinnerFrames := []string{"◐", "◓", "◑", "◒"}

// Error symbol (line 142)
symbol = "✕"
```

From tui-design-system-novo.md:
- Line 336: `✕` (U+2715, "heavy multiplication X") — Erro
- Line 338: `◐ ◓ ◑ ◒` — Spinner de atividade
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Document and verify error icon + spinner frame fixes</name>
  <files>.planning/phases/05.2.2-tui-scaffold-message-arch-fixes/05.2.2-03-SUMMARY.md</files>
  <action>
    Both bugs were already fixed in commit 1f7b0ef. This task formalizes the fix with automated verification:

    1. Verify error icon: grep `internal/tui/messages.go` for the error symbol on line 142. Confirm it is `✕` (U+2715, "heavy multiplication X") — NOT `✗` (U+2717, "ballot X"). Cross-reference with tui-design-system-novo.md line 336.

    2. Verify spinner frame order: grep `internal/tui/messages.go` for `spinnerFrames` on line 127. Confirm order is `[]string{"◐", "◓", "◑", "◒"}` — NOT `[]string{"◐", "◓", "◒", "◑"}`. Cross-reference with tui-design-system-novo.md line 338.

    3. Create 05.2.2-03-SUMMARY.md documenting:
       - Gap 1 (error icon): was `✗` (U+2717), now `✕` (U+2715), spec line 336, fixed in commit 1f7b0ef
       - Gap 2 (spinner order): was `["◐", "◓", "◒", "◑"]`, now `["◐", "◓", "◑", "◒"]`, spec line 338, fixed in commit 1f7b0ef
       - Verification commands and results
       - Confirmation that both fixes match tui-design-system-novo.md spec
  </action>
  <verify>
    <automated>grep -n 'symbol = "✕"' internal/tui/messages.go && grep -n 'spinnerFrames := \[\]string{"◐", "◓", "◑", "◒"}' internal/tui/messages.go</automated>
  </verify>
  <done>Both grep commands return matching lines confirming correct error icon (✕ U+2715) and spinner frame order (◐, ◓, ◑, ◒); summary file exists documenting the gap closure</done>
</task>

</tasks>

<verification>
- Error icon in messages.go matches tui-design-system-novo.md line 336 (`✕` U+2715)
- Spinner frame order in messages.go matches tui-design-system-novo.md line 338 (`◐ ◓ ◑ ◒`)
- Summary file created documenting both gap closures
</verification>

<success_criteria>
- [ ] Automated verification confirms error icon is `✕` (U+2715) not `✗` (U+2717)
- [ ] Automated verification confirms spinner order is `["◐", "◓", "◑", "◒"]` not `["◐", "◓", "◒", "◑"]`
- [ ] 05.2.2-03-SUMMARY.md exists with gap closure documentation
</success_criteria>

<output>
After completion, create `.planning/phases/05.2.2-tui-scaffold-message-arch-fixes/05.2.2-03-SUMMARY.md`
</output>

---
phase: 06.2-adequacao-design-system
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/help.go
  - internal/tui/help_test.go
autonomous: true
requirements:
  - DS-HELP-01

must_haves:
  truths:
    - "No modal de ajuda, teclas Ctrl simples são exibidas com notação '^' (ex.: 'ctrl+q' → '^Q')"
    - "Teclas funcionais são exibidas em maiúsculas (ex.: 'f1' → 'F1')"
    - "Teclas especiais usam nomes corretos (ex.: 'esc' → 'Esc', 'enter' → 'Enter', 'del' → 'Delete')"
    - "Teclas complexas com múltiplos modificadores usam Title Case (ex.: 'ctrl+alt+shift+q' → 'Ctrl+Alt+Shift+Q')"
    - "Setas usam símbolos Unicode (ex.: 'up' → '↑', 'down' → '↓', 'left' → '←', 'right' → '→')"
  artifacts:
    - path: "internal/tui/help.go"
      provides: "formatKeyForHelp() function and updated buildContentLines()"
      exports: ["formatKeyForHelp"]
  key_links:
    - from: "helpModal.buildContentLines()"
      to: "formatKeyForHelp()"
      via: "call in 2 render sites"
      pattern: "formatKeyForHelp"

must_haves:
  truths:
    - "formatKeyForHelp('ctrl+q') == '^Q'"
    - "formatKeyForHelp('ctrl+alt+shift+q') == 'Ctrl+Alt+Shift+Q'"
    - "formatKeyForHelp('f1') == 'F1'"
    - "formatKeyForHelp('shift+f6') == 'Shift+F6'"
    - "formatKeyForHelp('del') == 'Delete'"
    - "formatKeyForHelp('esc') == 'Esc'"
    - "formatKeyForHelp('up') == '↑'"
    - "O modal de ajuda renderiza os atalhos com as teclas formatadas (golden files atualizados)"
  artifacts:
    - path: "internal/tui/help.go"
      provides: "formatKeyForHelp() function"
  key_links:
    - from: "buildContentLines()"
      to: "formatKeyForHelp()"
      via: "substitui act.Keys[0] direto"
      pattern: "formatKeyForHelp\\(act\\.Keys\\[0\\]\\)"
---

<objective>
Adicionar `formatKeyForHelp(raw string) string` em `help.go` que converte o formato raw do bubbletea (ex.: "ctrl+q") para a notação do design system (ex.: "^Q"), e aplicar a função nas duas chamadas de `buildContentLines()`.

Purpose: Desvio 10 do CONTEXT.md — o modal de ajuda exibe teclas no formato interno do bubbletea em vez da notação do design system.
Output: help.go com formatKeyForHelp(); help_test.go com testes unitários da função + golden files regenerados.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-adequacao-design-system/06.2-CONTEXT.md

<interfaces>
<!-- De help.go — buildContentLines() usa act.Keys[0] em dois lugares -->

// Linha ~234 (group 0):
lines = append(lines, "  "+keyStyle.Render(act.Keys[0])+"  "+act.Description)

// Linha ~246 (grupos com label):
lines = append(lines, "  "+keyStyle.Render(act.Keys[0])+"  "+act.Description)

// Ambas devem usar formatKeyForHelp(act.Keys[0]) em vez de act.Keys[0] diretamente.

// Regras de conversão (da spec tui-specification-novo.md linhas 573–596):
// "ctrl+X" (uma letra)      → "^X" (maiúsculo, sem espaços)   ex: ctrl+q → ^Q
// "f1"–"f12"                → "F1"–"F12"
// "shift+f6"                → "Shift+F6"
// "ctrl+alt+shift+q"        → "Ctrl+Alt+Shift+Q" (complexo: não usa ^)
// "del"                     → "Delete"
// "insert"                  → "Insert"
// "esc"                     → "Esc"
// "enter"                   → "Enter"
// "space"                   → "Space"
// "backspace"               → "Backspace"
// "tab"                     → "Tab"
// "up"                      → "↑"
// "down"                    → "↓"
// "left"                    → "←"
// "right"                   → "→"
// "pgup"                    → "PgUp"
// "pgdown"                  → "PgDn"
// "home"                    → "Home"
// "end"                     → "End"
// default                   → strings.ToUpper(raw) (fallback seguro)

// Definição de "ctrl simples": exatamente "ctrl+" seguido de uma única letra (a-z)
// — sem outros modificadores (sem "shift+", sem "alt+")
// Verificar: strings.HasPrefix(raw, "ctrl+") && len(raw) == 7 (ctrl+ = 5 chars + 1 letra = 6... careful)
// Mais robusto: após remover "ctrl+", verificar se remaining é len==1 e é letra
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatKeyForHelp() — implementação TDD + aplicar em buildContentLines()</name>
  <files>internal/tui/help.go, internal/tui/help_test.go</files>
  <behavior>
    - formatKeyForHelp("ctrl+q") == "^Q"
    - formatKeyForHelp("ctrl+s") == "^S"
    - formatKeyForHelp("ctrl+alt+shift+q") == "Ctrl+Alt+Shift+Q"   // complexo: não usa ^
    - formatKeyForHelp("ctrl+alt+q") == "Ctrl+Alt+Q"               // dois modificadores: não usa ^
    - formatKeyForHelp("f1") == "F1"
    - formatKeyForHelp("f12") == "F12"
    - formatKeyForHelp("shift+f6") == "Shift+F6"
    - formatKeyForHelp("del") == "Delete"
    - formatKeyForHelp("insert") == "Insert"
    - formatKeyForHelp("esc") == "Esc"
    - formatKeyForHelp("enter") == "Enter"
    - formatKeyForHelp("space") == "Space"
    - formatKeyForHelp("backspace") == "Backspace"
    - formatKeyForHelp("tab") == "Tab"
    - formatKeyForHelp("up") == "↑"
    - formatKeyForHelp("down") == "↓"
    - formatKeyForHelp("left") == "←"
    - formatKeyForHelp("right") == "→"
    - formatKeyForHelp("pgup") == "PgUp"
    - formatKeyForHelp("pgdown") == "PgDn"
    - formatKeyForHelp("home") == "Home"
    - formatKeyForHelp("end") == "End"
    - formatKeyForHelp("xyz") == "XYZ"  // fallback: ToUpper
  </behavior>
  <action>
**RED:** Adicionar em `help_test.go` uma tabela de testes `TestFormatKeyForHelp` com todos os casos do bloco `<behavior>`. Executar — deve falhar (função não existe).

**GREEN:** Adicionar em `help.go` (após os imports, antes de `type helpModal`) a função:

```go
// formatKeyForHelp converts a raw bubbletea key string to the display format
// defined by the Abditum design system (tui-specification-novo.md §573-596).
//
// Rules:
//   - Simple ctrl (ctrl+single-letter): "ctrl+q" → "^Q"
//   - Complex multi-modifier: "ctrl+alt+shift+q" → "Ctrl+Alt+Shift+Q"
//   - Function keys: "f1" → "F1", "shift+f6" → "Shift+F6"
//   - Named special keys: "esc" → "Esc", "del" → "Delete", etc.
//   - Arrow keys: "up" → "↑", "down" → "↓", "left" → "←", "right" → "→"
//   - Fallback: strings.ToUpper(raw)
func formatKeyForHelp(raw string) string {
    // Named specials
    switch raw {
    case "esc":       return "Esc"
    case "enter":     return "Enter"
    case "space":     return "Space"
    case "backspace": return "Backspace"
    case "tab":       return "Tab"
    case "del":       return "Delete"
    case "insert":    return "Insert"
    case "pgup":      return "PgUp"
    case "pgdown":    return "PgDn"
    case "home":      return "Home"
    case "end":       return "End"
    case "up":        return "↑"
    case "down":      return "↓"
    case "left":      return "←"
    case "right":     return "→"
    }
    // Function keys: f1–f12
    if len(raw) >= 2 && raw[0] == 'f' {
        suffix := raw[1:]
        allDigits := true
        for _, c := range suffix {
            if c < '0' || c > '9' { allDigits = false; break }
        }
        if allDigits && len(suffix) > 0 {
            return "F" + suffix
        }
    }
    // Ctrl combinations
    if strings.HasPrefix(raw, "ctrl+") {
        rest := raw[5:] // after "ctrl+"
        // Simple: ctrl + single letter (e.g. "ctrl+q" → rest == "q")
        if len(rest) == 1 {
            return "^" + strings.ToUpper(rest)
        }
        // Complex multi-modifier (e.g. "ctrl+alt+shift+q"):
        // Title-case each segment separated by "+"
        parts := strings.Split(raw, "+")
        for i, p := range parts {
            if len(p) > 0 {
                parts[i] = strings.ToUpper(p[:1]) + p[1:]
            }
        }
        return strings.Join(parts, "+")
    }
    // shift+fN (e.g. "shift+f6" → "Shift+F6")
    if strings.HasPrefix(raw, "shift+") {
        rest := raw[6:]
        return "Shift+" + formatKeyForHelp(rest)
    }
    // Fallback
    return strings.ToUpper(raw)
}
```

**Aplicar nos 2 sites em `buildContentLines()`:**
Substituir `act.Keys[0]` por `formatKeyForHelp(act.Keys[0])` nas duas linhas de append que constroem a entrada de ação.

**Regenerar golden files** para help_test.go após a mudança:
```
go test ./internal/tui/... -run TestHelpModal -update
```
(se o flag -update não existir para help, usar `-update-golden` ou o equivalente do projeto — verificar como os golden files de help são atualizados em help_test.go)

**Strings package**: já importado em `help.go` — verificar e adicionar se necessário.
  </action>
  <verify>
    <automated>go test ./internal/tui/... -run "TestFormatKeyForHelp|TestHelpModal" -v -count=1</automated>
  </verify>
  <done>
    - TestFormatKeyForHelp: todos os 23 casos passam
    - TestHelpModal: golden files atualizados e passando
    - go test ./internal/tui/... sem regressões
  </done>
</task>

</tasks>

<verification>
go test ./internal/tui/... -count=1
</verification>

<success_criteria>
- formatKeyForHelp() existe em help.go e passa todos os testes de tabela
- buildContentLines() usa formatKeyForHelp() nas duas chamadas de render
- Golden files do helpModal regenerados e passando
- Todos os testes existentes do pacote tui passam sem regressão
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06.2-adequacao-design-system/06.2-04-SUMMARY.md`
</output>

---
phase: 06.2-adequacao-design-system
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/flow_create_vault.go
  - internal/tui/flow_create_vault_test.go
  - internal/tui/flow_save_and_exit.go
  - internal/tui/flow_save_and_exit_test.go
autonomous: true
requirements:
  - DS-DIALOG-06
  - DS-DIALOG-07
  - DS-DIALOG-08
  - DS-DIALOG-09

must_haves:
  truths:
    - "Ao criar vault com vault modificado: exibe Decision SeverityAlert, título 'Criar novo cofre', msg 'Cofre modificado. Salvar ou descartar?', ações S Salvar / D Descartar / Esc Voltar"
    - "Overwrite dialog: título 'Criar novo cofre', msg interpola nome do arquivo ('Arquivo [Nome] já existe. Sobrescrever?'), ações S Sobrescrever / I Outro caminho / Esc Voltar, SeverityAlert"
    - "Senha fraca: ação default é P Prosseguir (não R Revisar); ação secondary é R Revisar; Esc Voltar cancela o fluxo inteiro"
    - "Conflito externo no save-and-exit: título 'Salvar cofre', msg 'Arquivo modificado externamente. Sobrescrever ou salvar como novo?', ações S Sobrescrever / N Salvar como novo / Esc Voltar"
    - "Ação N Salvar como novo abre FilePicker em modo save para escolher caminho alternativo antes de salvar e sair"
  artifacts:
    - path: "internal/tui/flow_create_vault.go"
      provides: "Diálogos de criação de vault conformes com spec"
    - path: "internal/tui/flow_save_and_exit.go"
      provides: "Diálogo de conflito externo conforme spec com ação N Salvar como novo"
  key_links:
    - from: "flow_create_vault.go Init()"
      to: "Decision(SeverityAlert, 'Criar novo cofre')"
      via: "dirty check"
      pattern: "Decision.*SeverityAlert.*Criar novo cofre"
    - from: "flow_create_vault.go filePickerResult"
      to: "Decision(SeverityAlert, 'Criar novo cofre')"
      via: "overwrite check"
      pattern: "Decision.*SeverityAlert.*já existe"
    - from: "flow_save_and_exit.go extModDetectedMsg"
      to: "Decision(SeverityDestructive, 'Salvar cofre')"
      via: "conflict dialog"
      pattern: "Decision.*Salvar cofre"
---

<objective>
Corrigir os desvios 6, 7, 8 em `flow_create_vault.go` (dirty check, overwrite dialog, weak password dialog) e o desvio 9 em `flow_save_and_exit.go` (external modification conflict dialog com nova ação "N Salvar como novo").

Purpose: Itens 6–9 do CONTEXT.md. Inclui uma mudança comportamental relevante (inversão do default no weak-pwd dialog) e nova feature (N Salvar como novo no conflict dialog).
Output: 2 fluxos atualizados com conformidade total ao Catálogo de Diálogos da spec.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-adequacao-design-system/06.2-CONTEXT.md

<interfaces>
<!-- De flow_create_vault.go atual -->

// Tipos de mensagem existentes
type overwriteConfirmedMsg struct{}   // isModalResult
type overwriteCancelledMsg struct{}   // isModalResult
type weakPwdProceedMsg struct{ Password []byte }  // isModalResult
type weakPwdReviseMsg struct{}        // isModalResult

// De flow_save_and_exit.go atual
type extModDetectedMsg struct{}      // isModalResult
type extModOverwriteMsg struct{}     // isModalResult
type extModCancelMsg struct{}        // isModalResult
type saveAndExitReadyMsg struct{}
type saveAndExitOKMsg struct{}

// File picker modal
type filePickerModal struct{ mode FilePickerMode }
const (
    FilePickerFile      FilePickerMode = iota
    FilePickerDirectory
    FilePickerAny
)

// filepath.Base(path) retorna "cofre.abditum" para "/home/user/cofre.abditum"
// strings.TrimSuffix(base, ".abditum") retorna nome sem extensão

// Decision + Acknowledge assinaturas (ver plano 01/02)
// endFlow() tea.Cmd
// pushModalMsg{modal: ...}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: flow_create_vault.go — desvios 6, 7, 8</name>
  <files>internal/tui/flow_create_vault.go</files>
  <action>
**Desvio 6 — Init() dirty check** (linha ~51):

Adicionar novo tipo de mensagem:
```go
// createVaultSaveBeforeMsg — emitted by "S Salvar" in dirty-check dialog when creating vault.
type createVaultSaveBeforeMsg struct{}
func (createVaultSaveBeforeMsg) isModalResult() {}
```

Substituir `Acknowledge(SeverityNeutral, "Alterações não salvas", ...)` por:
```go
return Decision(SeverityAlert, "Criar novo cofre",
    "Cofre modificado. Salvar ou descartar?",
    DecisionAction{Key: "S", Label: "Salvar", Default: true,
        Cmd: func() tea.Msg { return createVaultSaveBeforeMsg{} }},
    []DecisionAction{
        {Key: "D", Label: "Descartar",
            Cmd: func() tea.Msg {
                return pushModalMsg{modal: &filePickerModal{mode: FilePickerFile}}
            }},
    },
    DecisionAction{Key: "Esc", Label: "Voltar"})
```

Adicionar handler no `Update()`:
```go
case createVaultSaveBeforeMsg:
    mgr := f.mgr
    messages := f.messages
    return func() tea.Msg {
        if err := mgr.Salvar(); err != nil {
            messages.Show(MsgError, "Não foi possível salvar o cofre.", 5, false)
            return endFlowMsg{}
        }
        f.state = statePickFile
        return pushModalMsg{modal: &filePickerModal{mode: FilePickerFile}}
    }
```

---

**Desvio 7 — filePickerResult overwrite dialog** (linha ~75):

Substituir o `Decision()` existente:
- Título: `"Arquivo existe"` → `"Criar novo cofre"`
- Mensagem: interpolar nome do arquivo usando `filepath.Base` + `strings.TrimSuffix`:
  ```go
  baseName := strings.TrimSuffix(filepath.Base(f.targetPath), ".abditum")
  msg := fmt.Sprintf("Arquivo '%s' já existe. Sobrescrever?", baseName)
  ```
- Ação default: `{Key: "Enter", Label: "Sobrescrever", ...}` → `{Key: "S", Label: "Sobrescrever", Default: true, Cmd: func() tea.Msg { return overwriteConfirmedMsg{} }}`
- Adicionar ação middle: `{Key: "I", Label: "Outro caminho", Cmd: func() tea.Msg { return overwriteCancelledMsg{} }}`
- Ação cancel: `{Key: "Esc", Label: "Voltar", Cmd: func() tea.Msg { return overwriteCancelledMsg{} }}`
- Severidade: `SeverityDestructive` → `SeverityAlert`

Garantir que `"fmt"`, `"path/filepath"` e `"strings"` estão nos imports (verificar — `"os"` já está, `"path/filepath"` pode precisar ser adicionado).

---

**Desvio 8 — pwdCreatedMsg weak password dialog** (linha ~96):

Substituir o `Decision()` existente:
- Título: `"Senha fraca"` → `"Criar novo cofre"` (via ação default no ctx)
- Mensagem: `"A senha não atende...Deseja prosseguir mesmo assim ou revisar a senha?"` → `"Senha é fraca. Prosseguir ou revisar?"`
- Ação default INVERTIDA: era `R Revisar`, agora deve ser `P Prosseguir`:
  ```go
  DecisionAction{Key: "P", Label: "Prosseguir", Default: true,
      Cmd: func() tea.Msg { return weakPwdProceedMsg{Password: password} }}
  ```
- Ação middle: `{Key: "R", Label: "Revisar", Cmd: func() tea.Msg { crypto.Wipe(password); return weakPwdReviseMsg{} }}`
- Ação cancel (NOVO comportamento): `Esc Voltar` deve cancelar o fluxo inteiro (endFlow), NÃO prosseguir:
  ```go
  DecisionAction{Key: "Esc", Label: "Voltar",
      Cmd: func() tea.Msg { crypto.Wipe(password); return endFlowMsg{} }}
  ```
- Severidade: `SeverityAlert` ✓ (mantém)

**Importante:** O Esc agora cancela o fluxo em vez de prosseguir — mudança comportamental intencional conforme spec (CONTEXT.md item 8).
  </action>
  <verify>
    <automated>go build ./internal/tui/...</automated>
  </verify>
  <done>Compilação limpa; Init() usa Decision(SeverityAlert, "Criar novo cofre"); overwrite dialog interpola nome; weak pwd tem P como default e Esc cancela fluxo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: flow_save_and_exit.go — desvio 9 (conflito externo + N Salvar como novo) + testes</name>
  <files>internal/tui/flow_save_and_exit.go, internal/tui/flow_save_and_exit_test.go, internal/tui/flow_create_vault_test.go</files>
  <behavior>
    - Test (create vault): dirty check usa Decision SeverityAlert, título "Criar novo cofre"
    - Test (create vault): overwrite dialog usa título "Criar novo cofre", mensagem contém nome do arquivo interpolado
    - Test (create vault): weak pwd dialog — ação default é "P" (não "R"); Esc retorna endFlowMsg
    - Test (save-and-exit): extModDetectedMsg → Decision com título "Salvar cofre" (não "Conflito de Modificação")
    - Test (save-and-exit): ação "N" emite extModSaveAsNewMsg
    - Test (save-and-exit): extModSaveAsNewMsg abre filePickerModal (mode FilePickerFile)
  </behavior>
  <action>
**Em `flow_save_and_exit.go`:**

Adicionar novo tipo de mensagem:
```go
// extModSaveAsNewMsg — emitted by "N Salvar como novo" in external modification
// conflict dialog. Opens a file picker so the user can choose an alternate save path.
type extModSaveAsNewMsg struct{}
func (extModSaveAsNewMsg) isModalResult() {}
```

Adicionar novo estado:
```go
const (
    stateCheckExtMod = iota + 20
    stateSaveAndExit
    stateDoneExit
    stateSaveAsNew  // user chose "N Salvar como novo" — waiting for new path
)
```

Substituir o `Decision()` em `extModDetectedMsg` handler:

DE:
```go
return Decision(SeverityDestructive, "Conflito de Modificação",
    "O arquivo do cofre foi modificado externamente desde a última vez que foi carregado.\n\nSobrescrever descartará as alterações externas.",
    DecisionAction{Key: "Enter", Label: "Sobrescrever e sair", Default: true,
        Cmd: func() tea.Msg { return extModOverwriteMsg{} }},
    nil,
    DecisionAction{Key: "Esc", Label: "Voltar",
        Cmd: func() tea.Msg { return extModCancelMsg{} }})
```

PARA:
```go
return Decision(SeverityDestructive, "Salvar cofre",
    "Arquivo modificado externamente. Sobrescrever ou salvar como novo?",
    DecisionAction{Key: "S", Label: "Sobrescrever", Default: true,
        Cmd: func() tea.Msg { return extModOverwriteMsg{} }},
    []DecisionAction{
        {Key: "N", Label: "Salvar como novo",
            Cmd: func() tea.Msg { return extModSaveAsNewMsg{} }},
    },
    DecisionAction{Key: "Esc", Label: "Voltar",
        Cmd: func() tea.Msg { return extModCancelMsg{} }})
```

Adicionar handler para `extModSaveAsNewMsg` no `Update()`:
```go
case extModSaveAsNewMsg:
    // User chose "Salvar como novo": open file picker to choose alternate path
    f.state = stateSaveAsNew
    return func() tea.Msg {
        return pushModalMsg{modal: &filePickerModal{mode: FilePickerFile}}
    }
```

Adicionar handler para `filePickerResult` no `Update()` (para quando vem do save-as-new):
```go
case filePickerResult:
    if f.state != stateSaveAsNew {
        return nil
    }
    if msg.Cancelled {
        return endFlow()
    }
    f.path = msg.Path
    return f.doSave()
```

O `doSave()` existente salva em `f.path` — que agora foi atualizado para o novo caminho.

Adicionar os testes no `flow_save_and_exit_test.go` (crie o arquivo se não existir) e adicionar testes de desvios 6/7/8 no `flow_create_vault_test.go` existente.
  </action>
  <verify>
    <automated>go test ./internal/tui/... -run "TestSaveAndExit|TestCreateVault|TestFlowCreate|TestFlowSaveAndExit" -v -count=1</automated>
  </verify>
  <done>
    - go build ./internal/tui/... limpo
    - Testes novos passam
    - go test ./internal/tui/... sem regressões
  </done>
</task>

</tasks>

<verification>
go test ./internal/tui/... -count=1
</verification>

<success_criteria>
- flow_create_vault.go: Init dirty-check usa Decision(SeverityAlert, "Criar novo cofre")
- flow_create_vault.go: overwrite dialog interpolates filename, usa SeverityAlert, tem ação "I Outro caminho"
- flow_create_vault.go: weak pwd dialog tem P=default, R=secondary, Esc=endFlow (não prosseguir)
- flow_save_and_exit.go: conflict dialog usa título "Salvar cofre", msg curta, ações S/N/Esc
- flow_save_and_exit.go: ação "N" abre file picker e salva no novo caminho
- Todos os testes existentes passam sem regressão
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06.2-adequacao-design-system/06.2-03-SUMMARY.md`
</output>

---
phase: 06.2-adequacao-design-system
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/flow_open_vault.go
  - internal/tui/flow_open_vault_test.go
autonomous: true
requirements:
  - DS-DIALOG-03
  - DS-DIALOG-04
  - DS-DIALOG-05

must_haves:
  truths:
    - "Ao abrir vault com vault modificado: exibe Decision SeverityAlert, título 'Abrir cofre', msg 'Cofre modificado. Salvar ou descartar?', ações S Salvar / D Descartar / Esc Voltar"
    - "Ação 'S Salvar' salva o vault atual e continua o fluxo de abertura"
    - "Senha incorreta: exibe Acknowledge SeverityError, título 'Abrir cofre', mensagem 'Senha incorreta. Necessário tentar novamente.', ação Enter OK"
    - "Arquivo corrompido/inválido: exibe Acknowledge SeverityError, título 'Abrir cofre', mensagem 'Arquivo corrompido ou inválido. Necessário fechar.', ação Enter OK"
    - "Versão não suportada: exibe Acknowledge SeverityError, título 'Abrir cofre', mensagem 'Arquivo inválido ou versão não suportada. Necessário corrigir.', ação Enter OK"
  artifacts:
    - path: "internal/tui/flow_open_vault.go"
      provides: "Diálogos do fluxo de abertura corrigidos conforme spec"
  key_links:
    - from: "flow_open_vault.go Init()"
      to: "Decision(SeverityAlert)"
      via: "dirty check branch"
      pattern: "Decision.*SeverityAlert.*Abrir cofre"
    - from: "flow_open_vault.go pwdEnteredMsg"
      to: "Acknowledge(SeverityError)"
      via: "ErrAuthFailed branch"
      pattern: "Acknowledge.*SeverityError.*Abrir cofre"
---

<objective>
Corrigir os 3 desvios de spec no fluxo de abertura de vault (`flow_open_vault.go`): converter dirty-check de Acknowledge→Decision com 3 ações, converter erros de senha e arquivo de message-bar→Acknowledge dialog, adicionar novo tipo de msg `openVaultSaveBeforeMsg`.

Purpose: Itens 3, 4, 5 do CONTEXT.md. Nenhuma lógica nova de negócio — apenas conformidade de UI com o Catálogo de Diálogos.
Output: flow_open_vault.go atualizado; testes atualizados/adicionados.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-adequacao-design-system/06.2-CONTEXT.md

<interfaces>
<!-- De flow_open_vault.go atual -->

// Tipos de mensagem de resultado já existentes
type filePickerResult struct { Cancelled bool; Path string }
type pwdEnteredMsg struct { Password []byte }
type flowCancelledMsg struct{}
type vaultOpenedMsg struct { Path string; Metadata storage.FileMetadata }

// openVaultFlow.Init() — dirty check atual (a substituir):
// return Acknowledge(SeverityNeutral, "Alterações não salvas",
//     "Existem alterações não salvas. Abrir outro cofre descartará as mudanças.", nil)

// openVaultFlow.Update() — erros de senha atual (a substituir):
// f.messages.Show(MsgWarn, "Senha incorreta. Tente novamente", 3, false)

// openVaultFlow.Update() — erros de arquivo atual (a substituir):
// f.messages.Show(MsgError, errMsg, 5, false)

// Decision — 3-action confirm dialog
func Decision(severity Severity, title, body string,
    defaultAction DecisionAction,
    middleActions []DecisionAction,
    cancelAction DecisionAction) tea.Cmd

// Acknowledge — single-action info/error dialog
func Acknowledge(severity Severity, title, body string, onOK tea.Cmd) tea.Cmd

// vaultSaver interface (usado pelo saveAndExitFlow — openVaultFlow pode reutilizar)
type vaultSaver interface { Salvar() error }

// endFlow() helper
func endFlow() tea.Cmd
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desvio 3 — Init dirty check: Acknowledge → Decision com 3 ações</name>
  <files>internal/tui/flow_open_vault.go</files>
  <action>
**Adicionar novo tipo de mensagem de resultado** no arquivo (antes ou depois de `pwdEnteredMsg`):

```go
// openVaultSaveBeforeMsg — emitted by the "S Salvar" action in the dirty-check
// Decision dialog when opening a vault. The flow should save the current vault
// and then continue to the file picker.
type openVaultSaveBeforeMsg struct{}
func (openVaultSaveBeforeMsg) isModalResult() {}
```

**Substituir o `Init()` dirty check** (linhas ~53-59 do arquivo atual):

DE:
```go
return func() tea.Msg {
    return Acknowledge(SeverityNeutral, "Alterações não salvas",
        "Existem alterações não salvas. Abrir outro cofre descartará as mudanças.", nil)
}
```

PARA:
```go
return Decision(SeverityAlert, "Abrir cofre",
    "Cofre modificado. Salvar ou descartar?",
    DecisionAction{Key: "S", Label: "Salvar", Default: true,
        Cmd: func() tea.Msg { return openVaultSaveBeforeMsg{} }},
    []DecisionAction{
        {Key: "D", Label: "Descartar",
            Cmd: func() tea.Msg {
                // Discard and proceed to file picker
                return pushModalMsg{modal: &filePickerModal{}}
            }},
    },
    DecisionAction{Key: "Esc", Label: "Voltar"})
```

**Adicionar handler para `openVaultSaveBeforeMsg` no `Update()`:**
```go
case openVaultSaveBeforeMsg:
    // Save current vault, then proceed to file picker
    mgr := f.mgr
    messages := f.messages
    return func() tea.Msg {
        if err := mgr.Salvar(); err != nil {
            messages.Show(MsgError, "Não foi possível salvar o cofre.", 5, false)
            return endFlowMsg{}
        }
        f.state = statePickFile
        return pushModalMsg{modal: &filePickerModal{}}
    }
```

Também adicionar handler do "D Descartar" — a ação já chama pushModalMsg direto via Cmd, então o caso `filePickerResult` existente trata a continuação naturalmente. Não é necessário novo case no Update.

**Atualizar state machine**: ao receber `openVaultSaveBeforeMsg`, o estado deve ser `statePickFile` após o save.
  </action>
  <verify>
    <automated>go build ./internal/tui/...</automated>
  </verify>
  <done>Compilação limpa; Init() agora usa Decision(SeverityAlert, "Abrir cofre", ...) com 3 ações S/D/Esc.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Desvios 4 e 5 — Erros de senha e arquivo: message bar → Acknowledge dialogs</name>
  <files>internal/tui/flow_open_vault.go, internal/tui/flow_open_vault_test.go</files>
  <behavior>
    - Test: `pwdEnteredMsg` com ErrAuthFailed (tentativa 1) → emite Acknowledge(SeverityError, "Abrir cofre", "Senha incorreta. Necessário tentar novamente.", ...) em vez de messages.Show
    - Test: `pwdEnteredMsg` com ErrAuthFailed (tentativa >= 5) → emite messages.Show + endFlow (comportamento atual mantido)
    - Test: `pwdEnteredMsg` com ErrCorrupted → emite Acknowledge(SeverityError, "Abrir cofre", "Arquivo corrompido ou inválido. Necessário fechar.", Enter OK)
    - Test: `pwdEnteredMsg` com ErrInvalidMagic → emite Acknowledge(SeverityError, "Abrir cofre", "Arquivo inválido ou versão não suportada. Necessário corrigir.", Enter OK)
    - Test: `pwdEnteredMsg` com ErrVersionTooNew → emite Acknowledge(SeverityError, "Abrir cofre", "Arquivo inválido ou versão não suportada. Necessário corrigir.", Enter OK)
  </behavior>
  <action>
No handler `case pwdEnteredMsg:` do `Update()` em `flow_open_vault.go`, localizar o trecho de tratamento de erros pós-load (~linhas 107-129).

**Substituir o bloco de erros de senha incorreta** (ErrAuthFailed, tentativa < 5):

DE:
```go
f.state = statePwdEntry
f.messages.Show(MsgWarn, "Senha incorreta. Tente novamente", 3, false)
return pushModalMsg{modal: &passwordEntryModal{}}
```

PARA:
```go
f.state = statePwdEntry
return Acknowledge(SeverityError, "Abrir cofre",
    "Senha incorreta. Necessário tentar novamente.",
    func() tea.Msg { return pushModalMsg{modal: &passwordEntryModal{}} })
```

**Substituir o bloco de erros de arquivo** (~linhas 118-129):

DE:
```go
errMsg := "Não foi possível abrir o cofre. Arquivo corrompido ou inacessível."
if errors.Is(err, storage.ErrInvalidMagic) {
    errMsg = "Arquivo não é um cofre válido."
} else if errors.Is(err, storage.ErrVersionTooNew) {
    errMsg = "Versão do cofre não é suportada por esta versão do Abditum."
} else if errors.Is(err, storage.ErrCorrupted) {
    errMsg = "Cofre corrompido e não pode ser recuperado."
}
f.messages.Show(MsgError, errMsg, 5, false)
f.state = statePickFile
return pushModalMsg{modal: &filePickerModal{}}
```

PARA (dois casos como na spec):
```go
f.state = statePickFile
if errors.Is(err, storage.ErrInvalidMagic) || errors.Is(err, storage.ErrVersionTooNew) {
    return Acknowledge(SeverityError, "Abrir cofre",
        "Arquivo inválido ou versão não suportada. Necessário corrigir.",
        func() tea.Msg { return pushModalMsg{modal: &filePickerModal{}} })
}
// ErrCorrupted and generic errors
return Acknowledge(SeverityError, "Abrir cofre",
    "Arquivo corrompido ou inválido. Necessário fechar.",
    func() tea.Msg { return pushModalMsg{modal: &filePickerModal{}} })
```

Nota: o import de `"errors"` já está presente no arquivo.

Adicionar/atualizar testes em `flow_open_vault_test.go` cobrindo os 5 behaviors acima.
  </action>
  <verify>
    <automated>go test ./internal/tui/... -run "TestOpenVault|TestFlowOpen" -v -count=1</automated>
  </verify>
  <done>
    - go build ./internal/tui/... limpo
    - Testes novos passam
    - go test ./internal/tui/... sem regressões
  </done>
</task>

</tasks>

<verification>
go test ./internal/tui/... -count=1
</verification>

<success_criteria>
- Init() dirty-check usa Decision(SeverityAlert) com título "Abrir cofre" e 3 ações (S/D/Esc)
- Ação "S Salvar" salva e continua ao file picker via openVaultSaveBeforeMsg
- Senha incorreta usa Acknowledge(SeverityError, "Abrir cofre", "Senha incorreta. Necessário tentar novamente.")
- Erros de arquivo usam os 2 Acknowledge dialogs corretos (corrompido / inválido-versão)
- Todos os testes existentes passam
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06.2-adequacao-design-system/06.2-02-SUMMARY.md`
</output>

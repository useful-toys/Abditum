---
phase: 06.2-adequacao-design-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/root.go
  - internal/tui/root_test.go
autonomous: true
requirements:
  - DS-DIALOG-01
  - DS-DIALOG-02
  - FLOW-6

must_haves:
  truths:
    - "Ctrl+Q sem vault aberto mostra diálogo com título 'Sair do Abditum', ação 'Enter Sair', ação cancel 'Esc Voltar'"
    - "Ctrl+Q com vault modificado mostra diálogo SeverityAlert com título 'Sair do Abditum', mensagem 'Cofre modificado. Salvar ou descartar?', ação default 'S Salvar', ação extra 'D Descartar', ação cancel 'Esc Voltar'"
    - "Ctrl+Alt+Shift+Q fecha o cofre imediatamente sem confirmação, limpa mgr/vaultPath/vaultMetadata/isDirty/activeFlow/modals, retorna para workAreaWelcome"
  artifacts:
    - path: "internal/tui/root.go"
      provides: "Diálogos Ctrl+Q corrigidos e handler Ctrl+Alt+Shift+Q"
  key_links:
    - from: "root.go KeyPressMsg handler"
      to: "Decision()"
      via: "ctrl+q branch"
      pattern: "Decision.*Sair do Abditum"
    - from: "root.go KeyPressMsg handler"
      to: "workAreaWelcome reset"
      via: "ctrl+alt+shift+q branch"
      pattern: "ctrl\\+alt\\+shift\\+q"
---

<objective>
Corrigir os dois diálogos do Ctrl+Q em `root.go` para conformidade com o Catálogo de Diálogos da spec e implementar o Fluxo 6 (bloqueio emergencial Ctrl+Alt+Shift+Q).

Purpose: 3 desvios de spec em root.go (itens 1, 2, e Fluxo 6 do CONTEXT.md). Zero lógica nova — apenas mensagens/títulos/severidades e um novo branch de tecla.
Output: root.go com 3 correções; root_test.go com casos de teste correspondentes.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.2-adequacao-design-system/06.2-CONTEXT.md

<interfaces>
<!-- Tipos e funções relevantes de decision.go / root.go -->

// Severity values
SeverityNeutral     Severity = iota  // border.focused, no symbol
SeverityAlert                        // semantic.warning, ⚠ symbol
SeverityError                        // semantic.error, ✕ symbol
SeverityDestructive                  // semantic.warning border, semantic.error default key

// Decision — 3-action confirm dialog
func Decision(
    severity Severity,
    title, body string,
    defaultAction DecisionAction,       // shown left, Enter triggers it
    middleActions []DecisionAction,     // shown middle (may be nil or slice)
    cancelAction DecisionAction,        // shown right, Esc triggers it
) tea.Cmd

type DecisionAction struct {
    Key     string   // displayed key (e.g. "S", "D", "Enter", "Esc")
    Label   string   // displayed label
    Default bool     // if true, Enter triggers it
    Cancel  bool     // set automatically by Decision() on cancelAction
    Cmd     func() tea.Msg
}

// rootModel fields (from root.go)
type rootModel struct {
    area          workArea
    mgr           *vault.Manager
    vaultPath     string
    vaultMetadata storage.FileMetadata
    isDirty       bool
    activeFlow    flowHandler
    modals        []modalView
    messages      *MessageManager
    actions       *ActionManager
    theme         *Theme
    // ...
}

// workArea constants (from state.go)
const (
    workAreaWelcome workArea = iota
    workAreaVaultTree
    // ...
)

// endFlow() — ends the active flow
func endFlow() tea.Cmd

// startFlowMsg — sent to rootModel to start a new flow
type startFlowMsg struct{ flow flowHandler }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir diálogos Ctrl+Q em root.go (desvios 1 e 2)</name>
  <files>internal/tui/root.go</files>
  <action>
No handler `case tea.KeyPressMsg:` em `root.go`, localizar o branch `if key == "ctrl+q"`.

**Desvio 1 — Fluxos 3 e 4 (sem vault ou vault limpo):**
Alterar o `Decision()` existente na linha ~268:
- Título: mantém `"Sair do Abditum"` ✓
- Mensagem: `"Tem certeza que deseja sair?"` → `"Sair do Abditum?"`
- Ação default: `{Key: "Enter", Label: "Sim", ...}` → `{Key: "Enter", Label: "Sair", Default: true, Cmd: tea.Quit}`
- Ação cancel: `{Key: "Esc", Label: "Não"}` → `{Key: "Esc", Label: "Voltar"}`
- Severidade: `SeverityNeutral` ✓ (mantém)

**Desvio 2 — Fluxo 5 (vault modificado):**
Alterar o `Decision()` existente na linha ~257:
- Título: `"Alterações não salvas"` → `"Sair do Abditum"`
- Mensagem: `"Deseja salvar as alterações antes de sair?"` → `"Cofre modificado. Salvar ou descartar?"`
- Ação default: `{Key: "Enter", Label: "Salvar", Default: true, Cmd: ...}` → `{Key: "S", Label: "Salvar", Default: true, Cmd: ...}` (mesmo Cmd que inicia saveAndExitFlow)
- Ação cancel: `{Key: "Esc", Label: "Voltar"}` ✓ (mantém)
- Severidade: `SeverityNeutral` → `SeverityAlert`

O `Cmd` da ação "S Salvar" permanece idêntico ao atual: inicia `newSaveAndExitFlow(...)`.
  </action>
  <verify>
    <automated>go build ./internal/tui/...</automated>
  </verify>
  <done>Compilação limpa; as duas chamadas Decision() para ctrl+q têm títulos/mensagens/ações corretos conforme spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar Fluxo 6 (Ctrl+Alt+Shift+Q — bloqueio emergencial) em root.go + testes</name>
  <files>internal/tui/root.go, internal/tui/root_test.go</files>
  <behavior>
    - Test: ao pressionar "ctrl+alt+shift+q" sem vault aberto, rootModel retorna ao workAreaWelcome (noop safe — já está lá)
    - Test: ao pressionar "ctrl+alt+shift+q" com vault aberto (mgr != nil), rootModel.mgr volta a nil, rootModel.vaultPath == "", rootModel.isDirty == false, rootModel.activeFlow == nil, rootModel.modals == []
    - Test: ao pressionar "ctrl+alt+shift+q" com diálogos empilhados (len(modals) > 0), modals é esvaziado
    - Test: ação é global — funciona mesmo com inFlowOrModal == true (não deve passar pelo dispatch do ActionManager)
  </behavior>
  <action>
Em `root.go`, no handler `case tea.KeyPressMsg:`, adicionar ANTES do branch `ctrl+q` (e antes do dispatch do ActionManager):

```go
// Fluxo 6: Emergency vault lock — discards all unsaved changes immediately
if key == "ctrl+alt+shift+q" {
    m.mgr = nil
    m.vaultPath = ""
    m.vaultMetadata = storage.FileMetadata{}
    m.isDirty = false
    m.activeFlow = nil
    m.modals = m.modals[:0]
    m.area = workAreaWelcome
    return m, nil
}
```

Posicionar este branch ANTES do branch `ctrl+q` para garantia de que é sempre tratado primeiro.

Em `root_test.go`, adicionar os 4 testes unitários definidos no bloco `<behavior>` acima, usando a infraestrutura existente de testes de rootModel.

**Não registrar Ctrl+Alt+Shift+Q no ActionManager** — é um atalho emergencial não listado na barra de comandos, tratado raw no handler de tecla.
  </action>
  <verify>
    <automated>go test ./internal/tui/... -run "TestCtrlAltShiftQ|TestEmergencyLock|TestFluxo6" -v</automated>
  </verify>
  <done>
    - go build ./internal/tui/... sem erros
    - Testes do Fluxo 6 passam
    - go test ./internal/tui/... sem regressões
  </done>
</task>

</tasks>

<verification>
go test ./internal/tui/... -count=1
</verification>

<success_criteria>
- Diálogos Ctrl+Q (itens 1 e 2 do CONTEXT.md) corrigidos: títulos, mensagens, labels das ações e severidades conforme spec
- Fluxo 6 (Ctrl+Alt+Shift+Q) implementado: limpa mgr/path/metadata/dirty/flow/modals, retorna para workAreaWelcome
- Todos os testes existentes passam sem regressão
- Novos testes para Fluxo 6 passam
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06.2-adequacao-design-system/06.2-01-SUMMARY.md`
</output>

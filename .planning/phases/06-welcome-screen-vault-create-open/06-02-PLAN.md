---
phase: 06-welcome-screen-vault-create-open
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - internal/tui/filepicker.go
  - internal/tui/messages.go
  - internal/tui/dialogs.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "A modal file picker is displayed when triggered by a flow."
    - "The file picker displays two panels: 'Estrutura' (tree) and 'Arquivos' (file list)."
    - "Navigation (keyboard arrows, PgUp/PgDn, Home/End) works correctly in both panels."
    - "Only '.abditum' files are listed, without the extension, and hidden files are excluded."
    - "File sizes are shown in human-readable format (e.g., '1.2MB', '500KB') and dates are relative (e.g., '3h', '2d')."
    - "Inaccessible directories trigger an error message in the message bar."
  artifacts:
    - path: "internal/tui/filepicker.go"
      provides: "filePickerModal struct and its Update/View methods"
      contains: "type filePickerModal struct"
    - path: "internal/tui/messages.go"
      provides: "filePickedMsg, flowCancelledMsg message types"
      contains: "type filePickedMsg struct"
  key_links:
    - from: "internal/tui/flow_open_vault.go" # (will be implemented in 06-04)
      to: "internal/tui/filepicker.go"
      via: "pushing filePickerModal via modalMsg"
      pattern: "pushModalMsg"
    - from: "internal/tui/filepicker.go"
      to: "internal/tui/messages.go"
      via: "emitting filePickedMsg or flowCancelledMsg"
      pattern: "return m, tea.Batch(cmd, tea.Quit)"
    - from: "internal/tui/filepicker.go"
      to: "internal/tui/dialogs.go"
      via: "showing MsgError for inaccessible directories"
      pattern: "m.messages.Show(messages.MsgError"
---

<objective>
Implement the two-panel file picker modal, enabling users to navigate directories, filter for `.abditum` files, and select a path for vault operations.

Purpose: Provide a robust and user-friendly interface for file selection, a critical step in both creating and opening vaults.
Output: A functional `filePickerModal` component that adheres to the design system, handles file system interactions, and emits appropriate messages upon user action.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-welcome-screen-vault-create-open/06-CONTEXT.md
@.planning/phases/06-welcome-screen-vault-create-open/06-UI-SPEC.md
@internal/tui/tui-design-system-novo.md
@internal/tui/tui-specification-novo.md
@internal/tui/root.go
@internal/tui/theme.go
@internal/tui/messages.go

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From internal/tui/root.go:
```go
// childModel is the interface that all child models must implement.
type childModel interface {
	Init() tea.Cmd
	Update(tea.Msg) (childModel, tea.Cmd)
	View(width, height int) string
	SetSize(width, height int)
}

// modalView is an interface for any Bubble Tea model that can be displayed as a modal.
type modalView interface {
	Update(tea.Msg) (modalView, tea.Cmd)
	View(width, height int) string
	SetSize(width, height int)
	ApplyTheme(t *Theme)
	Shortcuts() []Action
}
```

From internal/tui/messages.go:
```go
type MsgKind int // Info, Success, Warning, Error
type MessageManager struct {
	// ... fields
}
func (mm *MessageManager) Show(kind MsgKind, text string, ttl time.Duration)
```

From internal/tui/theme.go:
```go
type Theme struct {
	// ... color fields
}
func (t *Theme) Apply(s lipgloss.Style) lipgloss.Style
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement FilePicker Modal Structure and Navigation</name>
  <files>internal/tui/filepicker.go, internal/tui/messages.go</files>
  <behavior>
    - Create `internal/tui/filepicker.go` with `filePickerModal` struct.
    - Implement `Init()`, `Update(tea.Msg)`, `View(width, height int)` methods for the modal.
    - Ensure two-panel layout (`Estrutura` and `Arquivos`) with 0 vertical padding (D-03).
    - Implement basic keyboard navigation (`↑↓←→`, `PgUp`/`PgDn`, `Home`/`End`) for both panels.
    - Implement `*.abditum` filtering (excluding hidden files) and display names without the extension (D-03).
    - Emit `filePickedMsg{path string}` on `Enter` (file selected or confirmed) and `flowCancelledMsg{}` on `ESC`.
  </behavior>
  <action>
    1. Create `internal/tui/filepicker.go`. Define `filePickerModal` struct (including fields for current path, directory contents, cursor positions for both panels, scroll offsets, current theme, etc.). It should implement `modalView`.
    2. Implement `filePickerModal.Init()` to initialize the current directory (CWD or user home fallback per D-03).
    3. Implement `filePickerModal.Update(msg tea.Msg)` to handle:
        - Keyboard input for navigation in both panels (arrow keys, PgUp/PgDn, Home/End).
        - `Enter` key: If on a directory in the tree, expand/collapse. If on a file, emit `filePickedMsg`. If in filename input (for save mode, later), confirm input.
        - `ESC` key: Emit `flowCancelledMsg{}`.
        - `Tab` key: Cycle focus between Tree, Files, and (if applicable) Filename input (D-03).
        - `SetSize(width, height int)` method for the modal.
        - `ApplyTheme(t *Theme)` method to update the modal's theme.
    4. Implement `filePickerModal.View(width, height int)` to:
        - Render the main modal border (`border.focused`).
        - Draw two panels (`Estrutura` and `Arquivos`) with internal separators (`border.default`).
        - Populate panels with directory tree and filtered file list (omitting `.abditum` extension and hidden files).
        - Render current directory path, truncated with `…` if too long.
        - Implement scroll indicators (`↑`/`↓`/`■`) on panel borders using `text.secondary` (D-03).
        - Use 0 vertical padding between the modal border and its content (D-03).
    5. Modify `internal/tui/messages.go`:
        - Add `filePickedMsg struct { Path string }` (absolute path).
        - Add `flowCancelledMsg struct {}`.
  </action>
  <verify>
    <automated>go test -v ./internal/tui/...</automated>
  </verify>
  <done>
    `filepicker.go` exists, `filePickerModal` renders correctly with two panels and filtered content. Keyboard navigation works for both panels. `filePickedMsg` and `flowCancelledMsg` are defined.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add File Metadata Display and Error Handling to FilePicker</name>
  <files>internal/tui/filepicker.go, internal/tui/dialogs.go</files>
  <behavior>
    - Extend `filePickerModal` to display human-readable file sizes and relative dates (D-03).
    - Implement error handling for inaccessible directories using `MsgError` in the `MessageManager` (D-03).
    - Implement mouse scroll support for the focused panel.
  </behavior>
  <action>
    1. Modify `internal/tui/filepicker.go`:
        - In `filePickerModal.View()`, for each file entry, display its size (e.g., "1.2MB", "500KB" using a helper function for human-readable byte conversion) and relative date/time (e.g., "3h", "2d", "DD/MM/YY" for older files).
        - In `filePickerModal.Update()`, when attempting to read an inaccessible directory:
            - Show `MsgError` in the `MessageManager` (e.g., "Diretório inacessível: {path}").
            - The folder should remain collapsed.
            - Do not crash or block navigation in other parts of the UI.
        - Implement mouse scroll handling for the currently focused panel.
    2. Modify `internal/tui/dialogs.go`: (No direct changes, but `MessageManager.Show` will be used which is already in scope).
  </action>
  <verify>
    <automated>go test -v ./internal/tui/...</automated>
  </verify>
  <done>
    File picker displays accurate file metadata. Inaccessible directories trigger appropriate `MsgError` messages. Mouse scrolling works within the focused panel.
  </done>
</task>

</tasks>

<verification>
Launch the application (with a flow that triggers file picker):
1. Verify `filePickerModal` appears as a modal overlay, with a `border.focused` border.
2. Confirm the two-panel layout, 'Estrutura' (tree) and 'Arquivos' (file list), with `border.default` internal separators and 0 vertical padding.
3. Navigate using arrow keys, PgUp/PgDn, Home/End in both panels. Ensure selection and scrolling work independently for each.
4. Verify only `*.abditum` files are listed (without extension) and hidden files (starting with '.') are not shown.
5. Check file sizes are displayed in human-readable format (e.g., '1.5MB') and dates are relative (e.g., '2h ago', '5d ago' or 'DD/MM/YY').
6. Attempt to navigate into an inaccessible directory (if one can be simulated or created). Verify a `MsgError` appears in the message bar and the directory remains collapsed.
7. Confirm mouse scroll affects only the focused panel.
8. Press `ESC`. Verify the modal closes.
</verification>

<success_criteria>
- The file picker modal is fully functional, allowing users to browse and select vault files.
- Visual elements and interactions adhere to the TUI Design System and `06-CONTEXT.md` specifications.
- Error handling for file system access is graceful and informative.
</success_criteria>

<output>
After completion, create `.planning/phases/06-welcome-screen-vault-create-open/06-02-SUMMARY.md`
</output>

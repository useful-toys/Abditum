---
phase: 06-welcome-screen-vault-create-open
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - internal/tui/passwordentry.go
  - internal/tui/passwordcreate.go
  - internal/tui/messages.go
  - internal/tui/dialogs.go
autonomous: true
requirements:
  - VAULT-01
  - VAULT-03
must_haves:
  truths:
    - "A password entry modal is displayed with a single masked input field when opening a vault."
    - "The password entry modal displays 'Tentativa N de 5' from the second attempt onward."
    - "A password creation modal is displayed with two masked input fields when creating a vault."
    - "The password creation modal shows a real-time strength meter (Weak/Strong) using correct colors."
    - "Password creation modal fields can be navigated using Tab."
    - "Messages like 'Senha incorreta' and 'As senhas não conferem' appear correctly in the message bar."
  artifacts:
    - path: "internal/tui/passwordentry.go"
      provides: "passwordEntryModal struct and its Update/View methods"
      contains: "type passwordEntryModal struct"
    - path: "internal/tui/passwordcreate.go"
      provides: "passwordCreateModal struct and its Update/View methods"
      contains: "type passwordCreateModal struct"
    - path: "internal/tui/messages.go"
      provides: "pwdEnteredMsg, pwdCreatedMsg message types"
      contains: "type pwdEnteredMsg struct"
  key_links:
    - from: "internal/tui/passwordentry.go"
      to: "internal/tui/messages.go"
      via: "showing hints and error messages in MessageManager"
      pattern: "m.messages.Show"
    - from: "internal/tui/passwordentry.go"
      to: "internal/tui/messages.go"
      via: "emitting pwdEnteredMsg"
      pattern: "return m, tea.Batch(cmd, tea.Quit)"
    - from: "internal/tui/passwordcreate.go"
      to: "internal/crypto.EvaluatePasswordStrength"
      via: "real-time password strength evaluation"
      pattern: "crypto.EvaluatePasswordStrength"
    - from: "internal/tui/passwordcreate.go"
      to: "internal/tui/messages.go"
      via: "showing hints and error messages in MessageManager"
      pattern: "m.messages.Show"
    - from: "internal/tui/passwordcreate.go"
      to: "internal/tui/messages.go"
      via: "emitting pwdCreatedMsg"
      pattern: "return m, tea.Batch(cmd, tea.Quit)"
---

<objective>
Implement secure password input modals for opening and creating vaults, featuring masked input, password strength feedback, and an attempt counter.

Purpose: Provide secure and user-friendly interfaces for handling master passwords, a core security component of Abditum.
Output: Functional `passwordEntryModal` and `passwordCreateModal` components that adhere to the design system, manage password input securely, and provide appropriate feedback.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-welcome-screen-vault-create-open/06-CONTEXT.md
@.planning/phases/06-welcome-screen-vault-create-open/06-UI-SPEC.md
@internal/tui/tui-design-system-novo.md
@internal/tui/tui-specification-novo.md
@internal/tui/root.go
@internal/tui/theme.go
@internal/tui/messages.go
@internal/crypto/password.go

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From internal/tui/root.go:
```go
// modalView is an interface for any Bubble Tea model that can be displayed as a modal.
type modalView interface {
	Update(tea.Msg) (modalView, tea.Cmd)
	View(width, height int) string
	SetSize(width, height int)
	ApplyTheme(t *Theme)
	Shortcuts() []Action
}
```

From internal/tui/messages.go:
```go
type MsgKind int // Info, Success, Warning, Error
type MessageManager struct {
	// ... fields
}
func (mm *MessageManager) Show(kind MsgKind, text string, ttl time.Duration)
```

From internal/tui/theme.go:
```go
type Theme struct {
	// ... color fields
}
func (t *Theme) Apply(s lipgloss.Style) lipgloss.Style
```

From internal/crypto/password.go:
```go
type PasswordStrength int // Weak, Good, Strong
func EvaluatePasswordStrength(password []byte) PasswordStrength
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Password Entry Modal</name>
  <files>internal/tui/passwordentry.go, internal/tui/messages.go</files>
  <behavior>
    - Create `internal/tui/passwordentry.go` with `passwordEntryModal` struct.
    - Implement single `textinput` with masked `•` characters (fixed 8 `•`s, not reflecting actual length) (D-04).
    - Implement attempt counter "Tentativa N de 5", visible from attempt 2 onward (D-04).
    - Emit `pwdEnteredMsg{password []byte}` on `Enter` (password copied and zeroed), `flowCancelledMsg{}` on `ESC`.
    - Display messages: "• Digite a senha para desbloquear o cofre" (hint), "✕ Senha incorreta" (error) using `MessageManager` (D-04).
  </behavior>
  <action>
    1. Create `internal/tui/passwordentry.go`. Define `passwordEntryModal` struct, including `textinput.Model`, attempt counter, current theme, etc. It should implement `modalView`.
    2. Implement `passwordEntryModal.Init()` to initialize the `textinput` with `EchoMode` and `EchoCharacter` for masking.
    3. Implement `passwordEntryModal.Update(msg tea.Msg)` to handle:
        - `textinput` updates.
        - `Enter` key: If password is not empty, copy to `[]byte`, zero `textinput` buffer, then emit `pwdEnteredMsg`. If password is empty, show error/hint.
        - `ESC` key: Emit `flowCancelledMsg{}`.
        - Attempt counter logic: Increment on wrong password (simulated for now, actual check in flow). Auto-close on 5th attempt exhaust.
        - `SetSize(width, height int)` method for the modal.
        - `ApplyTheme(t *Theme)` method to update the modal's theme.
    4. Implement `passwordEntryModal.View(width, height int)` to:
        - Render the modal border (`border.focused`) with fixed width 50 columns.
        - Display the masked `textinput` field.
        - Render the "Tentativa N de 5" counter (conditionally visible).
        - Render action hints for "Confirmar" and "Cancelar".
    5. Modify `internal/tui/messages.go`:
        - Add `pwdEnteredMsg struct { Password []byte }`. Ensure `password` field is zeroed after use by the flow.
  </action>
  <verify>
    <automated>go test -v ./internal/tui/...</automated>
  </verify>
  <done>
    `passwordEntryModal` renders correctly with masked input. Attempt counter is displayed as specified. `pwdEnteredMsg` is defined. Messages are shown via `MessageManager`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Password Creation Modal with Strength Meter</name>
  <files>internal/tui/passwordcreate.go, internal/tui/messages.go</files>
  <behavior>
    - Create `internal/tui/passwordcreate.go` with `passwordCreateModal` struct.
    - Implement two `textinput` fields for "Nova senha" and "Confirmação", both masked (D-05).
    - Implement Tab navigation between fields.
    - Display real-time password strength meter using `crypto.EvaluatePasswordStrength`, with "Força: ████████░░ Boa" format and correct semantic colors (D-05).
    - Emit `pwdCreatedMsg{password []byte}` on `Enter` (passwords match, copied and zeroed), `flowCancelledMsg{}` on `ESC`.
    - Display messages: "• A senha mestra protege...", "• Redigite a senha...", "✕ As senhas não conferem..." using `MessageManager` (D-05).
  </behavior>
  <action>
    1. Create `internal/tui/passwordcreate.go`. Define `passwordCreateModal` struct, including two `textinput.Model` fields, password strength state, current theme, etc. It should implement `modalView`.
    2. Implement `passwordCreateModal.Init()` to initialize both `textinput` fields.
    3. Implement `passwordCreateModal.Update(msg tea.Msg)` to handle:
        - `textinput` updates for both fields.
        - `Tab` key: Cycle focus between the two fields (D-05).
        - `Enter` key: If passwords match and not empty, copy to `[]byte`, zero buffers, emit `pwdCreatedMsg`. If passwords diverge or empty, show `MsgError` and clear confirmation field. Weak password allows submission but flow will prompt (D-05).
        - `ESC` key: Emit `flowCancelledMsg{}`.
        - Call `crypto.EvaluatePasswordStrength` on every keystroke in the "Nova senha" field.
        - `SetSize(width, height int)` method for the modal.
        - `ApplyTheme(t *Theme)` method to update the modal's theme.
    4. Implement `passwordCreateModal.View(width, height int)` to:
        - Render the modal border (`border.focused`) with fixed width 50 columns.
        - Display both masked `textinput` fields.
        - Render the password strength meter, using `semantic.success`/`semantic.warning` and `text.disabled` for fill and `Boa`/`Forte`/`Fraca` labels (D-05).
        - Render action hints for "Confirmar" and "Cancelar".
    5. Modify `internal/tui/messages.go`:
        - Add `pwdCreatedMsg struct { Password []byte }`. Ensure `password` field is zeroed after use by the flow.
  </action>
  <verify>
    <automated>go test -v ./internal/tui/...</automated>
  </verify>
  <done>
    `passwordCreateModal` renders correctly with two masked inputs and strength meter. Tab navigation works. `pwdCreatedMsg` is defined. Messages are shown via `MessageManager`.
  </done>
</task>

</tasks>

<verification>
Launch the application (with flows that trigger password modals):
1.  **Password Entry Modal:**
    - Trigger a password entry flow (e.g., attempt to open a vault).
    - Verify a modal appears with a single masked input. Type something.
    - Simulate an incorrect password (e.g., by hitting Enter after typing gibberish, if the flow has a basic check). Verify "Tentativa 1 de 5" (or "Tentativa N de 5" on subsequent tries) appears.
    - Verify "✕ Senha incorreta" appears in the message bar.
    - Press ESC. Verify the modal closes.
2.  **Password Creation Modal:**
    - Trigger a password creation flow (e.g., attempt to create a vault).
    - Verify a modal appears with two masked input fields ("Nova senha", "Confirmação").
    - Type a weak password (e.g., "abc123") into "Nova senha". Verify the strength meter shows "Fraca" and the bar is partially filled.
    - Type a strong password (e.g., "Abc1!Abc1!12") into "Nova senha". Verify the strength meter shows "Forte" and the bar is mostly filled.
    - Use Tab to navigate between the two fields.
    - Type diverging passwords. Verify "✕ As senhas não conferem — digite novamente" appears and the confirmation field clears.
    - Type matching strong passwords and hit Enter. Verify the modal closes.
    - Press ESC. Verify the modal closes.
</verification>

<success_criteria>
- Password entry modal securely collects master password and shows attempt feedback.
- Password creation modal securely collects and confirms master password, providing real-time strength feedback.
- Both modals adhere to design system specifications and use `MessageManager` for user feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/06-welcome-screen-vault-create-open/06-03-SUMMARY.md`
</output>

---
phase: 05.7-golden-test-architecture-for-tui-modals
plan: "01"
subsystem: tui
tags: [go, bubbletea, tui, refactor, decoupling]

# Dependency graph
requires:
  - phase: 05.6-tui-scaffold-dialogs
    provides: helpModal and ActionManager implementations used in this refactor
provides:
  - RenderCommandBar as a pure package-level function accepting []Action
  - helpModal decoupled from ActionManager, accepts []Action + groupLabel func
  - Prerequisite for Plans 02-05 (golden test architecture)
affects:
  - 05.7-02
  - 05.7-03
  - 05.7-04
  - 05.7-05

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pure function pattern: extracted method receiver to parameter for testability"
    - "Dependency injection via function parameter: groupLabel func(int) string avoids struct coupling"

key-files:
  created: []
  modified:
    - internal/tui/actions.go
    - internal/tui/actions_test.go
    - internal/tui/help.go
    - internal/tui/root.go

key-decisions:
  - "RenderCommandBar receives []Action (pre-computed Visible() slice) — callers decide which actions to pass"
  - "helpModal.groupLabel stored as func(int) string field — preserves GroupLabel fallback logic without ActionManager dependency"
  - "root.go call site passes actions.All() and actions.GroupLabel directly — no behavior change"

patterns-established:
  - "TUI component testability: pure functions and injected closures instead of manager references"

requirements-completed: []

# Metrics
duration: 4min
completed: 2026-04-06
---

# Phase 05.7 Plan 01: Decouple RenderCommandBar and helpModal from ActionManager Summary

**RenderCommandBar converted to pure package function and helpModal decoupled via []Action + groupLabel injection, enabling golden tests to construct both components with plain fixtures**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-04-06T06:50:13Z
- **Completed:** 2026-04-06T06:53:29Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- `RenderCommandBar` is now a package-level pure function `func RenderCommandBar(actions []Action, width int) string`
- `helpModal` struct decoupled: `actions []Action` + `groupLabel func(int) string` — no `*ActionManager` anywhere
- All 3 `TestRenderCommandBar_*` tests updated and passing; full test suite green

## Task Commits

Each task was committed atomically:

1. **Task 1: Refactor RenderCommandBar to pure package function** - `83d99a3` (refactor)
2. **Task 2: Refactor helpModal to decouple from ActionManager** - `e995d76` (refactor)

**Plan metadata:** _(docs commit below)_

## Files Created/Modified

- `internal/tui/actions.go` — `RenderCommandBar` method converted to package-level function; parameter `actions []Action` replaces receiver
- `internal/tui/actions_test.go` — 3 tests updated: `am.RenderCommandBar(N)` → `RenderCommandBar(am.Visible(), N)`
- `internal/tui/help.go` — `helpModal` struct refactored: `actions []Action`, `groupLabel func(int) string`; constructor updated; `View()`, `totalLines()`, `buildContentLines()` use new fields
- `internal/tui/root.go` — Two call sites updated: `RenderCommandBar(m.actions.Visible(), m.width)` and `newHelpModal(actions.All(), actions.GroupLabel)`

## Decisions Made

- Passed `actions []Action` (caller's responsibility to call `Visible()` or `All()`) rather than computing inside — keeps the function pure and callers explicit about which action set to render.
- `groupLabel func(int) string` stored on `helpModal` struct (not passed per-call) — consistent with the original design where it was part of the modal's configuration.
- No behavior changes were made — only signatures and field types changed.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plans 02-05 can now construct `helpModal` and call `RenderCommandBar` with plain `[]Action` fixtures — prerequisite fully satisfied.
- `go build ./...` and `go test ./internal/tui/...` both clean.

## Self-Check: PASSED

- `internal/tui/actions.go` — FOUND
- `internal/tui/help.go` — FOUND
- `internal/tui/root.go` — FOUND
- `05.7-01-SUMMARY.md` — FOUND
- Commit `83d99a3` — FOUND
- Commit `e995d76` — FOUND

---
*Phase: 05.7-golden-test-architecture-for-tui-modals*
*Completed: 2026-04-06*

---
phase: 05.7-golden-test-architecture-for-tui-modals
plan: "07"
subsystem: testing
tags: [golden-tests, tui, ansi, regexp, decision-dialog, help-modal]

# Dependency graph
requires:
  - phase: 05.7-golden-test-architecture-for-tui-modals
    provides: golden test helpers (checkOrUpdateGolden, decisionGoldenPath, helpGoldenPath), ParseANSIStyle

provides:
  - stripANSI helper for stripping ANSI escape codes from golden txt output
  - All four test files produce plain-text .txt.golden files (no ANSI escape sequences)
  - decision golden file paths use WxH suffix (e.g. decision-destructive-1action-short-30x24)
  - help golden file paths use WxH suffix (e.g. help-3actions-30x12)
  - Old misnamed golden files deleted (20 decision + 16 help = 36 files removed)

affects:
  - 05.7-08 (Plan 08 regenerates golden files with -update using the corrected paths/format)

# Tech tracking
tech-stack:
  added: [regexp (stdlib)]
  patterns:
    - "stripANSI strips ANSI escape sequences for plain-text golden comparisons"
    - "WxH size suffix in golden file names for multi-line components (decision, help)"
    - "Width-only suffix preserved for single-line components (messages, commandbar)"

key-files:
  created: []
  modified:
    - internal/tui/messages_test.go
    - internal/tui/actions_test.go
    - internal/tui/decision_test.go
    - internal/tui/help_test.go

key-decisions:
  - "stripANSI uses spec pattern \\x1b\\[[0-9;]*m (not broader [a-zA-Z]) for correctness"
  - "All 10 decision variants get x24 suffix since all fixtures call SetSize(W, 24)"
  - "3actions help variants get x12 suffix; 15actions help variants get x16 suffix"
  - "Old golden files deleted now (plan 07) rather than in plan 08 to avoid confusion"

patterns-established:
  - "txt.golden files: always pass stripANSI(out) — no raw ANSI escape codes"
  - "json.golden files: always pass raw out to ParseANSIStyle — ANSI required for style parsing"
  - "Multi-line component golden paths: WxH suffix (e.g. 30x24, 60x16)"
  - "Single-line component golden paths: width-only suffix (e.g. 30, 60) — unchanged"

requirements-completed: []

# Metrics
duration: 7min
completed: 2026-04-06
---

# Phase 05.7 Plan 07: Golden Test Architecture Fix — Plain-Text Output and WxH Naming

**stripANSI helper added to messages_test.go; all four test files now produce plain-text .txt.golden output; decision/help golden paths corrected to WxH naming convention; 36 old misnamed golden files deleted**

## Performance

- **Duration:** 7 min
- **Started:** 2026-04-06T09:52:38Z
- **Completed:** 2026-04-06T09:59:41Z
- **Tasks:** 3
- **Files modified:** 4 source files; 36 golden files deleted

## Accomplishments

- `stripANSI` function added to `messages_test.go` — accessible from all 4 test files in `package tui`
- All four test files (`messages_test.go`, `actions_test.go`, `decision_test.go`, `help_test.go`) now pass `stripANSI(out)` to `.txt.golden` writes
- `.json.golden` calls remain unchanged — `ParseANSIStyle` still receives raw ANSI string
- All 10 decision variant strings updated with `x24` suffix (e.g. `destructive-1action-short-30x24`)
- All 8 help variant strings updated with `x12` or `x16` suffix (e.g. `3actions-30x12`, `15actions-top-30x16`)
- 20 old decision golden files + 16 old help golden files deleted (36 total)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add stripANSI helper; use stripped output for .txt.golden files** — `90f7cdb` (feat)
2. **Task 2: Fix decision_test.go — WxH naming (x24) + stripANSI for txt golden** — `ff7464d` (feat)
3. **Task 3: Fix help_test.go — WxH naming (x12/x16) + stripANSI for txt golden** — `8fe20a9` (feat)

**Plan metadata:** *(committed with docs commit below)*

## Files Created/Modified

- `internal/tui/messages_test.go` — Added `"regexp"` import + `stripANSI` function; changed txt golden write to `stripANSI(out)`
- `internal/tui/actions_test.go` — Changed txt golden write to `stripANSI(out)` (reuses `stripANSI` from same package)
- `internal/tui/decision_test.go` — Updated 10 variant strings to `x24`; txt golden write uses `stripANSI(out)`
- `internal/tui/help_test.go` — Updated 8 variant strings to `x12`/`x16`; txt golden write uses `stripANSI(out)`
- `internal/tui/testdata/golden/decision-*.golden` — 20 old files deleted
- `internal/tui/testdata/golden/help-*.golden` — 16 old files deleted

## Decisions Made

- Used the exact spec pattern `\x1b\[[0-9;]*m` for `stripANSI` (not the broader `[a-zA-Z]` alternative noted in plan interfaces comment)
- All decision test cases call `SetSize(W, 24)` so H=24 applies uniformly for all 10 decision variants
- `3actions` help fixtures call `SetSize(w, 12)` → `x12`; `15actions` help fixtures call `SetSize(w, 16)` → `x16`

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. Note: Tests will FAIL at this point because golden files were deleted and new ones (with correct WxH names) have not yet been generated. This is expected — Plan 08 runs with `-update` to regenerate them.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Ready for Plan 08: Run `go test ./internal/tui/... -run TestDecisionDialog_Golden -update` and `go test ./internal/tui/... -run TestHelpModal_Golden -update` to regenerate golden files with correct WxH names and plain-text content
- All four test files correctly configured to produce spec-compliant golden files on next `-update` run
- `go build ./internal/tui/...` exits 0 ✓

## Self-Check: PASSED

- `internal/tui/messages_test.go` ✓ exists
- `internal/tui/actions_test.go` ✓ exists
- `internal/tui/decision_test.go` ✓ exists
- `internal/tui/help_test.go` ✓ exists
- `.planning/phases/05.7-golden-test-architecture-for-tui-modals/05.7-07-SUMMARY.md` ✓ exists
- commit `90f7cdb` ✓ present
- commit `ff7464d` ✓ present
- commit `8fe20a9` ✓ present

---
*Phase: 05.7-golden-test-architecture-for-tui-modals*
*Completed: 2026-04-06*

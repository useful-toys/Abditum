---
phase: 05.7-golden-test-architecture-for-tui-modals
plan: "03"
subsystem: testing
tags: [golden-tests, ansi, tui, command-bar, go]

# Dependency graph
requires:
  - phase: 05.7-01
    provides: RenderCommandBar refactored as pure function receiving []Action
  - phase: 05.7-02
    provides: goldenPath/checkOrUpdateGolden helpers in messages_test.go, testdata.ParseANSIStyle

provides:
  - TestRenderCommandBar_Golden with 10 sub-tests (5 scenarios × 2 widths)
  - 20 golden files: commandbar-{no-actions,f1-only,typical,unsorted,many}-{30,60}.{txt,json}.golden
  - RenderCommandBar now sorts actions by Priority desc internally

affects: [05.7-golden-test-architecture-for-tui-modals, future-tui-phases]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Golden test runner pattern extended to CommandBar (5 scenarios × 2 widths)"
    - "RenderCommandBar sorts actions internally — callers may pass unsorted slices"

key-files:
  created:
    - internal/tui/testdata/golden/commandbar-*.golden (20 files)
  modified:
    - internal/tui/actions_test.go
    - internal/tui/actions.go
    - internal/tui/testdata/ansiparser.go

key-decisions:
  - "RenderCommandBar sorts []Action by Priority desc internally — callers may pass unsorted slices"
  - "ansiState type moved to package level in testdata to fix build error (was a local type in ParseANSIStyle)"

patterns-established:
  - "Golden test runner: 5 scenarios × 2 widths = 10 sub-tests per component"
  - "Unsorted scenario validates internal sorting by producing identical output to sorted scenario"

requirements-completed: []

# Metrics
duration: 15min
completed: 2026-04-06
---

# Phase 05.7 Plan 03: TestRenderCommandBar_Golden Summary

**TestRenderCommandBar_Golden with 5 scenarios × 2 widths generating 20 golden files (.txt + .json) via sorted RenderCommandBar**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-06T07:12:00Z
- **Completed:** 2026-04-06T07:27:28Z
- **Tasks:** 1
- **Files modified:** 3 + 20 golden files created

## Accomplishments
- Implemented `TestRenderCommandBar_Golden` in `actions_test.go` with 10 sub-tests across 5 scenarios × 2 widths
- Generated 20 golden files capturing ANSI output (.txt) and style transitions (.json)
- Fixed `RenderCommandBar` to sort actions by Priority descending internally, enabling the "unsorted" scenario to prove sorting correctness
- Fixed pre-existing build errors in testdata package (ansiState type promotion, unused fullMatch variable)

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement golden test runner for CommandBar** - `033d126` (feat)

**Plan metadata:** TBD (docs commit)

## Files Created/Modified
- `internal/tui/actions_test.go` — Added `TestRenderCommandBar_Golden` with 5 scenario fixtures and 10 sub-tests
- `internal/tui/actions.go` — Added internal sort to `RenderCommandBar` (sort copy of actions by Priority desc)
- `internal/tui/testdata/ansiparser.go` — Fixed build errors: moved `ansiState` type to package level, removed unused `fullMatch`
- `internal/tui/testdata/golden/commandbar-*.golden` — 20 new golden files (10 .txt.golden + 10 .json.golden)

## Decisions Made
- Added internal sort to `RenderCommandBar` so that callers can pass unsorted slices and get deterministic output — the "unsorted" test scenario proves the sort is happening
- Moved `ansiState` (formerly `State`) to package level in `ansiparser.go` — the local type inside `ParseANSIStyle` was unusable by the package-level `applyCode` function

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed testdata package build errors**
- **Found during:** Task 1 (pre-flight test run)
- **Issue:** `ansiparser.go` had two compile errors: (1) `State` type declared inside `ParseANSIStyle` function body but referenced in package-level `applyCode(state *State, code int)` — undefined type; (2) `fullMatch` variable declared but never used
- **Fix:** Moved `State` to package level as `ansiState`, updated all references; removed unused `fullMatch` declaration
- **Files modified:** `internal/tui/testdata/ansiparser.go`
- **Verification:** `go test ./internal/tui/... -count=1` exits 0
- **Committed in:** `033d126`

**2. [Rule 1 - Bug] Added internal sort to RenderCommandBar**
- **Found during:** Task 1 (implementing "unsorted" scenario)
- **Issue:** The plan requires the "unsorted" scenario output to be identical to "typical" (proving internal sort). But `RenderCommandBar` did not sort — it used actions as-is, so unsorted inputs would produce different output
- **Fix:** Added `sort.Slice` on a copy of the actions slice at the start of `RenderCommandBar`, sorting by Priority descending
- **Files modified:** `internal/tui/actions.go`
- **Verification:** `go test ./internal/tui/... -run TestRenderCommandBar_Golden -count=1 -v` — unsorted-30 and unsorted-60 produce identical golden output to typical-30 and typical-60
- **Committed in:** `033d126`

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** Both fixes necessary for correctness. The sort fix is required by the plan's specification that "unsorted" output equals "typical" output.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- CommandBar golden test architecture complete: 20 files covering all 5 specified scenarios
- `go test ./internal/tui/... -count=1` fully green
- Phase 05.7 golden test suite now spans: DecisionDialog (phase 02, prior parallel), HelpDialog (phase 02, prior parallel), MessageBar (phase 02), CommandBar (this plan)
- Ready for Phase 06 (Welcome Screen + Vault Create/Open flows)

---
*Phase: 05.7-golden-test-architecture-for-tui-modals*
*Completed: 2026-04-06*

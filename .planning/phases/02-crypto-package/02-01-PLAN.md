---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/crypto/doc.go
  - internal/crypto/errors.go
  - internal/crypto/kdf.go
  - internal/crypto/aead.go
  - internal/crypto/memory.go
  - internal/crypto/mlock_unix.go
  - internal/crypto/mlock_windows.go
  - internal/crypto/mlock_other.go
  - internal/crypto/password.go
  - internal/crypto/kdf_test.go
  - internal/crypto/aead_test.go
  - internal/crypto/memory_test.go
  - internal/crypto/password_test.go
  - internal/crypto/crypto_test.go
autonomous: true
requirements:
  - CRYPTO-01
  - CRYPTO-02
  - CRYPTO-03
  - CRYPTO-04
  - CRYPTO-05
  - CRYPTO-06
  - PWD-01

must_haves:
  truths:
    - "Argon2id derives 32-byte keys from password+salt with t=3, m=262144 KiB, p=4"
    - "AES-256-GCM encrypts with unique 12-byte nonce per operation"
    - "Decrypt with wrong key returns ErrAuthFailed, not panic"
    - "ZeroBytes overwrites sensitive buffers with zeros"
    - "mlock/VirtualLock failures are non-fatal"
    - "Password strength evaluates without string conversion"
  artifacts:
    - path: "internal/crypto/kdf.go"
      provides: "Argon2id key derivation"
      exports: ["GenerateSalt", "DeriveKey", "ArgonParams"]
    - path: "internal/crypto/aead.go"
      provides: "AES-256-GCM encryption/decryption"
      exports: ["Encrypt", "Decrypt"]
    - path: "internal/crypto/memory.go"
      provides: "Memory security primitives"
      exports: ["ZeroBytes"]
    - path: "internal/crypto/mlock_unix.go"
      provides: "Unix memory locking"
      exports: ["Mlock", "Munlock"]
    - path: "internal/crypto/mlock_windows.go"
      provides: "Windows memory locking"
      exports: ["Mlock", "Munlock"]
    - path: "internal/crypto/password.go"
      provides: "Password strength evaluation"
      exports: ["EvaluatePasswordStrength", "StrengthLevel"]
    - path: "internal/crypto/errors.go"
      provides: "Sentinel errors"
      exports: ["ErrAuthFailed", "ErrInsufficientEntropy", "ErrInvalidParams", "ErrMLockFailed"]
    - path: "internal/crypto/kdf_test.go"
      provides: "KDF tests"
      min_lines: 50
    - path: "internal/crypto/aead_test.go"
      provides: "AEAD tests"
      min_lines: 100
    - path: "internal/crypto/memory_test.go"
      provides: "Memory security tests"
      min_lines: 50
    - path: "internal/crypto/password_test.go"
      provides: "Password strength tests"
      min_lines: 40
  key_links:
    - from: "internal/crypto/aead.go"
      to: "crypto/rand.Reader"
      via: "io.ReadFull for nonce generation"
      pattern: "io\\.ReadFull\\(rand\\.Reader, nonce\\)"
    - from: "internal/crypto/kdf.go"
      to: "golang.org/x/crypto/argon2"
      via: "IDKey function call"
      pattern: "argon2\\.IDKey"
    - from: "internal/crypto/aead.go"
      to: "internal/crypto/errors"
      via: "ErrAuthFailed on decrypt failure"
      pattern: "return nil, ErrAuthFailed"
    - from: "internal/crypto/mlock_unix.go"
      to: "golang.org/x/sys/unix"
      via: "Mlock syscall"
      pattern: "unix\\.Mlock"
    - from: "internal/crypto/mlock_windows.go"
      to: "golang.org/x/sys/windows"
      via: "VirtualLock API"
      pattern: "windows\\.VirtualLock"
---

<objective>
Deliver production-ready cryptographic primitives for Abditum's password manager: Argon2id key derivation, AES-256-GCM authenticated encryption, platform-specific memory locking, password strength evaluation, and comprehensive test coverage that will catch any future security regression.

Purpose: Establish the security foundation for vault encryption before any file I/O or domain logic depends on it.

Output: Complete `internal/crypto` package with all primitives, platform-specific build tags, sentinel errors, and exhaustive unit tests passing with race detector.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-crypto-package/02-CONTEXT.md
@.planning/phases/02-crypto-package/02-RESEARCH.md
@.planning/phases/02-crypto-package/02-VALIDATION.md
@.planning/REQUIREMENTS.md
</context>

<interfaces>
<!-- Key Go stdlib and x/crypto types the executor needs -->

From crypto/cipher godoc:
```go
// AEAD is an interface for authenticated encryption with associated data
type AEAD interface {
    NonceSize() int
    Overhead() int
    Seal(dst, nonce, plaintext, additionalData []byte) []byte
    Open(dst, nonce, ciphertext, additionalData []byte) ([]byte, error)
}

// NewGCM returns the given 128-bit block cipher wrapped in Galois Counter Mode
func NewGCM(cipher Block) (AEAD, error)
```

From crypto/aes godoc:
```go
// NewCipher creates and returns a new cipher.Block
// The key argument should be 16, 24, or 32 bytes (AES-128, AES-192, AES-256)
func NewCipher(key []byte) (cipher.Block, error)
```

From golang.org/x/crypto/argon2 godoc:
```go
// IDKey derives a key from the password, salt, and cost parameters using Argon2id
// time: number of iterations
// memory: amount of memory in KiB
// threads: degree of parallelism
// keyLen: length of the generated key in bytes
func IDKey(password, salt []byte, time, memory uint32, threads uint8, keyLen uint32) []byte
```

From golang.org/x/sys/unix godoc:
```go
// Mlock locks pages in memory
func Mlock(b []byte) error
func Munlock(b []byte) error
```

From golang.org/x/sys/windows godoc:
```go
// VirtualLock locks pages in memory
func VirtualLock(addr uintptr, length uintptr) error
func VirtualUnlock(addr uintptr, length uintptr) error
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Package documentation and sentinel errors</name>
  <files>
    internal/crypto/doc.go
    internal/crypto/errors.go
  </files>
  <read_first>
    - .planning/phases/02-crypto-package/02-CONTEXT.md (error handling decisions D-01 through D-12)
    - .planning/phases/02-crypto-package/02-RESEARCH.md (sentinel error patterns)
    - .planning/REQUIREMENTS.md (CRYPTO-01 through CRYPTO-06)
  </read_first>
  <behavior>
    - Test 1: Package doc.go exists and contains Argon2id purpose explanation (KDF not encryption)
    - Test 2: Package doc.go explains chosen parameters (t=3, m=256 MiB = 262144 KiB, p=4, keyLen=32)
    - Test 3: Package doc.go explains AES-256-GCM AEAD concept (nonce, ciphertext, tag)
    - Test 4: errors.go defines ErrAuthFailed, ErrInsufficientEntropy, ErrInvalidParams, ErrMLockFailed as package-level vars
    - Test 5: All sentinel errors are exported and can be checked with errors.Is()
  </behavior>
  <action>
Write `internal/crypto/doc.go` with:
- Package-level comment explaining Argon2id is a Key Derivation Function (NOT encryption itself)
- Explain chosen parameters in plain language accessible to non-crypto-expert Go readers: t=3 iterations, m=256 MiB (262144 KiB) memory, p=4 parallelism, keyLen=32 bytes for AES-256
- Explain AES-256-GCM AEAD concept: nonce uniqueness requirement, ciphertext includes authentication tag, decrypt verifies integrity
- Define `ArgonParams` struct with fields: Time uint32, Memory uint32, Threads uint8, KeyLen uint32
- Define `FormatVersion` constant = 1

Write `internal/crypto/errors.go` with sentinel errors as package-level vars (NOT wrapped errors):
- `var ErrAuthFailed = errors.New("authentication failed")` — returned by Decrypt on wrong key OR corrupted ciphertext (D-02: no timing leak, single error for both cases)
- `var ErrInsufficientEntropy = errors.New("insufficient entropy")` — returned when crypto/rand.Reader exhausted (D-06)
- `var ErrInvalidParams = errors.New("invalid parameters")` — returned for nil password, zero-length salt, invalid Argon2id params (D-09)
- `var ErrMLockFailed = errors.New("memory lock failed")` — returned by Mlock when unavailable, but execution continues (D-03: non-fatal)

All error messages minimal per D-05: no internal details, no stdlib error text exposed.
  </action>
  <acceptance_criteria>
    - `internal/crypto/doc.go` exists and contains substring "Argon2id" and "Key Derivation Function"
    - `doc.go` contains "t=3", "m=262144", "p=4", "keyLen=32"
    - `doc.go` contains "AES-256-GCM" and "nonce"
    - `doc.go` defines `type ArgonParams struct` with fields Time, Memory, Threads, KeyLen
    - `doc.go` defines `const FormatVersion = 1`
    - `internal/crypto/errors.go` exists
    - `errors.go` exports `var ErrAuthFailed`, `var ErrInsufficientEntropy`, `var ErrInvalidParams`, `var ErrMLockFailed`
    - All error vars use `errors.New()` (not fmt.Errorf with wrapping)
    - `go build ./internal/crypto` succeeds
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/crypto && grep -q "Argon2id" internal/crypto/doc.go && grep -q "ErrAuthFailed" internal/crypto/errors.go</automated>
  </verify>
  <done>Package documentation written, ArgonParams and FormatVersion defined, four sentinel errors exported</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Argon2id key derivation</name>
  <files>
    internal/crypto/kdf.go
    internal/crypto/kdf_test.go
  </files>
  <read_first>
    - internal/crypto/doc.go (ArgonParams struct)
    - internal/crypto/errors.go (ErrInvalidParams, ErrInsufficientEntropy)
    - .planning/phases/02-crypto-package/02-RESEARCH.md (Pattern 1: Argon2id Key Derivation, Pitfall 1)
    - .planning/phases/02-crypto-package/02-CONTEXT.md (D-29: parameters)
  </read_first>
  <behavior>
    - Test 1: GenerateSalt() returns 32-byte slice and nil error
    - Test 2: GenerateSalt() called twice produces distinct salts
    - Test 3: DeriveKey with valid inputs produces 32-byte key
    - Test 4: DeriveKey with nil password returns ErrInvalidParams
    - Test 5: DeriveKey with zero-length salt returns ErrInvalidParams
    - Test 6: Argon2id memory parameter is 262144 KiB (not 256)
  </behavior>
  <action>
Write `internal/crypto/kdf.go`:

1. Import `crypto/rand`, `io`, `golang.org/x/crypto/argon2`

2. Implement `GenerateSalt() ([]byte, error)`:
   - Allocate 32-byte slice: `salt := make([]byte, 32)`
   - Fill with crypto/rand: `if _, err := io.ReadFull(rand.Reader, salt); err != nil { return nil, ErrInsufficientEntropy }`
   - Return salt and nil

3. Implement `DeriveKey(password, salt []byte, params ArgonParams) ([]byte, error)`:
   - Validate inputs: if len(password) == 0 OR len(salt) == 0, return nil, ErrInvalidParams
   - Call argon2.IDKey: `key := argon2.IDKey(password, salt, params.Time, params.Memory, params.Threads, params.KeyLen)`
   - Return key, nil
   - Add comment: "Caller's responsibility to zero key after use with ZeroBytes()"

4. Argon2id memory parameter in KiB: 256 MiB = 262144 KiB (CRITICAL per Pitfall 1 from RESEARCH.md)

Write `internal/crypto/kdf_test.go`:

1. TestGenerateSalt: call GenerateSalt twice, assert len=32 for both, assert not equal (unique)
2. TestDeriveKey: call DeriveKey with test password "TestPassword123!" + 32-byte salt + params{Time:3, Memory:262144, Threads:4, KeyLen:32}, assert len(key)==32 and err==nil
3. TestDeriveKeyInvalidPassword: call with nil password, assert err == ErrInvalidParams
4. TestDeriveKeyInvalidSalt: call with []byte{} salt, assert err == ErrInvalidParams
5. BenchmarkDeriveKey: benchmark Argon2id with standard params (should take 200-500ms per op)
  </action>
  <acceptance_criteria>
    - `internal/crypto/kdf.go` exists and contains `func GenerateSalt() ([]byte, error)`
    - `kdf.go` contains `func DeriveKey(password, salt []byte, params ArgonParams) ([]byte, error)`
    - `kdf.go` calls `argon2.IDKey` with correct parameter order
    - `kdf.go` returns `ErrInvalidParams` for nil/empty password or salt
    - `kdf.go` returns `ErrInsufficientEntropy` if `io.ReadFull` fails
    - `internal/crypto/kdf_test.go` exists with TestGenerateSalt, TestDeriveKey, TestDeriveKeyInvalidPassword, TestDeriveKeyInvalidSalt, BenchmarkDeriveKey
    - `go test ./internal/crypto -run TestGenerateSalt -v` passes
    - `go test ./internal/crypto -run TestDeriveKey -v` passes
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/crypto -run "TestGenerateSalt|TestDeriveKey" -v</automated>
  </verify>
  <done>Argon2id KDF functions implemented with validation, salt generation verified as unique, key derivation tested</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AES-256-GCM encryption and decryption</name>
  <files>
    internal/crypto/aead.go
    internal/crypto/aead_test.go
  </files>
  <read_first>
    - internal/crypto/errors.go (ErrAuthFailed, ErrInsufficientEntropy, ErrInvalidParams)
    - .planning/phases/02-crypto-package/02-RESEARCH.md (Pattern 2: AES-256-GCM Encryption, Pattern 3: Decryption, Pitfall 2, Pitfall 6)
    - .planning/phases/02-crypto-package/02-CONTEXT.md (D-19: nonce generation internal to Encrypt)
  </read_first>
  <behavior>
    - Test 1: Encrypt with valid 32-byte key produces ciphertext starting with 12-byte nonce
    - Test 2: Encrypting same plaintext twice with same key produces DISTINCT ciphertexts (nonce uniqueness)
    - Test 3: Encrypt → Decrypt roundtrip returns original plaintext byte-for-byte
    - Test 4: Decrypt with wrong key returns ErrAuthFailed
    - Test 5: Decrypt with short ciphertext (<12 bytes) returns ErrAuthFailed
    - Test 6: Decrypt with corrupted ciphertext returns ErrAuthFailed
  </behavior>
  <action>
Write `internal/crypto/aead.go`:

1. Import `crypto/aes`, `crypto/cipher`, `crypto/rand`, `io`

2. Implement `Encrypt(key, plaintext []byte) ([]byte, error)`:
   - Validate key length: if len(key) != 32, return nil, ErrInvalidParams
   - Create AES cipher: `block, err := aes.NewCipher(key)`; if err != nil, return nil, err
   - Create GCM: `gcm, err := cipher.NewGCM(block)`; if err != nil, return nil, err
   - Generate nonce IMMEDIATELY before Seal (D-19, Pitfall 2): `nonce := make([]byte, gcm.NonceSize())` (12 bytes)
   - Fill nonce: `if _, err := io.ReadFull(rand.Reader, nonce); err != nil { return nil, ErrInsufficientEntropy }` (Pitfall 6: use ReadFull not Read)
   - Encrypt: `ciphertext := gcm.Seal(nonce, nonce, plaintext, nil)` (prepends nonce to output)
   - Return ciphertext, nil
   - Output layout: nonce (12) || ciphertext (len(plaintext)) || tag (16)

3. Implement `Decrypt(key, ciphertext []byte) ([]byte, error)`:
   - Validate key: if len(key) != 32, return nil, ErrInvalidParams
   - Create AES cipher: `block, err := aes.NewCipher(key)`; if err != nil, return nil, err
   - Create GCM: `gcm, err := cipher.NewGCM(block)`; if err != nil, return nil, err
   - Check ciphertext length: if len(ciphertext) < gcm.NonceSize(), return nil, ErrAuthFailed
   - Split nonce and payload: `nonce, ciphertextAndTag := ciphertext[:gcm.NonceSize()], ciphertext[gcm.NonceSize():]`
   - Decrypt and verify: `plaintext, err := gcm.Open(nil, nonce, ciphertextAndTag, nil)`
   - If err != nil (wrong key OR corrupted data OR tampered ciphertext), return nil, ErrAuthFailed (D-02: single sentinel, no internal details)
   - Return plaintext, nil

Write `internal/crypto/aead_test.go`:

1. TestEncrypt: encrypt "test plaintext" with valid 32-byte key, assert len(output) == 12 + len(plaintext) + 16
2. TestNonceUniqueness: encrypt same plaintext twice with same key, assert ciphertexts differ (CRITICAL for GCM security)
3. TestRoundtrip: Encrypt → Decrypt with same key, assert decrypted == original
4. TestDecryptWrongKey: Encrypt with key1, Decrypt with key2, assert err == ErrAuthFailed
5. TestDecryptShortCiphertext: Decrypt ciphertext with len < 12, assert err == ErrAuthFailed
6. TestDecryptCorrupted: Encrypt, flip a byte in ciphertext, Decrypt, assert err == ErrAuthFailed
7. BenchmarkEncrypt and BenchmarkDecrypt for throughput baseline
  </action>
  <acceptance_criteria>
    - `internal/crypto/aead.go` exists with `func Encrypt(key, plaintext []byte) ([]byte, error)` and `func Decrypt(key, ciphertext []byte) ([]byte, error)`
    - `aead.go` uses `io.ReadFull(rand.Reader, nonce)` before `gcm.Seal()` (not `rand.Read`)
    - `aead.go` Decrypt returns `ErrAuthFailed` on any `gcm.Open()` error (never exposes stdlib error)
    - `aead.go` validates key length == 32, returns `ErrInvalidParams` otherwise
    - `internal/crypto/aead_test.go` exists with TestEncrypt, TestNonceUniqueness, TestRoundtrip, TestDecryptWrongKey, TestDecryptShortCiphertext, TestDecryptCorrupted
    - `go test ./internal/crypto -run TestNonceUniqueness -v` passes (CRITICAL nonce uniqueness test)
    - `go test ./internal/crypto -run "TestEncrypt|TestDecrypt|TestRoundtrip" -v` passes
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/crypto -run "TestEncrypt|TestDecrypt|TestNonceUniqueness|TestRoundtrip" -v</automated>
  </verify>
  <done>AES-256-GCM encrypt/decrypt implemented with nonce uniqueness, roundtrip verified, wrong-key returns ErrAuthFailed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Memory security primitives</name>
  <files>
    internal/crypto/memory.go
    internal/crypto/memory_test.go
  </files>
  <read_first>
    - .planning/phases/02-crypto-package/02-RESEARCH.md (Pattern 4: Memory Zeroing)
    - .planning/phases/02-crypto-package/02-CONTEXT.md (D-14, D-15: explicit zeroing)
  </read_first>
  <behavior>
    - Test 1: ZeroBytes fills entire slice with 0x00
    - Test 2: ZeroBytes with nil slice does not panic
    - Test 3: ZeroBytes with empty slice does not panic
  </behavior>
  <action>
Write `internal/crypto/memory.go`:

1. Implement `ZeroBytes(b []byte)`:
   - Guard: if len(b) == 0, return immediately
   - Create zero slice: `zeros := make([]byte, len(b))`
   - Overwrite: `copy(b, zeros)`
   - Alternative implementation (also acceptable): manual loop `for i := range b { b[i] = 0 }`
   - Add comment: "Overwrites sensitive byte slice with zeros. Caller must call this explicitly after use — no implicit defer pattern per D-15."

Write `internal/crypto/memory_test.go`:

1. TestZeroBytes: create []byte{1, 2, 3, 4, 5}, call ZeroBytes, assert all bytes == 0
2. TestZeroBytesEmpty: call ZeroBytes([]byte{}), assert no panic
3. TestZeroBytesNil: call ZeroBytes(nil), assert no panic
  </action>
  <acceptance_criteria>
    - `internal/crypto/memory.go` exists with `func ZeroBytes(b []byte)`
    - `memory.go` uses `copy(b, make([]byte, len(b)))` OR manual loop (both acceptable)
    - `memory.go` guards against len(b) == 0
    - `internal/crypto/memory_test.go` exists with TestZeroBytes, TestZeroBytesEmpty, TestZeroBytesNil
    - `go test ./internal/crypto -run TestZeroBytes -v` passes
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/crypto -run TestZeroBytes -v</automated>
  </verify>
  <done>ZeroBytes implemented, tested with actual buffer, empty slice, and nil slice</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Platform-specific memory locking</name>
  <files>
    internal/crypto/mlock_unix.go
    internal/crypto/mlock_windows.go
    internal/crypto/mlock_other.go
    internal/crypto/memory_test.go
  </files>
  <read_first>
    - internal/crypto/errors.go (ErrMLockFailed)
    - .planning/phases/02-crypto-package/02-RESEARCH.md (Pattern 5: Platform-Specific Memory Locking, Pitfall 4, Pitfall 7)
    - .planning/phases/02-crypto-package/02-CONTEXT.md (D-03, D-16, D-17)
  </read_first>
  <behavior>
    - Test 1: Mlock on Unix with valid buffer succeeds or returns ErrMLockFailed (non-fatal)
    - Test 2: Mlock on Windows with valid buffer succeeds or returns ErrMLockFailed (non-fatal)
    - Test 3: Mlock on "other" platforms returns ErrMLockFailed gracefully
    - Test 4: Munlock on Unix succeeds
    - Test 5: Munlock on Windows succeeds
  </behavior>
  <action>
Write `internal/crypto/mlock_unix.go`:

1. Add build tag: `//go:build !windows`
2. Import `golang.org/x/sys/unix`
3. Implement `Mlock(b []byte) error`:
   - Guard: if len(b) == 0, return nil
   - Call `err := unix.Mlock(b)`
   - If err != nil, return ErrMLockFailed (D-03: non-fatal, execution continues)
   - Return nil
4. Implement `Munlock(b []byte) error`:
   - Guard: if len(b) == 0, return nil
   - Return `unix.Munlock(b)`

Write `internal/crypto/mlock_windows.go`:

1. Add build tag: `//go:build windows`
2. Import `golang.org/x/sys/windows`, `unsafe`
3. Implement `Mlock(b []byte) error`:
   - Guard: if len(b) == 0, return nil
   - Call `err := windows.VirtualLock(uintptr(unsafe.Pointer(&b[0])), uintptr(len(b)))`
   - If err != nil, return ErrMLockFailed
   - Return nil
4. Implement `Munlock(b []byte) error`:
   - Guard: if len(b) == 0, return nil
   - Return `windows.VirtualUnlock(uintptr(unsafe.Pointer(&b[0])), uintptr(len(b)))`

Write `internal/crypto/mlock_other.go`:

1. Add build tag: `//go:build !unix && !windows`
2. Implement `Mlock(b []byte) error`:
   - Return ErrMLockFailed (signal unavailable but non-fatal per D-03)
3. Implement `Munlock(b []byte) error`:
   - Return nil

Extend `internal/crypto/memory_test.go`:

1. TestMlock: allocate 32-byte buffer, call Mlock, if err == nil (succeeded) OR err == ErrMLockFailed (unavailable) → both OK, assert no panic (D-03: failure is non-fatal)
2. TestMunlock: allocate buffer, Mlock, Munlock, assert no panic
3. Add comment: "mlock failure is expected in containers, low-privilege contexts, and unsupported platforms — test verifies non-fatal behavior"

CRITICAL per Pitfall 7: Never append to locked buffer (reallocates, loses lock). Pre-allocate exact size: `key := make([]byte, 32)` then `Mlock(key)`.
  </action>
  <acceptance_criteria>
    - `internal/crypto/mlock_unix.go` exists with build tag `//go:build !windows`
    - `mlock_unix.go` imports `golang.org/x/sys/unix` and calls `unix.Mlock`
    - `internal/crypto/mlock_windows.go` exists with build tag `//go:build windows`
    - `mlock_windows.go` imports `golang.org/x/sys/windows` and calls `windows.VirtualLock`
    - `internal/crypto/mlock_other.go` exists with build tag `//go:build !unix && !windows`
    - `mlock_other.go` Mlock returns `ErrMLockFailed`
    - `memory_test.go` extended with TestMlock and TestMunlock
    - `go test ./internal/crypto -run TestMlock -v` passes (may return ErrMLockFailed, which is OK)
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/crypto -run "TestMlock|TestMunlock" -v</automated>
  </verify>
  <done>Platform-specific memory locking implemented for Unix, Windows, and other; mlock failure is non-fatal as required</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Password strength evaluation</name>
  <files>
    internal/crypto/password.go
    internal/crypto/password_test.go
  </files>
  <read_first>
    - .planning/phases/02-crypto-package/02-RESEARCH.md (Pattern 6: Password Strength Evaluation, Pitfall 3)
    - .planning/phases/02-crypto-package/02-CONTEXT.md (D-34: strength criteria)
    - .planning/REQUIREMENTS.md (PWD-01)
  </read_first>
  <behavior>
    - Test 1: Password with 12 chars + all categories returns StrengthStrong
    - Test 2: Password with 11 chars + all categories returns StrengthWeak (length boundary)
    - Test 3: Password with 12 chars missing uppercase returns StrengthWeak
    - Test 4: Password with 12 chars missing lowercase returns StrengthWeak
    - Test 5: Password with 12 chars missing digit returns StrengthWeak
    - Test 6: Password with 12 chars missing special returns StrengthWeak
    - Test 7: "Abc1!Abc1!12" returns StrengthStrong
    - Test 8: "abc123" returns StrengthWeak
  </behavior>
  <action>
Write `internal/crypto/password.go`:

1. Define `type StrengthLevel int`
2. Define constants:
   ```go
   const (
       StrengthWeak StrengthLevel = iota
       StrengthStrong
   )
   ```

3. Implement `EvaluatePasswordStrength(password []byte) StrengthLevel`:
   - Check length: if len(password) < 12, return StrengthWeak
   - Initialize flags: `var hasUpper, hasLower, hasDigit, hasSpecial bool`
   - Iterate over password bytes: `for _, b := range password`
     - If b >= 'A' && b <= 'Z': hasUpper = true
     - If b >= 'a' && b <= 'z': hasLower = true
     - If b >= '0' && b <= '9': hasDigit = true
     - If none of above: hasSpecial = true
   - Return StrengthStrong if ALL four flags true, else StrengthWeak
   - CRITICAL per Pitfall 3: operate directly on []byte, NEVER `string(password)` (creates unzeroaple copy)

Write `internal/crypto/password_test.go`:

1. TestPasswordStrength12CharsAllCategories: `EvaluatePasswordStrength([]byte("Abc1!Abc1!12"))` → assert StrengthStrong
2. TestPasswordStrength11Chars: `EvaluatePasswordStrength([]byte("Abc1!Abc1!1"))` → assert StrengthWeak (11 chars)
3. TestPasswordStrengthNoUppercase: `EvaluatePasswordStrength([]byte("abc123!@#456"))` → assert StrengthWeak
4. TestPasswordStrengthNoLowercase: `EvaluatePasswordStrength([]byte("ABC123!@#456"))` → assert StrengthWeak
5. TestPasswordStrengthNoDigit: `EvaluatePasswordStrength([]byte("Abcdef!@#Xyz"))` → assert StrengthWeak
6. TestPasswordStrengthNoSpecial: `EvaluatePasswordStrength([]byte("Abcdef123456"))` → assert StrengthWeak
7. TestPasswordStrengthWeak: `EvaluatePasswordStrength([]byte("abc123"))` → assert StrengthWeak
  </action>
  <acceptance_criteria>
    - `internal/crypto/password.go` exists with `type StrengthLevel int` and constants StrengthWeak, StrengthStrong
    - `password.go` contains `func EvaluatePasswordStrength(password []byte) StrengthLevel`
    - `password.go` checks len(password) >= 12
    - `password.go` checks for uppercase, lowercase, digit, special character presence
    - `password.go` NEVER calls `string(password)` (Pitfall 3)
    - `internal/crypto/password_test.go` exists with 7 boundary tests
    - `go test ./internal/crypto -run TestPasswordStrength -v` passes all cases
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/crypto -run TestPasswordStrength -v</automated>
  </verify>
  <done>Password strength evaluation implemented without string conversion, boundary cases tested (11 vs 12 chars, missing categories)</done>
</task>

<task type="auto">
  <name>Task 7: Integration tests and race detector verification</name>
  <files>
    internal/crypto/crypto_test.go
  </files>
  <read_first>
    - internal/crypto/kdf.go (GenerateSalt, DeriveKey)
    - internal/crypto/aead.go (Encrypt, Decrypt)
    - internal/crypto/memory.go (ZeroBytes)
    - .planning/phases/02-crypto-package/02-VALIDATION.md (validation strategy)
  </read_first>
  <action>
Write `internal/crypto/crypto_test.go` with integration tests:

1. TestFullRoundtrip: 
   - Generate salt with GenerateSalt
   - Derive key with DeriveKey using password "TestVaultPassword123!", salt, params{Time:3, Memory:262144, Threads:4, KeyLen:32}
   - Encrypt plaintext "secret vault data" with key
   - Decrypt ciphertext with same key
   - Assert decrypted == original plaintext byte-for-byte
   - Zero key with ZeroBytes, assert key is all zeros
   
2. TestKeyReuseProducesDistinctCiphertexts:
   - Derive one key
   - Encrypt same plaintext twice with same key
   - Assert two ciphertexts are different (proves nonce uniqueness in real usage)

3. TestMemorySafety:
   - Create sensitive buffer []byte("sensitive data here")
   - Call ZeroBytes
   - Assert every byte == 0x00

Run full suite with race detector to verify CRYPTO-04 compliance:
`CGO_ENABLED=0 go test ./internal/crypto/... -race -count=1`

This must pass with zero race detector alerts per UAT requirement.
  </action>
  <acceptance_criteria>
    - `internal/crypto/crypto_test.go` exists with TestFullRoundtrip, TestKeyReuseProducesDistinctCiphertexts, TestMemorySafety
    - TestFullRoundtrip performs GenerateSalt → DeriveKey → Encrypt → Decrypt → ZeroBytes sequence
    - TestFullRoundtrip asserts decrypted == original
    - TestFullRoundtrip asserts zeroed key is all 0x00
    - `go test ./internal/crypto -run TestFullRoundtrip -v` passes
    - `CGO_ENABLED=0 go test ./internal/crypto/... -race -count=1` exits 0 with zero race alerts
  </acceptance_criteria>
  <verify>
    <automated>CGO_ENABLED=0 go test ./internal/crypto/... -race -count=1</automated>
  </verify>
  <done>Integration tests verify full encrypt/decrypt roundtrip, key reuse safety, memory zeroing; race detector passes</done>
</task>

</tasks>

<verification>
All UAT criteria from ROADMAP.md must pass:

- [ ] `Encrypt(key, p)` + `Decrypt(key, c)` roundtrip returns original plaintext byte-for-byte (TestFullRoundtrip)
- [ ] Encrypting identical plaintext twice with same key produces distinct ciphertexts (TestNonceUniqueness, TestKeyReuseProducesDistinctCiphertexts)
- [ ] `Decrypt` with wrong key returns `ErrAuthFailed` — no panic, no internal error string exposed (TestDecryptWrongKey)
- [ ] `ZeroBytes(b)` fills every byte of `b` with `0x00` (TestZeroBytes, TestMemorySafety)
- [ ] `EvaluatePasswordStrength([]byte("Abc1!Abc1!12"))` returns `StrengthStrong`; `"abc123"` returns `StrengthWeak` (TestPasswordStrength cases)
- [ ] `CGO_ENABLED=0 go test ./internal/crypto/... -race` passes with zero race conditions (Task 7)

Run verification suite:
```bash
CGO_ENABLED=0 go test ./internal/crypto/... -v -race -count=1
go build ./internal/crypto
```

All tests must be green before marking phase complete.
</verification>

<success_criteria>
Phase complete when:

1. All 7 tasks done
2. All test files exist and pass
3. Package builds successfully: `go build ./internal/crypto`
4. Full test suite passes: `go test ./internal/crypto/... -v`
5. Race detector clean: `go test ./internal/crypto/... -race -count=1`
6. All 6 UAT criteria from ROADMAP verified
7. Must-haves achieved:
   - All 6 truths verified by tests
   - All 8 artifacts exist with exports present
   - All 5 key links verified by grep
8. Requirements CRYPTO-01 through CRYPTO-06 and PWD-01 fully addressed
9. Zero security regressions possible (comprehensive test coverage)
</success_criteria>

<output>
After completion, create `.planning/phases/02-crypto-package/02-01-SUMMARY.md`
</output>

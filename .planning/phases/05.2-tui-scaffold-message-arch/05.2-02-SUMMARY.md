---
plan: 05.2-02
phase: 05.2-tui-scaffold-message-arch
status: complete
completed: 2026-04-03
---

# Plan 05.2-02: Actions/Root/Help Refactor — COMPLETE

## What Was Built

Refactored `internal/tui/actions.go`, `root.go`, and `help.go` to implement spec-correct D-02/D-03/D-05 command bar and Help modal with int-based group system.

## Key Changes

### actions.go
- `Action.Group` changed from `string` to `int`
- `Action.Priority int` added — higher = further left in command bar
- `Action.HideFromBar bool` added — excluded from command bar, visible in Help modal
- `ActionManager.groupLabels map[int]string` field added
- `RegisterGroupLabel(group int, label string)` and `GroupLabel(group int) string` methods added
- `Visible()` updated: filters `HideFromBar`, sorts by Priority descending
- `RenderCommandBar()` rewritten: spec tokens (#7aa2f7 keys, #a9b1d6 labels, #565f89 sep), F1 right anchor

### root.go
- `ctrl+q`: Group: 1, Priority: 10, HideFromBar: false
- `f1` (was "?"): Group: 1, Priority: 0, HideFromBar: false
- `RegisterGroupLabel(1, "Global")` called after Register

### help.go
- `buildContent`: int-based group iteration with `sort.Ints`, uses `GroupLabel()` for headers
- Dismiss hint updated: "Esc or F1 close this help"
- Update handler: "esc", "f1" (was "esc", "?")
- Import `"sort"` added

### root_test.go
- `TestD09_ActionManagerBeforeModal`: updated to test "f1" key (was "?")

## Verification

- `go build ./internal/tui/...` — ✓ passes
- `go test ./internal/tui/...` — ✓ all tests pass

## key-files

### modified
- internal/tui/actions.go
- internal/tui/root.go
- internal/tui/help.go
- internal/tui/root_test.go

## Commit

5dc6bc3 feat(tui/05.2-02): Action struct int Group, Priority, HideFromBar; spec-correct command bar

---
phase: 05.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/root.go
  - internal/tui/help.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "F10 action responds to both f10 and f11 key presses"
    - "F3 and F5 absent from command bar but present in help modal"
    - "F6 absent from command bar (disabled) but present in help modal"
    - "Command bar renders actions in priority order: F10(90) leftmost, F1(0) right-anchored"
    - "Help modal shows 'Status' header for Group 2, no header for Group 0"
  artifacts:
    - path: "internal/tui/root.go"
      provides: "Updated action registrations with new priorities, groups, visibility"
      contains: 'Keys: []string{"f10", "f11"}'
    - path: "internal/tui/help.go"
      provides: "Group 0 skip logic in buildContentLines"
      contains: "if grp == 0"
  key_links:
    - from: "internal/tui/root.go"
      to: "internal/tui/actions.go"
      via: "ActionManager.Register() with new Keys/HideFromBar/Enabled/Priority/Group values"
      pattern: "actions\\.Register\\(m,"
    - from: "internal/tui/help.go"
      to: "internal/tui/root.go"
      via: "buildContentLines reads action.Group from All()"
      pattern: "if grp == 0"
---

<objective>
Update action configuration in root.go and help modal group rendering in help.go to validate the action architecture through targeted changes.

Purpose: Verify multi-key dispatch, selective visibility, priority inversion, and group reorganization work correctly with existing ActionManager code.
Output: Modified root.go with new action registrations, modified help.go with group 0 skip logic, all existing tests still passing.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.4-05-tui-scaffold-action-arch/05.4-CONTEXT.md
@.planning/STATE.md
@internal/tui/actions.go
@internal/tui/actions_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update root.go action registrations with new priorities, groups, and visibility flags</name>
  <files>internal/tui/root.go</files>
  <read_first>
    - internal/tui/root.go (lines 75-151 — current action registrations)
    - internal/tui/actions.go (Action struct definition, lines 23-33)
  </read_first>
  <action>
    In internal/tui/root.go newRootModel(), update the 15+ action registrations (lines 75-150) with these exact changes:

    1. F10 multi-key: Change `Keys: []string{"f10"}` to `Keys: []string{"f10", "f11"}` on the F10 "Truncar" action

    2. HideFromBar flags:
       - F3 "Dica campo": set `HideFromBar: true`
       - F5 "Alerta": set `HideFromBar: true`

    3. F6 disabled: Change `Enabled: func() bool { return true }` to `Enabled: func() bool { return false }` on F6 "Erro"

    4. Priority inversion (update Priority field on each action):
       - F2 "Dica uso": Priority 15
       - F3 "Dica campo": Priority 20
       - F4 "Info": Priority 30
       - F5 "Alerta": Priority 40
       - F6 "Erro": Priority 50
       - F7 "Ocupado": Priority 60
       - F8 "Sucesso": Priority 70
       - F9 "Limpar": Priority 80
       - F10 "Truncar": Priority 90
       - F1 "Ajuda": Priority 0 (unchanged)
       - ctrl+q "Sair": Priority 10 (unchanged)
       - Shift variants: keep existing priorities (they're HideFromBar so don't affect command bar)

    5. Group reorganization (update Group field on each action):
       - Group 1 "Mensagens": F2, F3, F4, F5, F6, F1, shift+F2, shift+F3, shift+F4, shift+F5, shift+F6
       - Group 2 "Status": F7 "Ocupado", F8 "Sucesso"
       - Group 0 (no header): F9 "Limpar", F10 "Truncar", ctrl+q "Sair"

    6. Add group label registration after the existing `actions.RegisterGroupLabel(1, "Mensagens")` line:
       Add: `actions.RegisterGroupLabel(2, "Status")`

    Do NOT modify any Handler functions, Scope values, or action Labels/Descriptions. Only change Keys, HideFromBar, Enabled, Priority, and Group fields.
  </action>
  <acceptance_criteria>
    - internal/tui/root.go contains `Keys: []string{"f10", "f11"}` on F10 action
    - internal/tui/root.go contains `HideFromBar: true` on F3 and F5 actions
    - internal/tui/root.go contains `Enabled: func() bool { return false }` on F6 action
    - internal/tui/root.go F10 action has `Priority: 90`, F2 has `Priority: 15`, F1 has `Priority: 0`
    - internal/tui/root.go contains `actions.RegisterGroupLabel(2, "Status")`
    - internal/tui/root.go F7 and F8 actions have `Group: 2`
    - internal/tui/root.go F9, F10, ctrl+q actions have `Group: 0`
    - `go build ./internal/tui/...` succeeds with zero errors
    - `go test ./internal/tui/...` passes all existing 8 tests (no test modifications)
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/tui/... && go test ./internal/tui/... -count=1</automated>
  </verify>
  <done>F10 has dual keys, F3/F5 hidden from bar, F6 disabled, priorities inverted, groups reorganized, build and all existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update help.go buildContentLines to skip group header for group 0</name>
  <files>internal/tui/help.go</files>
  <read_first>
    - internal/tui/help.go (buildContentLines function, lines 211-244)
    - internal/tui/root.go (verify Group 0 actions exist after Task 1)
  </read_first>
  <action>
    In internal/tui/help.go buildContentLines() function (lines 211-244), modify the group header rendering loop (around line 230-234) to skip the group header line when grp == 0.

    Current code (approximate):
    ```go
    for _, grp := range groupOrder {
        label := m.actions.GroupLabel(grp)
        if label != "" {
            lines = append(lines, groupStyle.Render(label))
        }
    ```

    Change to:
    ```go
    for _, grp := range groupOrder {
        if grp == 0 {
            // Group 0: no header — actions listed without section label
            for _, act := range grouped[grp] {
                if len(act.Keys) == 0 {
                    continue
                }
                lines = append(lines, "  "+keyStyle.Render(act.Keys[0])+"  "+act.Description)
            }
            continue
        }
        label := m.actions.GroupLabel(grp)
        if label != "" {
            lines = append(lines, groupStyle.Render(label))
        }
    ```

    This ensures Group 0 actions (F9, F10, ctrl+q) appear in the help modal without a "Group 0" or empty header line, while Groups 1 and 2 still show their registered labels ("Mensagens", "Status").
  </action>
  <acceptance_criteria>
    - internal/tui/help.go buildContentLines() contains `if grp == 0` check before rendering group header
    - internal/tui/help.go contains comment `// Group 0: no header` or similar explanatory comment
    - `go build ./internal/tui/...` succeeds with zero errors
    - `go test ./internal/tui/...` passes all existing tests
    - Help modal still renders Groups 1 and 2 with their headers ("Mensagens", "Status")
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/tui/... && go test ./internal/tui/... -count=1</automated>
  </verify>
  <done>Group 0 actions render without header in help modal, Groups 1-2 still show headers, build and all tests pass</done>
</task>

</tasks>

<verification>
- All existing 8 tests in actions_test.go pass without modification
- Build succeeds: `go build ./internal/tui/...`
- F10 action responds to both "f10" and "f11" keys (manual verification: run abditum, press both keys)
- F3 and F5 do NOT appear in command bar but DO appear in help modal (F1 → help overlay)
- F6 does NOT appear in command bar but DOES appear in help modal
- Command bar shows F10 (Priority 90) leftmost among visible actions, F1 right-anchored
- Help modal shows "Mensagens" header for Group 1, "Status" header for Group 2, no header for Group 0 actions
</verification>

<success_criteria>
- [ ] root.go: F10 has Keys: []string{"f10", "f11"}
- [ ] root.go: F3 and F5 have HideFromBar: true
- [ ] root.go: F6 has Enabled: func() bool { return false }
- [ ] root.go: Priorities inverted (F10=90, F9=80, F8=70, F7=60, F6=50, F5=40, F4=30, F3=20, F2=15, F1=0)
- [ ] root.go: Groups reorganized (Group 0: F9/F10/ctrl+q, Group 1: Mensagens, Group 2: Status)
- [ ] root.go: RegisterGroupLabel(2, "Status") added
- [ ] help.go: Group 0 actions render without header line
- [ ] All existing tests pass (8 tests in actions_test.go)
- [ ] Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05.4-05-tui-scaffold-action-arch/05.4-01-SUMMARY.md`
</output>

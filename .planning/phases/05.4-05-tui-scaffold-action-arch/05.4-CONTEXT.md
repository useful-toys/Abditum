# Phase 05.4: 05 tui-scaffold-action-arch - Context

**Gathered:** 2026-04-05
**Status:** Ready for planning
**Source:** User conversation (decisions captured inline)

<domain>
## Phase Boundary

Validate the action architecture through targeted configuration changes — multi-key dispatch, selective command bar visibility, priority-based truncation, and help modal grouping — plus comprehensive unit tests covering RenderCommandBar edge cases.

This phase touches NO logic in actions.go, only configuration (root.go registrations) and display (help.go group header skip). All 6 new tests verify existing behavior in actions.go.

</domain>

<decisions>
## Implementation Decisions

### Multi-Key Dispatch
- F10 gets second key F11: `Keys: []string{"f10", "f11"}` — both dispatch same handler, only "f10" (Keys[0]) shown in command bar
- No code change needed in actions.go — multi-key already supported by `Action.Keys []string` field and `Dispatch` loop over all keys

### Selective Command Bar Visibility
- F3 gets `HideFromBar: true` — hidden from command bar, still visible in help modal
- F5 gets `HideFromBar: true` — hidden from command bar, still visible in help modal
- F6 gets `Enabled: func() bool { return false }` — disabled, visible in help modal but excluded from command bar (Visible() filters out Enabled()==false)

### Priority Inversion (F2-F10)
Current → New:
| Action | Old Priority | New Priority |
|--------|-------------|-------------|
| F2     | 90          | 15          |
| F3     | 80          | 20          |
| F4     | 70          | 30          |
| F5     | 60          | 40          |
| F6     | 50          | 50          |
| F7     | 40          | 60          |
| F8     | 30          | 70          |
| F9     | 20          | 80          |
| F10    | 15          | 90          |
| F1     | 0           | 0 (unchanged) |

### Group Reorganization
- Group 1 "Mensagens": F2, F3, F4, F5, F6, F1, shift+F2, shift+F3, shift+F4, shift+F5, shift+F6
- Group 2 "Status": F7, F8
- Group 0 (no header in help modal): F9, F10, ctrl+q
- Add `actions.RegisterGroupLabel(2, "Status")` in root.go

### Help Modal Group 0 Skip
- `help.go` `buildContentLines`: skip group header line when `grp == 0` — actions in group 0 listed without a section header

### the agent's Discretion
- Test naming: follow existing `TestActionManager_*` and `TestRenderCommandBar_*` patterns
- Test fixtures: inline ActionManager setup, no external fixtures
- Existing 8 tests in actions_test.go remain untouched
- Shift+Fx priorities: keep relative to their base (shift+F2=14, shift+F3=19, etc.) or leave as-is since they're HideFromBar

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### TUI Action Architecture
- `internal/tui/actions.go` — ActionManager, Action struct, RenderCommandBar, Visible, All, Dispatch
- `internal/tui/actions_test.go` — Existing 8 tests (patterns to follow)
- `internal/tui/root.go` — PoC action registrations in newRootModel()
- `internal/tui/help.go` — helpModal buildContentLines with group headers

</canonical_refs>

<specifics>
## Specific Ideas

### Existing code state (pre-change):
- `actions.go` — 245 lines, 0 tests for RenderCommandBar
- `actions_test.go` — 8 tests (Dispatch, Visible, All, ClearOwned, ActiveOwner)
- `root.go` — 466 lines, 15 PoC actions all in Group 1
- `help.go` — 249 lines, group headers always rendered
- All 26 existing tests pass

### Current action registration order in root.go (lines 75-150):
1. F2 "Dica uso" — Group 1, Priority 90
2. F3 "Dica campo" — Group 1, Priority 80
3. F4 "Info" — Group 1, Priority 70
4. F5 "Alerta" — Group 1, Priority 60
5. F6 "Erro" — Group 1, Priority 50
6. F7 "Ocupado" — Group 1, Priority 40
7. F8 "Sucesso" — Group 1, Priority 30
8. F9 "Limpar" — Group 1, Priority 20
9. F10 "Truncar" — Group 1, Priority 15
10. shift+F2 — Group 1, Priority 89, HideFromBar
11. shift+F3 — Group 1, Priority 79, HideFromBar
12. shift+F4 — Group 1, Priority 69, HideFromBar
13. shift+F5 — Group 1, Priority 59, HideFromBar
14. shift+F6 — Group 1, Priority 49, HideFromBar
15. ctrl+q "Sair" — Group 1, Priority 10
16. F1 "Ajuda" — Group 1, Priority 0

### Test behaviors to verify:
1. Multi-key: both "f10" and "f11" trigger same handler
2. HideFromBar: action excluded from Visible() but present in All()
3. RenderCommandBar multi-key: shows Keys[0] only ("f10", not "f11")
4. RenderCommandBar truncation: drops lowest priority first on narrow width
5. RenderCommandBar F1 anchor: F1 stays right-anchored under truncation
6. RenderCommandBar narrow (20-col): shows only F1 anchor

</specifics>

<deferred>
## Deferred Ideas

None — phase scope fully captured in decisions above.

</deferred>

---

*Phase: 05.4-05-tui-scaffold-action-arch*
*Context gathered: 2026-04-05 via user conversation*

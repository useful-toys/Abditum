# Plan 05.4-03: Bug Fixes — Modal Overlay + Scroll Alignment

**Phase:** 05.4 — 05 tui-scaffold-action-arch
**Wave:** 3
**Status:** Complete

## What Was Done

### Task 1: Fixed root.go View() — modal overlay instead of replace
- Replaced `lipgloss.Place` with `overlayModal` function
- `overlayModal` splits frame into lines, calculates centered modal position, and overlays modal lines onto frame lines
- Frame content remains visible behind/around the modal
- Added helper functions: `overlayLine`, `decodeRune`, `runeWidth`

### Task 2: Fixed help.go scroll indicator alignment
- Lines with scroll indicators (↑↓■) now have the same visual width as lines without indicators
- Before: indicator lines were 4 chars narrower than regular lines
- After: both branches produce lines of width `innerW+6` (left border + padding + content + padding + indicator + right border)

## Files Modified
- `internal/tui/root.go` — View() + overlayModal + helpers
- `internal/tui/help.go` — renderDialog indicator alignment fix

## Verification
- `go build ./internal/tui/...` — passed
- `go test ./internal/tui/... -count=1` — 14 tests passed (all existing)
- No test modifications needed

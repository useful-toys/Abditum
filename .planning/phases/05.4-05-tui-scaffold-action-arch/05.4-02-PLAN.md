---
phase: 05.4
plan: 02
type: execute
wave: 2
depends_on: ["05.4-01"]
files_modified:
  - internal/tui/actions_test.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Both f10 and f11 keys trigger the same action handler"
    - "HideFromBar actions excluded from Visible() but present in All()"
    - "Disabled actions (Enabled=false) excluded from Visible() but present in All()"
    - "RenderCommandBar truncates lowest priority actions first when width constrained"
    - "RenderCommandBar preserves F1 anchor even at 20-column width"
  artifacts:
    - path: "internal/tui/actions_test.go"
      provides: "6 new unit tests"
      contains: "TestActionManager_Dispatch_MultiKey"
    - path: "internal/tui/actions_test.go"
      provides: "RenderCommandBar edge case tests"
      contains: "TestRenderCommandBar_TruncatesLowestPriority"
  key_links:
    - from: "internal/tui/actions_test.go"
      to: "internal/tui/actions.go"
      via: "ActionManager.Dispatch/Visible/All/RenderCommandBar methods"
      pattern: "am\\.(Dispatch|Visible|All|RenderCommandBar)"
---

<objective>
Add 6 new unit tests to actions_test.go covering multi-key dispatch, HideFromBar visibility, and RenderCommandBar edge cases (multi-key display, priority truncation, F1 anchor, narrow terminal).

Purpose: Validate that existing ActionManager behavior works correctly for the new configuration patterns introduced in Plan 01 — without modifying any production code.
Output: 6 new tests in actions_test.go, all 14 tests (8 existing + 6 new) passing.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.4-05-tui-scaffold-action-arch/05.4-CONTEXT.md
@.planning/phases/05.4-05-tui-scaffold-action-arch/05.4-01-PLAN.md
@.planning/STATE.md
@internal/tui/actions.go
@internal/tui/actions_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 ActionManager behavior tests (multi-key dispatch, HideFromBar visibility, Enabled=false visibility)</name>
  <files>internal/tui/actions_test.go</files>
  <read_first>
    - internal/tui/actions_test.go (existing 8 tests — follow same patterns)
    - internal/tui/actions.go (Dispatch lines 93-120, Visible lines 123-158, All lines 161-167)
  </read_first>
  <action>
    Append 4 new test functions to internal/tui/actions_test.go after the existing TestActionManager_All test (line 146). Follow the exact same pattern: package tui, import tea "charm.land/bubbletea/v2", inline ActionManager setup with closures.

    Test 1 — TestActionManager_Dispatch_MultiKey:
    - Create ActionManager, register action with Keys: []string{"f10", "f11"}, Enabled: func() bool { return true }, Handler sets called=true
    - Assert am.Dispatch("f10", false) sets called=true
    - Reset called=false, assert am.Dispatch("f11", false) sets called=true
    - Error message: "both f10 and f11 must trigger the same handler"

    Test 2 — TestActionManager_Visible_HidesFromBar:
    - Create ActionManager, register two actions:
      - Action A: Keys: []string{"a"}, HideFromBar: false, Enabled: func() bool { return true }
      - Action B: Keys: []string{"b"}, HideFromBar: true, Enabled: func() bool { return true }
    - Assert len(am.Visible()) == 1 and Visible()[0].Keys[0] == "a"
    - Assert len(am.All()) == 2 (both present in All())
    - Error message: "HideFromBar action excluded from Visible() but present in All()"

    Test 3 — TestActionManager_Visible_DisabledExcluded:
    - Create ActionManager, register two actions:
      - Action A: Keys: []string{"a"}, Enabled: func() bool { return true }
      - Action B: Keys: []string{"b"}, Enabled: func() bool { return false }
    - Assert len(am.Visible()) == 1 and Visible()[0].Keys[0] == "a"
    - Assert len(am.All()) == 2 (both present in All())
    - Error message: "Enabled()==false action excluded from Visible() but present in All()"

    Test 4 — TestActionManager_Dispatch_MultiKey_InFlowOrModal:
    - Create ActionManager, register action with Keys: []string{"f10", "f11"}, Scope: ScopeGlobal, Enabled: func() bool { return true }
    - Assert am.Dispatch("f10", true) fires (ScopeGlobal, in flow/modal)
    - Reset called=false, assert am.Dispatch("f11", true) fires
    - Error message: "both keys must work when inFlowOrModal=true"
  </action>
  <acceptance_criteria>
    - internal/tui/actions_test.go contains function TestActionManager_Dispatch_MultiKey
    - internal/tui/actions_test.go contains function TestActionManager_Visible_HidesFromBar
    - internal/tui/actions_test.go contains function TestActionManager_Visible_DisabledExcluded
    - internal/tui/actions_test.go contains function TestActionManager_Dispatch_MultiKey_InFlowOrModal
    - All 4 new tests pass: `go test ./internal/tui/... -run "TestActionManager_Dispatch_MultiKey|TestActionManager_Visible_HidesFromBar|TestActionManager_Visible_DisabledExcluded|TestActionManager_Dispatch_MultiKey_InFlowOrModal" -v`
    - All 8 existing tests still pass: `go test ./internal/tui/... -count=1`
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/tui/... -count=1 -v</automated>
  </verify>
  <done>4 new tests pass, all 12 existing+new tests pass, no production code modified</done>
</task>

<task type="auto">
  <name>Task 2: Add 2 RenderCommandBar edge case tests (truncation, F1 anchor, narrow terminal)</name>
  <files>internal/tui/actions_test.go</files>
  <read_first>
    - internal/tui/actions_test.go (after Task 1 additions)
    - internal/tui/actions.go (RenderCommandBar lines 172-245, StyleCommandKey/StyleCommandLabel in tokens.go)
  </read_first>
  <action>
    Append 2 new test functions to internal/tui/actions_test.go. These test RenderCommandBar which depends on StyleCommandKey() and StyleCommandLabel() from tokens.go — ensure the tui package builds cleanly.

    Test 5 — TestRenderCommandBar_TruncatesLowestPriority:
    - Create ActionManager with 4 actions at different priorities:
      - "high": Keys: []string{"f10"}, Priority: 90, Label: "High"
      - "mid": Keys: []string{"f9"}, Priority: 50, Label: "Mid"  
      - "low": Keys: []string{"f2"}, Priority: 10, Label: "Low"
      - "anchor": Keys: []string{"f1"}, Priority: 0, Label: "Help"
    - Call am.RenderCommandBar(40) — narrow width that can't fit all actions
    - Assert the result contains "f10" (highest priority, kept)
    - Assert the result does NOT contain "f2" (lowest priority, truncated first)
    - Assert the result contains "f1" (anchor, always preserved)
    - Error message: "lowest priority action must be truncated first, anchor preserved"

    Test 6 — TestRenderCommandBar_NarrowTerminal_F1Only:
    - Create ActionManager with same 4 actions as Test 5
    - Call am.RenderCommandBar(20) — extremely narrow width
    - Assert the result contains "f1" (F1 anchor must survive even at 20 cols)
    - Assert the result does NOT contain "f10", "f9", or "f2" (all body actions truncated)
    - Error message: "at 20 cols only F1 anchor should remain"

    Note: RenderCommandBar uses lipgloss styling which adds ANSI escape codes. Use strings.Contains for key tokens (e.g., "f10", "f1") rather than exact string matching. The key style is bold + #7aa2f7, so raw output will contain escape sequences — test for the key string presence, not exact rendering.
  </action>
  <acceptance_criteria>
    - internal/tui/actions_test.go contains function TestRenderCommandBar_TruncatesLowestPriority
    - internal/tui/actions_test.go contains function TestRenderCommandBar_NarrowTerminal_F1Only
    - TestRenderCommandBar_TruncatesLowestPriority: result contains "f10", does NOT contain "f2", contains "f1"
    - TestRenderCommandBar_NarrowTerminal_F1Only: result contains "f1", does NOT contain "f10"/"f9"/"f2"
    - All 14 tests (8 existing + 6 new) pass: `go test ./internal/tui/... -count=1`
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/tui/... -count=1 -v</automated>
  </verify>
  <done>2 RenderCommandBar tests pass, all 14 tests pass, no production code modified</done>
</task>

</tasks>

<verification>
- All 14 tests pass: `go test ./internal/tui/... -count=1`
- Build succeeds: `go build ./internal/tui/...`
- No production code (actions.go, root.go, help.go) modified by this plan
- Test coverage: multi-key dispatch (2 tests), HideFromBar visibility (1 test), Disabled visibility (1 test), RenderCommandBar truncation (1 test), RenderCommandBar narrow terminal (1 test)
</verification>

<success_criteria>
- [ ] TestActionManager_Dispatch_MultiKey: both f10 and f11 trigger handler
- [ ] TestActionManager_Visible_HidesFromBar: HideFromBar excluded from Visible(), present in All()
- [ ] TestActionManager_Visible_DisabledExcluded: Enabled()==false excluded from Visible(), present in All()
- [ ] TestActionManager_Dispatch_MultiKey_InFlowOrModal: both keys work in flow/modal context
- [ ] TestRenderCommandBar_TruncatesLowestPriority: lowest priority dropped first, anchor preserved
- [ ] TestRenderCommandBar_NarrowTerminal_F1Only: 20-col terminal shows only F1
- [ ] All 14 tests pass (8 existing + 6 new)
- [ ] Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05.4-05-tui-scaffold-action-arch/05.4-02-SUMMARY.md`
</output>

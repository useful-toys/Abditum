---
plan: 04.1-03
phase: 04.1
status: complete
completed: 2026-03-31
---

## Summary

Refactored Manager to use entity private methods added in Plan 01, fixed the `AlternarFavorito` bug (D-08), and added 3 regression tests proving the correct behavior.

## What Was Built

**PATTERN A/B** — All `m.cofre.modificado = true` + `m.cofre.dataUltimaModificacao = time.Now()` blocks across 20+ Manager methods replaced with:
- `m.cofre.marcarModificado()` (PATTERN A — cofre-only mutations)
- `segredo.marcarModificacao()` + `m.cofre.marcarModificado()` (PATTERN B — with secret timestamp)

**PATTERN C — AlternarFavoritoSegredo bug fix (D-08, BUG-01)**
- Removed `segredo.dataUltimaModificacao = now` — favoriting is navigation preference, NOT content edit
- Only `cofre.marcarModificado()` called

**PATTERN D — Factory estadoSessao override removal (D-05)**
- Removed `segredo.estadoSessao = EstadoModificado` in `CriarSegredo`
- Removed `duplicate.estadoSessao = EstadoModificado` in `DuplicarSegredo`
- Factory already sets `EstadoIncluido` (Plan 01 fix)

**PATTERN E — prepararSnapshot (D-06)**
- `snapshot.pastaGeral = m.cofre.pastaGeral.copiarProfundo(true)` (was `copiarPastaRecursivamente`)

**PATTERN F — limparCamposSensiveis (CRYPTO-04)**
- Per-secret loop body replaced with `segredo.zerarValoresSensiveis()`

**Removed methods** (replaced by entity methods):
- `copiarPastaRecursivamente` → `Pasta.copiarProfundo`
- `copiarSegredo` → `Segredo.copiar`
- `copiarCampo` → `CampoSegredo.copiar`

**3 Regression tests added to manager_test.go:**
- `TestAlternarFavorito_NaoAlteraDataUltimaModificacao` — D-08 invariant verified
- `TestCriarSegredo_EstadoIncluido` — D-05 factory fix verified
- `TestDuplicarSegredo_EstadoIncluido` — D-05 duplicate factory fix verified

**Updated existing tests** to reflect correct D-05/D-08 semantics (EstadoIncluido for new secrets, hard delete for EstadoIncluido deletion).

## Key Files

### Modified
- `internal/vault/manager.go` — full refactoring, removed 3 methods, removed `time` import
- `internal/vault/manager_test.go` — 3 new regression tests + TestAtomicSave fix
- `internal/vault/validation_test.go` — 8 test functions updated for D-05/D-08 semantics

## Verification

- `go build ./...` — clean
- Zero `time.Now()` calls in manager.go
- Zero `cofre.dataUltimaModificacao =` direct assignments in manager.go
- `go test ./... -count=1` — all packages green
- All 3 regression tests pass

## Decisions

- `m.cofre.modificado = false` in Salvar() kept as direct assignment (save-completion reset, not mutation tracking)
- `crypto` import kept (Lock() still wipes password directly)

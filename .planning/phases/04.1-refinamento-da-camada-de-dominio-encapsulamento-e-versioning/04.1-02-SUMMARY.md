---
plan: 04.1-02
phase: 04.1
status: complete
completed: 2026-03-31
---

## Summary

Updated `DeserializarCofre` to accept a `version uint8` parameter, updated the storage call site, deleted `migrate.go`, and removed 4 Migrate test functions from `storage_test.go`.

## What Was Built

- `DeserializarCofre(data []byte, version uint8)` — new signature with version reserved for future compat field selection (D-01)
- `segredoJSON` struct gets compat field comment documenting the pattern for future format evolution
- `internal/storage/storage.go` call site updated: `vault.DeserializarCofre(jsonBytes, version)` — `version` already in scope from file header
- `internal/storage/migrate.go` deleted — retroactive compatibility handled via compat fields in JSON structs (D-02)
- 4 Migrate test functions removed: `TestMigrate_SameVersion_NoOp`, `TestMigrate_FutureVersion_Error`, `TestMigrate_Downgrade_Error`, `TestMigrate_V1FixtureRoundtrip`
- All `serialization_test.go` call sites updated to pass `version=1`

## Key Files

### Modified
- `internal/vault/serialization.go` — updated signature + compat comment on segredoJSON
- `internal/storage/storage.go` — updated DeserializarCofre call site
- `internal/storage/storage_test.go` — removed Migrate test block
- `internal/vault/serialization_test.go` — updated all call sites to pass version=1

### Deleted
- `internal/storage/migrate.go`

## Verification

- `go test ./internal/vault/... ./internal/storage/... -count=1` — all green
- `internal/storage/migrate.go` does not exist
- `grep storage.Migrate internal/**/*.go` — no references found

## Decisions

- Tests pass `version=1` (current format); `version` not yet used in body (reserved)

---
phase: 05.3
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/help.go
autonomous: true
requirements: []
gap_closure: true
---

<objective>
Fix help modal to match DS dialog anatomy: title in top border, "Esc Fechar" in bottom border with border.default color, 2-col horizontal padding, 1-line vertical padding, dynamic sizing min(70, 80% terminal), scroll indicators.

Purpose: Help modal was a small 60-column inline box with wrong colors, no padding, no scroll, wrong border rendering.
Output: Full DS-compliant dialog with correct anatomy, padding, sizing, and scroll support.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-merge-poc-to-app/05.3-CONTEXT.md
@tui-design-system-novo.md §Dimensionamento de diálogos (§234-253)
@tui-design-system-novo.md §Scroll em diálogos (§255-288)
@tui-design-system-novo.md §Sobreposição (§388-508) — Dialog anatomy
@tui-specification-novo.md §Help (§604-710) — Full spec
@internal/tui/help.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite help modal View() with DS-compliant anatomy</name>
  <files>internal/tui/help.go</files>
  <read_first>
    - internal/tui/help.go
    - tui-design-system-novo.md §Dimensionamento de diálogos
    - tui-design-system-novo.md §Scroll em diálogos
    - tui-specification-novo.md §Help
  </read_first>
  <action>
    Rewrite helpModal.View():
    1. Dynamic sizing: width = min(70, 80% of terminal width)
    2. Dynamic height: max 80% of terminal height, minus top/bottom borders
    3. Top border: "╭── Ajuda — Atalhos e Ações ──╮" with title embedded
    4. Bottom border: "╰── ──────── Esc Fechar ──╯" with action in border color
    5. Content: 2-col horizontal padding (DS §240), 1-line vertical padding top/bottom (DS §241)
    6. Scroll indicators: ↑/↓ arrows on right border, ■ thumb for position
    7. Scroll navigation: ↑↓ (line), PgUp/PgDn (page), Home/End (top/bottom)
    8. Use border.default color for borders and bottom action (DS §454)
    9. Use text.primary bold for title, accent.primary for keys, text.secondary bold for group labels
  </action>
  <verify>
    <automated>go build ./internal/tui/</automated>
  </verify>
  <done>Help modal renders with correct DS anatomy, padding, sizing, scroll, and colors</done>
</task>

<task type="auto">
  <name>Task 2: Add SetSize to helpModal and root.go View()</name>
  <files>internal/tui/help.go, internal/tui/root.go, internal/tui/flows.go</files>
  <read_first>
    - internal/tui/flows.go — modalView interface
    - internal/tui/root.go — View() method
    - internal/tui/help.go — helpModal struct
  </read_first>
  <action>
    1. Add SetSize(w, h int) to modalView interface in flows.go
    2. Implement SetSize on helpModal to store terminal dimensions
    3. Call top.SetSize(m.width, m.height) in rootModel.View() before rendering modal
    4. Add SetSize stub (no-op) to modalModel, passwordEntryModal, passwordCreateModal, filePickerModal
  </action>
  <verify>
    <automated>go build ./internal/tui/ && go test ./internal/tui/... -count=1</automated>
  </verify>
  <done>modalView interface has SetSize; all modal types implement it; rootModel passes dimensions</done>
</task>

</tasks>

<verification>
- Help modal title embedded in top border: "╭── Ajuda — Atalhos e Ações ──╮"
- Bottom border uses border.default color for "Esc Fechar" action
- 2-col horizontal padding on content lines (DS §240)
- 1-line vertical padding at top and bottom of content (DS §241)
- Scroll indicators ↑/↓/■ on right border when content exceeds viewport
- Dynamic sizing: min(70, 80% terminal width)
- All modal types implement SetSize(w, h int)
- root_test.go stubModal updated with SetSize stub
</verification>

<success_criteria>
- [ ] Help modal renders with title in top border per DS §436-458
- [ ] "Esc Fechar" in bottom border with border.default color per DS §454
- [ ] 2-col horizontal + 1-line vertical padding per DS §240-241
- [ ] Scroll indicators functional when content exceeds viewport
- [ ] modalView interface includes SetSize(w, h int)
- [ ] All modal types implement SetSize
- [ ] go build ./internal/tui/ succeeds
- [ ] go test ./internal/tui/... passes
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-merge-poc-to-app/05.3-05-SUMMARY.md`
</output>

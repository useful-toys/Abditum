---
phase: 05.3
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/root.go
  - internal/tui/messages.go
autonomous: true
requirements: []
gap_closure: true
---

<objective>
Fix message bar position and symbol-text spacing.

Purpose: Message bar was rendered between header and work area (line 2) instead of as penultimate line above command bar. Spec defines 4-zone vertical layout: header(2) → workArea(rest) → msgBar(1) → cmdBar(1). Also: 2 spaces between symbol and text for wide-char visibility.
Output: Correct zone ordering and visible spacing.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-merge-poc-to-app/05.3-CONTEXT.md
@tui-design-system-novo.md §Zonas verticais (§214-221)
@tui-design-system-novo.md §Barra de mensagens (§290-300)
@internal/tui/root.go — renderFrame()
@internal/tui/messages.go — RenderMessageBar()
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix message bar position and symbol spacing</name>
  <files>internal/tui/root.go, internal/tui/messages.go</files>
  <read_first>
    - internal/tui/root.go — renderFrame()
    - tui-design-system-novo.md §Zonas verticais
  </read_first>
  <action>
    1. In root.go renderFrame(): reorder zones from [header, msgBar, workArea, cmdBar] to [header, workArea, msgBar, cmdBar]
    2. In messages.go: change symbol+text separator from 1 space to 2 spaces (symStyle.Render(symbol + "  ")) for visibility with wide chars like ℹ and ⚠
  </action>
  <verify>
    <automated>go build ./internal/tui/</automated>
  </verify>
  <done>Message bar is penultimate line; 2 spaces between symbol and text</done>
</task>

</tasks>

<verification>
- renderFrame() returns zones in order: header → workArea → msgBar → cmdBar
- messages.go uses 2 spaces between symbol and text
- go build ./internal/tui/ succeeds
</verification>

<success_criteria>
- [ ] Message bar positioned as penultimate line (above command bar)
- [ ] 2 spaces between symbol and text in RenderMessageBar
- [ ] go build ./internal/tui/ succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-merge-poc-to-app/05.3-06-SUMMARY.md`
</output>

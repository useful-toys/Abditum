---
phase: 05.3
plan: 05
type: execute
wave: 1
gap_closure: true
completed: 2026-04-04
---

# 05.3-05: Help modal DS anatomy + SetSize interface

## Objective
Fix help modal to match DS dialog anatomy and add SetSize to modalView interface.

## What was done
- Rewrote `helpModal.View()` with full DS-compliant anatomy:
  - Title "Ajuda — Atalhos e Ações" embedded in top border (`╭── Title ──╮`)
  - "Esc Fechar" in bottom border with `border.default` color (DS §454)
  - 2-col horizontal padding (DS §240), 1-line vertical padding top/bottom (DS §241)
  - Dynamic sizing: width = min(70, 80% terminal), height = 80% terminal
  - Scroll indicators: ↑/↓ arrows, ■ thumb for position
  - Scroll navigation: ↑↓ (line), PgUp/PgDn (page), Home/End (top/bottom)
- Added `SetSize(w, h int)` to `modalView` interface in `flows.go`
- Added `SetSize` stubs to all modal types:
  - `modalModel` (modal.go)
  - `passwordEntryModal`, `passwordCreateModal`, `filePickerModal` (dialogs.go)
  - `stubModal` (root_test.go)
- `rootModel.View()` calls `top.SetSize(m.width, m.height)` before rendering modal

## Key files
- `internal/tui/help.go` — full rewrite
- `internal/tui/flows.go` — modalView interface updated
- `internal/tui/root.go` — View() calls SetSize on modals
- `internal/tui/modal.go`, `dialogs.go`, `root_test.go` — SetSize stubs

## Verification
- `go build ./internal/tui/` succeeds
- `go test ./internal/tui/...` passes
- Help modal renders with correct DS anatomy, padding, and scroll

---
phase: 05.3
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/actions.go
autonomous: true
requirements: []
gap_closure: true
---

<objective>
Fix command bar: F1 anchor right-aligned with priority-based truncation.

Purpose: RenderCommandBar() rendered ALL visible actions regardless of width, pushing F1 off-screen. Spec requires F1 always visible, dropping lowest-priority actions first when space is insufficient.
Output: F1 "Ajuda" fixed at right edge; body actions truncated by priority when terminal is narrow.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-merge-poc-to-app/05.3-CONTEXT.md
@tui-specification-novo.md §Barra de Comandos (§904-1001) — Anatomy, F1 anchor, truncation
@internal/tui/actions.go — RenderCommandBar()
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add priority-based truncation to RenderCommandBar</name>
  <files>internal/tui/actions.go</files>
  <read_first>
    - internal/tui/actions.go
    - tui-specification-novo.md §Barra de Comandos
  </read_first>
  <action>
    In RenderCommandBar():
    1. Separate F1 anchor from body actions
    2. Build body string with all actions
    3. If body + anchor + min gap (1) exceeds width, drop lowest-priority body actions one by one until it fits
    4. If even empty body + anchor doesn't fit, show only "  F1 Ajuda"
    5. Use lipgloss.Width() for all width calculations
  </action>
  <verify>
    <automated>go build ./internal/tui/</automated>
  </verify>
  <done>F1 always visible at right edge; body actions drop by priority when width is insufficient</done>
</task>

</tasks>

<verification>
- F1 anchor visible at right edge at any terminal width ≥ 20 cols
- Lowest-priority actions dropped first when space is tight
- lipgloss.Width() used for all width calculations (not len())
</verification>

<success_criteria>
- [ ] RenderCommandBar truncates body actions by priority when width is insufficient
- [ ] F1 "Ajuda" always visible at right edge
- [ ] go build ./internal/tui/ succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-merge-poc-to-app/05.3-04-SUMMARY.md`
</output>

---
phase: 05.3
plan: 04
type: execute
wave: 1
gap_closure: true
completed: 2026-04-04
---

# 05.3-04: Command bar F1 anchor + priority truncation

## Objective
Fix command bar: F1 anchor right-aligned with priority-based truncation.

## What was done
- `RenderCommandBar()` separates F1 anchor from body actions
- Body actions truncated by priority (lowest dropped first) when width insufficient
- Minimum gap of 1 space enforced between body and anchor
- If even empty body + anchor doesn't fit, shows only "  F1 Ajuda"
- All width calculations use `lipgloss.Width()` (not `len()`)

## Key files
- `internal/tui/actions.go` — RenderCommandBar() truncation logic

## Verification
- F1 always visible at right edge at any terminal width ≥ 20 cols
- Lowest-priority actions dropped first when space is tight
- `go build ./internal/tui/` succeeds

# Phase 06.3: Reimplement file picker - Discussion Log (Assumptions Mode)

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in CONTEXT.md — this log preserves the analysis.

**Date:** 2026-04-07
**Phase:** 06.3-reimplement-file-picker
**Mode:** assumptions
**Areas analyzed:** Tree State Model, Open/Save Semantic Mode, MessageManager Wiring, Test Suite Compatibility, File Location

---

## Assumptions Presented

### Tree State Model
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| Use recursive `treeNode` struct with lazy loading | Likely | `dialogs.go:119` uses flat `[]string`; spec requires expand/collapse, 3 indicators, depth indentation — impossible without tree structure |

Alternatives offered: flat `[]renderItem` vs recursive `treeNode`.

### Open/Save Semantic Mode
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| Replace `FilePickerMode` enum with `FilePickerOpen \| FilePickerSave` | Likely | `flow_create_vault.go:80` passes `FilePickerFile` to mean save; modal treats all modes identically; spec defines structurally different behaviors |

Alternatives offered: replace enum vs add `saveMode bool` field.

### MessageManager Wiring
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| Receive `*MessageManager` from flow at construction | Unclear | `filePickerModal` struct has no `messages` field; password modals create own `NewMessageManager()` but hints are silently dropped |

Alternatives offered: receive pointer vs emit `filePickerHintMsg`.

### Test Suite Compatibility
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| ≥5 tests will break and must be updated in same plan | Confident | `filepicker_test.go:270` asserts relative dates; spec uses `dd/mm/aa HH:MM`; `filepicker_test.go:228` initial focus will change from 1→0 |

No alternatives (confident — factual breakage analysis).

### File Location
*Added interactively during corrections.*
| Assumption | Confidence | Evidence |
|------------|-----------|----------|
| Extract to `internal/tui/filepicker.go` | Likely | `dialogs.go` ~400 lines; `filepicker_test.go` already exists as separate file |

---

## Corrections Made

### Tree State Model
- **Original assumption:** Either flat `[]renderItem` or recursive `treeNode`
- **User correction:** Alt B — recursive `treeNode` with lazy loading
- **Reason:** More faithful to OS file picker behavior

### Open/Save Semantic Mode
- **Original assumption:** Either enum replacement or `saveMode bool` field
- **User correction:** Alt A — replace enum with `FilePickerOpen | FilePickerSave`
- **Reason:** Cleaner API

### MessageManager Wiring
- **Original assumption:** Either receive pointer or emit new message type
- **User correction:** Alt A — receive `*MessageManager` from flow at construction
- **Reason:** Consistent with how flows already pass messages

### File Location
- **Original assumption:** Implicit (filepicker in dialogs.go)
- **User correction:** Extract to `internal/tui/filepicker.go`
- **Reason:** dialogs.go already large; dedicated file matches existing test file naming

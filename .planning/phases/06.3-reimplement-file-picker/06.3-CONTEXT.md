# Phase 06.3: Reimplement file picker - Context

**Gathered:** 2026-04-07 (assumptions mode)
**Status:** Ready for planning

<domain>
## Phase Boundary

Replace the existing placeholder `filePickerModal` (currently embedded in `dialogs.go`) with a spec-compliant implementation of the two-panel file picker defined in `tui-specification-novo.md` §FilePicker.

The current implementation is a simplified stub: flat directory list (no tree), no path header, no proper borders/junctions, wrong metadata format, wrong initial focus, no MessageManager wiring, and Open/Save modes are not differentiated. This phase makes the picker fully spec-compliant.

This phase implements:
- **`filePickerModal`** extracted into `internal/tui/filepicker.go` — spec-compliant two-panel modal
- **Lazy recursive tree** (`treeNode` struct) with expand/collapse, `▶`/`▼`/`▷` indicators, 2-space indentation per level
- **Open mode:** Tree → Files (2 Tab stops); `Enter` on file confirms; cursor movement in tree updates file panel in real-time
- **Save mode:** Tree → Files → Campo Nome (3 Tab stops); `Enter` on file copies name to field (does not confirm); `bubbles/textinput` filename field
- **Spec-accurate borders:** `border.focused` main border; `├┬┴┤` junctions with `border.default`; scroll indicators `↑`/`↓`/`■` replace border chars on the respective panel side
- **Path header:** `Caminho: /path/to/dir` — updates as tree cursor moves
- **Metadata format:** `dd/mm/aa HH:MM` (not relative)
- **MessageManager wiring:** hints from the flow's `*MessageManager` passed at construction time
- **Semantic mode enum:** `FilePickerOpen | FilePickerSave` replacing `FilePickerFile/Directory/Any`
- **Updated tests** in `filepicker_test.go` to match new behavior (dates, sizes, focus, struct shape)

This phase does NOT implement:
- Mouse click/double-click interactions (spec documents them but they are lower priority; keyboard nav is the MVP)
- `special.highlight` token as a new Theme field — use hardcoded Tokyo Night hex for the selected file row background until Phase 9 (theme system)
- Any changes to password modals, flows, or other TUI components
</domain>

<decisions>
## Implementation Decisions

### Architectural Classification

- **D-00: `filePickerModal` is a functional modal dialog — implements `modalView`, not `childModel`**
  - The spec classifies FilePicker under §Diálogos Funcionais alongside PasswordEntry and PasswordCreate.
  - `filePickerModal` must satisfy the `modalView` interface: `Init() tea.Cmd`, `Update(tea.Msg) tea.Cmd`, `View() string`, `SetSize(w, h int)`, `Shortcuts() []Shortcut`.
  - It is pushed onto the modal stack via `pushModalMsg{modal: fpk}` and dismissed via `popModalMsg{}` — the same lifecycle as all other modals.
  - It does **not** implement `childModel` (no `Render(width, height int) string` variant; it is not a persistent work area).
  - **No border.default:** functional dialogs use `border.focused` (see D-09). Decision dialogs use `border.default` with semantic severity colors — FilePicker is explicitly not a decision dialog.

### Tree Data Structure

- **D-01: Lazy recursive `treeNode` struct for the Estrutura panel**
  - `treeNode` struct: `path string`, `name string`, `depth int`, `expanded bool`, `loaded bool`, `children []*treeNode`, `hasSubdirs bool` (for `▷` detection).
  - Lazy loading: `os.ReadDir` is called on a node only when it is first expanded (`expanded = true && !loaded`). After loading, `loaded = true`.
  - **Visible items list:** a `[]visibleNode` (flattened view of expanded tree) is rebuilt after every expand/collapse and on init. Navigation (`↑↓`) operates on this flat slice.
  - **Initial tree state:** expand only the path from `/` down to the initial directory (CWD or fallback). All other nodes start collapsed.
  - **▷ indicator:** a directory shows `▷` when it has no subdirectories (but may still contain `.abditum` files). Detected at load time by checking if `os.ReadDir` returns any `IsDir()` entries.
  - **Cursor movement auto-updates file panel:** moving the tree cursor (even without expanding) updates `currentPath` to the node under the cursor and reloads the file list.

### Mode Enum

- **D-02: Replace `FilePickerMode` with semantic `FilePickerOpen | FilePickerSave`**
  - Remove constants `FilePickerFile`, `FilePickerDirectory`, `FilePickerAny`.
  - Add `FilePickerOpen FilePickerMode = iota` and `FilePickerSave`.
  - Update 3 call sites:
    - `flow_open_vault.go` — push `&filePickerModal{mode: FilePickerOpen}`
    - `flow_create_vault.go` — push `&filePickerModal{mode: FilePickerSave}`
    - `flow_save_and_exit.go` — push `&filePickerModal{mode: FilePickerSave}`
  - The `FilePicker(title, mode, ext)` factory in `dialogs.go` can be updated or removed if callers push directly.

### MessageManager Wiring

- **D-03: Receive `*MessageManager` from the flow at construction time**
  - `filePickerModal` struct gains a `messages *MessageManager` field.
  - Callers pass the flow's `messages` when constructing the modal:
    ```go
    &filePickerModal{mode: FilePickerOpen, messages: f.messages}
    ```
  - Hints are emitted via `m.messages.Show(MsgHint, text, 0, true)` on every focus change (panel switch, file selection change).
  - Messages per spec:
    - Open — tree focus: `• Navegue pelas pastas e selecione um cofre`
    - Open — files focus (with selection): `• Selecione o cofre para abrir`
    - Open — files focus (empty panel): `• Nenhum cofre neste diretório — navegue para outra pasta`
    - Save — tree focus: `• Navegue pelas pastas e escolha onde salvar`
    - Save — files focus: `• Arquivos existentes neste diretório`
    - Save — field focus (empty): `• Digite o nome do arquivo — .abditum será adicionado automaticamente`
    - Save — field focus (filled): `• Confirme para salvar o cofre`

### File Location

- **D-04: Extract `filePickerModal` into `internal/tui/filepicker.go`**
  - Remove the `filePickerModal` struct and all its methods from `dialogs.go`.
  - Keep the `FilePicker()` factory function in `dialogs.go` (or inline at call sites — planner's discretion).
  - `filepicker_test.go` already exists and imports `package tui` — no test file relocation needed.

### Metadata Format

- **D-05: Absolute date/time format `dd/mm/aa HH:MM` (spec format)**
  - Go format string: `"02/01/06 15:04"` applied to `fileInfo.ModTime().Local()`.
  - Replaces the current `formatRelativeDate()` function (remove it).
  - Retains `formatFileSize()` but updates to space-separated format: `25.8 MB`, `1.2 KB` (not `25.8MB`).

### Tab Stop Order and Confirmation

- **D-06: Tab cycles differ by mode; Enter semantics differ on file panel**
  - Open mode: Tab cycles Tree (0) → Files (1) → Tree (0). 2 stops.
  - Save mode: Tab cycles Tree (0) → Files (1) → Campo Nome (2) → Tree (0). 3 stops.
  - `Enter` on file panel — Open mode: confirms selection (emits `filePickerResult{Path}`), pops modal.
  - `Enter` on file panel — Save mode: copies filename (without extension) into the textinput field, moves focus to campo nome (focusPanel = 2). Does NOT confirm.
  - `Enter` on campo nome (Save mode): confirms if field non-empty (emits `filePickerResult{Path}` with `.abditum` appended if missing).
  - `Enter` on tree panel: if current node contains `.abditum` files, move focus to Files panel. Otherwise no-op.
  - Initial focus: Tree (focusPanel = 0) for both modes.

### Golden File Testing

- **D-07: `filePickerModal` must have golden file test coverage following `arquitetura-teste.md`**
  - Apply the two-level validation architecture: `.txt.golden` (layout/spacing) + `.json.golden` (colors/styles) per case.
  - **Deterministic test setup:** all golden tests use a fixed fake directory tree created via `t.TempDir()` + `os.Chtimes()` to pin file mtimes to a known value (e.g., `2025-03-15 14:32:00 UTC`). File sizes are set by writing known bytes. No real CWD or home directory is used in golden tests.
  - **mtime injection:** add a `timeFmt func(time.Time) string` field to `filePickerModal` (default: renders `ModTime().Local()` as `"02/01/06 15:04"`). Golden tests inject `func(t time.Time) string { return t.UTC().Format("02/01/06 15:04") }` to eliminate timezone variance on CI.
  - **Golden file matrix (8 pairs — 16 files total):**

    | Golden name | Mode | State | Terminal |
    |-------------|------|-------|----------|
    | `filepicker-open-withfiles-80x24` | Open | dir with 3 `.abditum` files; first pre-selected; action active | 80×24 |
    | `filepicker-open-nofiles-80x24` | Open | dir with no `.abditum` files; action disabled | 80×24 |
    | `filepicker-open-treescroll-80x24` | Open | deep tree (8+ visible nodes, scroll active in tree panel) | 80×24 |
    | `filepicker-open-filescroll-80x24` | Open | 10+ `.abditum` files in panel; scroll active in files panel | 80×24 |
    | `filepicker-save-fieldempty-80x24` | Save | filename field empty; action disabled | 80×24 |
    | `filepicker-save-fieldfilled-80x24` | Save | filename field filled; action active | 80×24 |
    | `filepicker-open-withfiles-50x20` | Open | same as `open-withfiles` — narrow: truncation + compact layout | 50×20 |
    | `filepicker-save-fieldfilled-50x20` | Save | same as `save-fieldfilled` — narrow | 50×20 |

  - **`Update()` behavioral test matrix** (table-driven, not golden — state transitions):

    | Input | Panel focus | State | Expected |
    |-------|-------------|-------|----------|
    | `↓` | Tree | cursor < last visible | cursor+1; file panel reloads |
    | `↑` | Tree | cursor = 0 | cursor stays 0 (clamp) |
    | `→` | Tree | collapsed node | node expands; `visibleNode` rebuilt |
    | `←` | Tree | expanded node | node collapses; `visibleNode` rebuilt |
    | `→` | Tree | `▷` node (no subdirs) | no-op |
    | `Enter` | Tree | folder with `.abditum` | focus moves to Files (focusPanel=1) |
    | `Enter` | Tree | folder without `.abditum` | no-op |
    | `Tab` | Tree | any | focus → Files |
    | `Tab` | Files | Open mode | focus → Tree |
    | `Tab` | Files | Save mode | focus → Campo Nome |
    | `Tab` | Campo Nome | Save mode | focus → Tree |
    | `Enter` | Files | Open mode, file selected | emits `filePickerResult{Path}` + `popModalMsg` |
    | `Enter` | Files | Save mode, file selected | copies name to field; focus → Campo Nome |
    | `Enter` | Campo Nome | Save mode, field non-empty | emits `filePickerResult{Path}` (`.abditum` appended) + `popModalMsg` |
    | `Enter` | Campo Nome | Save mode, field empty | no-op |
    | `PgDn` | Files | scroll=0, 10 files | scroll advances by visible height |
    | `Home` | Files | scroll=5 | scroll=0 |
    | `End` | Files | scroll=0 | scroll=max |
    | `Esc` | any | any | emits `filePickerResult{Cancelled:true}` + `popModalMsg` |

  - Golden files live in `internal/tui/testdata/golden/` (existing path, same as other components).
  - Tests added to `filepicker_test.go` — no new test file needed.

### Layout and Sizing

- **D-08: Modal dimensions, proportions, and structural borders**
  - **Width:** `min(70, 80% of terminal width)` columns.
  - **Height:** `80% of terminal height` rows.
  - **Panel split:** tree panel ~40% of inner width; files panel ~60%.
  - **Vertical padding:** 0 (spec exception — justified by density; `╭╮╰╯` + internal headers provide containment).
  - **Horizontal padding:** 2 columns inside the main border.
  - **Title:** `Abrir cofre` (Open mode) / `Salvar cofre` (Save mode).
  - **Bottom border action layout:**
    - Open: `╰── Enter Abrir ───────────────┴────────────────────── Esc Cancelar ──╯`
    - Save: `╰── Enter Salvar ───────────────────────────────────────── Esc Cancelar ──╯`
    - `┴` marks the junction of the panel separator with the base — only present in Open mode (no filename panel below).
    - In Save mode the bottom border has no `┴` (the `├─┴─┤` separator above the filename field provides the junction).
  - **Action `Enter Abrir`/`Enter Salvar` state:**
    - Open — blocked (`text.disabled`) when the directory under the tree cursor contains no `.abditum` files; active (`accent.primary` bold) when it does — **even if focus is on the Tree panel**.
    - Save — blocked when filename field is empty; active when non-empty.
  - **Scroll indicators replace border chars:**
    - Tree panel scroll: `↑`/`■`/`↓` replace the `│` of the panel separator (right edge of tree).
    - Files panel scroll: `↑`/`■`/`↓` replace the `│` of the modal's right border.
  - **Command bar while FilePicker is active:** only `Tab Painel` and `F1 Ajuda` — all other actions hidden.

### Visual Tokens

- **D-09: Complete token assignments from spec §FilePicker token tables**

  | Element | Token | Attribute |
  |---------|-------|-----------|
  | Title `Abrir cofre` / `Salvar cofre` | `text.primary` | **bold** |
  | Header `Estrutura` | `text.secondary` | **bold** |
  | Header `Arquivos` | `text.secondary` | **bold** |
  | Internal separators (`├`, `┬`, `┴`, `─`, `│`) | `border.default` | — |
  | Main modal border | `border.focused` | — |
  | Scroll indicators (`↑`/`↓`/`■`) | `text.secondary` | — |
  | Selected folder in tree | `accent.primary` | **bold** |
  | Non-selected folder | `text.primary` | — |
  | Folder indicators `▶` `▼` `▷` | `accent.secondary` | — |
  | Selected file row | `special.highlight` (background) + `text.primary` | **bold** |
  | Non-selected file | `text.primary` | — |
  | File bullet `●` | `text.secondary` | — |
  | Metadata (size, date) | `text.secondary` | — |
  | `Nenhum cofre neste diretório` | `text.secondary` | — |
  | `Caminho:` label | `text.secondary` | — |
  | Path value | `text.primary` | — |
  | Action default — blocked | `text.disabled` | — |
  | Action default — active | `accent.primary` | **bold** |
  | Save: `Nome do arquivo` label (field focused) | `accent.primary` | **bold** |
  | Save: `Nome do arquivo` label (field not focused) | `text.secondary` | — |
  | Save: filename field area | `surface.input` | — |
  | Save: filename cursor `▌` | `text.primary` | — |

  - `special.highlight`: use hardcoded hex `#3d59a1` (Tokyo Night) until Phase 9 theme cleanup.

### Complete Keyboard Navigation

- **D-10: Full keyboard spec for tree and files panels**

  **Tree panel:**
  | Key | Effect |
  |-----|--------|
  | `↑` / `↓` | Move cursor among visible nodes; clamp at 0 and last; file panel updates instantly |
  | `→` | Expand collapsed node (call `os.ReadDir` if not loaded); no-op on `▷` (no subdirs) |
  | `←` | Collapse expanded node; no-op at root `/` |
  | `Enter` | If cursor dir has `.abditum`: move focus to Files (focusPanel=1); otherwise no-op |
  | `Home` / `End` | Jump to first / last visible node |
  | `PgUp` / `PgDn` | Scroll by visible panel height |
  | `Tab` | Advance focus to next panel stop |

  **Files panel:**
  | Key | Effect |
  |-----|--------|
  | `↑` / `↓` | Move cursor; clamp at 0 and last |
  | `Enter` | Open mode: confirm selection; Save mode: copy name to field + focus → Campo Nome |
  | `Home` / `End` | Jump to first / last file |
  | `PgUp` / `PgDn` | Scroll by visible panel height |
  | `Tab` | Open mode: focus → Tree; Save mode: focus → Campo Nome |

  **Campo Nome (Save mode only):**
  | Key | Effect |
  |-----|--------|
  | Printable chars | Appended to field; invalid filesystem chars (`/ \ : * ? " < > |`) silently blocked |
  | `Enter` | If field non-empty: emit `filePickerResult{Path}` (with `.abditum` appended to returned path if missing); Save mode: `┴` junction present above filename panel |
  | `Tab` | Focus → Tree |
  | `Backspace` | Standard textinput behavior |

  **Global (any panel):**
  | Key | Effect |
  |-----|--------|
  | `Esc` | Emit `filePickerResult{Cancelled: true}` + `popModalMsg` |
  | `F1` | Open Help modal (global anchor) |

### Sort, Metadata Alignment, and Truncation

- **D-11: Ordering, column layout, and truncation priority**
  - **Sort order — tree:** alphabetical, case-insensitive (lexicographic, `a` == `A`).
  - **Sort order — files:** alphabetical, case-insensitive by name without `.abditum` extension.
  - **Tree indentation:** 2 spaces per depth level.
  - **Metadata column layout in files panel:**
    - Name occupies the left portion; size is right-aligned; date/time is at a fixed position to the right of size.
    - Format: `● {name:<N}  {size:>8}  {datetime}` — exact column widths are implementation detail; intent is vertical scanning alignment.
  - **Truncation priority in narrow terminals:** metadata (date first, then size) truncated before filename. Filename only truncates if no space remains even for it.
  - **Path header truncation:** `Caminho:` value truncated from the **left** with `…` (e.g., `…/projetos/abditum`) when it exceeds available width.
  - **Size format:** base-1024, 1 decimal place, space before unit — `25.8 MB`, `1.2 KB`, `512 B`. Units: B, KB, MB, GB.

### Save Mode Field Behavior

- **D-12: Filename field semantics in Save mode**
  - **`↑↓` in files panel** only moves the highlight — does **not** copy the filename to the field. Only `Enter` (on a file row) copies the name (without `.abditum` extension) and moves focus to Campo Nome.
  - **Field preserved on tree navigation:** navigating the tree (moving cursor, expanding/collapsing) does **not** clear the filename field. Whatever the user typed stays.
  - **Invalid chars:** `/ \ : * ? " < > |` are silently blocked in the textinput — no error message.
  - **`.abditum` extension:** added silently to the returned `filePickerResult.Path` if the typed name does not already end in `.abditum`. The field text itself is **not** modified — the extension appears only in the emitted result.
  - **`Nome sugerido` parameter:** if the flow passes a suggested name at construction, it pre-fills the textinput field (without extension in the display). Currently no flow passes a suggested name — field starts empty.

### Agent's Discretion

- Whether `visibleNode` is a struct or an index into the `treeNode` tree — implementation detail.
- Exact lipgloss styling calls for junction characters and scroll indicator replacement logic.
- Scroll offset arithmetic for both panels.
- Column width formula for metadata alignment (name/size/date) — derive from panel width at render time.
</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

- `tui-specification-novo.md` §FilePicker (lines ~334–625) — Full spec: wireframes (Open + Save), element token tables, state tables, message tables, keyboard navigation rules, scroll indicator rules, metadata format, ordering, indentation, edge cases.
- `tui-design-system-novo.md` §Dimensionamento de diálogos, §Sobreposições — Dialog sizing formula, border token semantics.
- `arquitetura-teste.md` — Two-level golden file architecture (`.txt.golden` layout + `.json.golden` style), SGR parser, runner pattern, naming conventions, `-update` flag workflow.
- `internal/tui/dialogs.go` — Current `filePickerModal` implementation to be replaced; `filePickerResult`, `FilePickerMode`, `FilePicker()` factory to be updated.
- `internal/tui/filepicker_test.go` — Existing tests: must be updated in the same PR as the reimplementation.
- `internal/tui/flow_open_vault.go` — Call site for `FilePickerOpen` mode.
- `internal/tui/flow_create_vault.go` — Call site for `FilePickerSave` mode.
- `internal/tui/flow_save_and_exit.go` — Call site for `FilePickerSave` mode.
- `internal/tui/modal.go` — `modalView` interface contract.
- `internal/tui/messages.go` — `MessageManager.Show()` API.
- `internal/tui/theme.go` + `internal/tui/tokens.go` — Token constants for border colors.
- `.planning/phases/06-welcome-screen-vault-create-open/06-CONTEXT.md` §D-03 — Locked decisions from Phase 06 (border tokens, filtering rules, 0-vertical-padding exception).
</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `charm.land/bubbles/v2/textinput` — Already a dep (used in `passwordcreate.go`); use for the Save mode filename field.
- `filePickerResult{Path string, Cancelled bool}` — Existing result type; no change needed.
- `popModalMsg{}` — Used by all modals to dismiss themselves.
- `MsgHint`, `MsgError`, `MsgWarn` — MessageManager kind constants.
- `formatFileSize()` in `dialogs.go` — Keep but update to space-separated format.
- `ThemeTokyoNight`, `ThemeCyberpunk` — Theme instances; picker should accept `*Theme` field.

### Established Patterns
- Modals own their size via `SetSize(w, h int)` called by rootModel on `WindowSizeMsg`.
- `Init() tea.Cmd` sets up initial state (CWD, theme, focus).
- `Shortcuts() []Shortcut` returns the action bar entries for when this modal is active.
- All key handling via `tea.KeyPressMsg` switch on `msg.Code`.
- `os.ReadDir` (not `os.Open` + readdir) for directory listing — already used in current impl.

### Integration Points
- `dialogs.go` `FilePicker()` factory — still the public entry point from flows; update to pass `*MessageManager`.
- Three flow files push `filePickerModal` — all must be updated to pass `messages` and use new mode constants.
- `rootModel` calls `SetSize` on all modals when the terminal resizes.
</code_context>

<specifics>
## Specific Ideas

- The `treeNode` struct should track `hasSubdirs bool` so `▷` can be set at load time without needing a second `os.ReadDir` call.
- The `visibleNode` flat slice should store a pointer to the `treeNode` and its depth, rebuilt via DFS after every structural change.
- For the path header, `Caminho:` value should be the path of the node *under the cursor* (not necessarily the currently expanded node), truncated from the left with `…` if too wide (spec: `…/projetos/abditum`).
- Scroll indicators replace the `│` border char: for tree panel, `↑`/`■`/`↓` replace the right-side `│` of the panel separator; for files panel, they replace the `│` of the modal's right border.
- Inaccessible directory on expand: `m.messages.Show(MsgError, "✕ Sem permissão para acessar "+name, 5, false)`; node remains collapsed.
- CWD inaccessible fallback: `m.messages.Show(MsgWarn, "⚠ Diretório atual inacessível — navegando para home", 0, true)`.
</specifics>

<deferred>
## Deferred Ideas

- Mouse click/double-click interactions — spec documents these but keyboard is MVP for Phase 06.3; defer to a later phase if needed.
- `special.highlight` as a proper `Theme` field — use hardcoded hex for now; Theme cleanup is Phase 9.
- `FilePickerDirectory` and `FilePickerAny` mode constants — removing them is fine; no current callers use these modes.

None — analysis stayed within phase scope.
</deferred>

---

*Phase: 06.3-reimplement-file-picker*
*Context gathered: 2026-04-07 (assumptions mode)*

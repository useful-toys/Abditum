---
phase: 06.3-reimplement-file-picker
plan: "02"
subsystem: ui
tags: [bubbletea, filepicker, keyboard, navigation, tui]

requires:
  - phase: "06.3-01"
    provides: "filePickerModal struct skeleton, SetSize, Shortcuts, Init/Update/View stubs"

provides:
  - "Init() with CWD fallback chain (os.Getwd → os.UserHomeDir → /)"
  - "buildTreeChain() — pre-expanded path chain from / to currentPath"
  - "buildVisibleNodes() — DFS flatten of expanded tree into []visibleNode"
  - "expandNode() — lazy loads treeNode children, returns error on permission failure"
  - "loadFilesForCursor() — populates m.files/m.fileInfos with ext filter and D-15 auto-selection"
  - "emitHint() — D-03 hint messages per panel/mode/state"
  - "Update() dispatching to updateTree/updateFiles/updateField by focusPanel"
  - "updateTree() — ↑↓←→ Home/End/PgUp/PgDown/Enter/Tab/Esc handlers"
  - "updateFiles() — ↑↓ Home/End/PgUp/PgDown/Enter/Tab handlers"
  - "updateField() — Enter/Tab/printable char handlers with D-12 invalid char blocking"
  - "adjustTreeScroll() / adjustFileScroll() — viewport clamping"
  - "currentNode() / parentCursor() — tree navigation helpers"

affects:
  - "06.3-03 (View() renders what this layer computes)"
  - "06.3-04 (call sites and tests)"
  - "06.3-05 (golden tests exercise Update() paths)"

tech-stack:
  added: []
  patterns:
    - "Panel dispatch via focusPanel int (0=tree, 1=files, 2=campo nome)"
    - "Lazy tree expansion: loaded=false → expandNode() on first DFS visit"
    - "tea.Batch for multi-message returns (result + popModalMsg)"

key-files:
  created: []
  modified:
    - "internal/tui/filepicker.go"

key-decisions:
  - "msg.Text != '' used for printable char detection in updateField (BubbleTea v2 API)"
  - "MessageManager.Show ttlSeconds is int not time.Duration — used 5 for D-22 errors"
  - "Right key no-op when node.hasSubdirs == false (▷ indicator dirs)"
  - "Tab in Open mode files panel cycles back to tree (only 2 stops); Save mode adds campo nome"
  - "Enter on tree with no files → no-op (stays in tree, no panel switch)"

patterns-established:
  - "D-22 error: MsgError with filepath.Base(node.path) only, ttl=5"
  - "D-15 auto-selection: fileCursor=0 when files exist, -1 when empty"
  - "D-16 Left key: collapse if expanded, navigate to parent if already collapsed"

requirements-completed: []

duration: 30min
completed: 2026-04-07
---

# Plan 06.3-02: Logic Layer Summary

**Full interactive logic layer for filePickerModal: Init with CWD fallback chain, lazy tree builder, file loader, and all keyboard handlers for all three panels**

## Performance

- **Duration:** ~30 min
- **Completed:** 2026-04-07
- **Tasks:** 2/2
- **Files modified:** 1

## Accomplishments

- `Init()` resolves CWD with fallback (os.Getwd → os.UserHomeDir → "/"), builds pre-expanded tree chain, and positions treeCursor on currentPath node
- Complete keyboard handling for all 3 panels: tree (14 key cases), files (8 key cases), campo nome (Enter/Tab + filtered printable chars)
- D-22 permission errors surface via MsgError with dir basename; D-15 fileCursor auto-selection; D-16 parent navigation on collapsed Left press

## Task Commits

1. **Task 1: Init(), CWD fallback, treeNode building, loadFilesForCursor()** — `ba369e5` (feat)
2. **Task 2: Update() with full keyboard handling for all panels** — `d908beb` (feat)

## Files Created/Modified

- `internal/tui/filepicker.go` — Added ~300 lines: Update(), updateTree(), updateFiles(), updateField(), all scroll/navigation helpers

## Decisions Made

- Used `msg.Text != ""` (not `msg.Code >= 32`) for printable char detection in updateField — correct BubbleTea v2 API
- `MessageManager.Show` accepts `ttlSeconds int` not `time.Duration` — D-22 uses `5` (5 seconds)
- Task 1 was done by prior agent run (commit ba369e5); Task 2 implemented inline after abort

## Deviations from Plan

None — plan executed exactly as specified. The only deviation was execution mode (inline instead of subagent) due to agent abort.

## Issues Encountered

Subagent was aborted after Task 1 completed. Task 2 was implemented inline without spawning a new agent. Build passes cleanly for `./internal/tui/` (flow call-site errors in test file are expected — Plan 04 scope).

## Next Phase Readiness

- Wave 3 (06.3-03) can now implement View() — all state fields (focusPanel, treeCursor, fileCursor, treeScroll, fileScroll, currentPath, files, visibleNodes) are populated and maintained correctly by Update()
- `go vet` reports `loadDirectory undefined` in filepicker_test.go — this is the old test referencing the removed method; Plan 04 will fix it

---
*Phase: 06.3-reimplement-file-picker*
*Completed: 2026-04-07*

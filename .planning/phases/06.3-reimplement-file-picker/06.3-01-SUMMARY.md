---
phase: 06.3-reimplement-file-picker
plan: "01"
subsystem: ui
tags: [go, bubbletea, filepicker, tui, textinput, tokens]

# Dependency graph
requires: []
provides:
  - filePickerModal skeleton with all D-00 through D-22 fields
  - FilePickerOpen and FilePickerSave mode enum (replaces FilePickerFile/Directory/Any)
  - treeNode and visibleNode structs for lazy recursive tree
  - ColorBorderFocused and ColorSurfaceInput token constants
  - FilePicker() factory with correct signature (title, mode, ext, messages, theme)
  - formatFileSize with space-separated format (25.8 MB, not 25.8MB)
affects:
  - 06.3-02 (Init/Update implementation reads all fields)
  - 06.3-03 (View implementation consumes struct and tokens)
  - 06.3-04 (flow call sites use new FilePicker() factory signature)

# Tech tracking
tech-stack:
  added: [charm.land/bubbles/v2 promoted from indirect to direct (textinput)]
  patterns:
    - modalView compile-time assertion pattern (var _ modalView = &T{})
    - Factory batches Init() return value with pushModalMsg (correct pattern)

key-files:
  created:
    - internal/tui/filepicker.go
  modified:
    - internal/tui/dialogs.go
    - internal/tui/tokens.go
    - go.mod
    - go.sum

key-decisions:
  - "filepicker.go is the sole file for filePickerModal — removed from dialogs.go"
  - "FilePickerFile/Directory/Any removed; FilePickerOpen/FilePickerSave are the new enum"
  - "FilePicker() factory takes messages *MessageManager and theme *Theme (D-14, D-17)"
  - "formatFileSize space-separated per D-05: '25.8 MB' not '25.8MB'"
  - "ColorBorderFocused=#7aa2f7, ColorSurfaceInput=#1e1f2e added to tokens.go"
  - "Flow call site errors (FilePickerFile undefined) are expected; fixed in Plan 04"

patterns-established:
  - "filePickerModal satisfies modalView via compile-time var _ modalView = &filePickerModal{}"
  - "Init() called by factory before push; return value must not be discarded"

requirements-completed: []

# Metrics
duration: 15min
completed: 2026-04-07
---

# Phase 06.3 Plan 01: Create filePickerModal Skeleton Summary

**filePickerModal extracted into filepicker.go — complete struct skeleton with all D-00–D-22 fields, new FilePickerOpen/FilePickerSave enum, ColorBorderFocused/ColorSurfaceInput tokens, updated FilePicker() factory with MessageManager+Theme params**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-07T13:10:00Z
- **Completed:** 2026-04-07T13:25:00Z
- **Tasks:** 2
- **Files modified:** 5 (filepicker.go created, dialogs.go, tokens.go, go.mod, go.sum)

## Accomplishments
- Created `internal/tui/filepicker.go` with complete struct skeleton: all types, all fields, all method signatures
- Removed old `filePickerModal` stub from `dialogs.go` along with the incorrect 3-value enum
- Added `ColorBorderFocused` and `ColorSurfaceInput` semantic color constants to `tokens.go`
- Updated `FilePicker()` factory in `dialogs.go` to correct pattern (no discard of Init() return)
- Fixed `formatFileSize` to space-separated format per D-05

## Task Commits

Each task was committed atomically:

1. **Task 1: Create internal/tui/filepicker.go** - `f7f76c4` (feat)
2. **Task 2: Clean dialogs.go + add tokens** - `fea7ca3` (feat)

## Files Created/Modified
- `internal/tui/filepicker.go` — New file: FilePickerMode enum, treeNode, visibleNode, filePickerModal struct, SetSize, Shortcuts, stub Init/Update/View, compile-time assertion
- `internal/tui/dialogs.go` — Old filePickerModal struct and methods removed; FilePicker() factory updated with new signature; formatFileSize fixed to space-separated
- `internal/tui/tokens.go` — ColorBorderFocused and ColorSurfaceInput constants added
- `go.mod` — charm.land/bubbles/v2 promoted from indirect to direct dependency
- `go.sum` — Updated checksums

## Decisions Made
- Flow files (`flow_create_vault.go`, `flow_save_and_exit.go`) still reference `FilePickerFile` (removed enum) — per plan, these compilation errors are expected and fixed in Plan 04
- `filepicker.go` itself compiles cleanly; only cross-file errors from flow call sites remain
- The `suggestedName` field preserved in struct — used by Save mode Init() per D-14 even though no current flow passes a value

## Deviations from Plan

None - plan executed exactly as written. Both tasks were already in the working tree (uncommitted) from prior planning work; this execution verified all done criteria and committed them atomically.

## Issues Encountered
- `go build ./internal/tui/` shows errors from flow files using `FilePickerFile` (undefined) — this is explicitly expected per the plan verification section and will be fixed in Plan 04

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Ready for Plan 02: Init() and Update() implementation
- filePickerModal struct is complete with all fields Plans 02-05 will reference
- Factory pattern is established; Plans 02-03 can rely on `theme` and `messages` being set before Init()
- Flow files will need Plan 04 to compile the package cleanly

---
*Phase: 06.3-reimplement-file-picker*
*Completed: 2026-04-07*

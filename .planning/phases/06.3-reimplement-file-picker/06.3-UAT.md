---
status: testing
phase: 06.3-reimplement-file-picker
source:
  - 06.3-01-SUMMARY.md
  - 06.3-02-SUMMARY.md
  - 06.3-03-SUMMARY.md
  - 06.3-04-SUMMARY.md
  - 06.3-05-SUMMARY.md
  - 06.3-06-SUMMARY.md
started: 2026-04-07T00:00:00Z
updated: 2026-04-07T01:00:00Z
---

## Current Test

number: 3
name: Open Vault — Tree Navigation Updates File Panel in Real-Time
expected: |
  Moving the tree cursor up or down (↑/↓ keys) to a different directory causes the right "Arquivos"
  panel to update immediately to show the .abditum files in that directory.
  The path header also updates to reflect the newly highlighted directory.
  No Tab or Enter is required — it updates on cursor move.
awaiting: user response

## Tests

### 1. Open Vault — File Picker Opens with Tree Panel Focused
expected: When the "Abrir cofre" flow triggers the file picker, a modal appears with title "Abrir cofre". The left panel is labeled "Estrutura" (tree panel) and the right panel is labeled "Arquivos". Initial focus is on the tree (left) panel — the tree cursor is visible and active. A path header line shows the current directory path at the top of the modal content area.
result: fixed
fix: "dialogs.go factories were wrapping tea.Batch() in func() tea.Msg{} which BubbleTea v2 discarded. Fixed by returning tea.Batch() directly as tea.Cmd."
retest: required

### 2. Open Vault — Tab Cycles Between Two Panels Only
expected: Pressing Tab while in the tree panel moves focus to the files panel. Pressing Tab again moves focus back to the tree panel. There are only 2 tab stops in Open mode (no filename field). The currently focused panel is visually distinguishable from the unfocused one.
result: fixed
fix: |
  Tab no painel da árvore agora só avança para o painel de arquivos se len(m.files) > 0.
  Em Open mode com diretório vazio, Tab é no-op.
  Em Save mode com diretório vazio, Tab pula o painel de arquivos e vai direto ao campo nome.
  Highlight (selectedBg #3d59a1) agora aparece somente no painel com foco ativo.
  Painel sem foco mostra cursor passivo: bold + cor do texto, sem background.
retest: required

### 3. Open Vault — Tree Navigation Updates File Panel in Real-Time
expected: Moving the tree cursor up or down (↑/↓ keys) to a different directory causes the right "Arquivos" panel to update immediately to show the .abditum files in that directory. The path header also updates to reflect the newly highlighted directory. No Tab or Enter is required — it updates on cursor move.
result: [pending]

### 4. Open Vault — Tree Expand and Collapse (→/←)
expected: Pressing → on a collapsed directory in the tree expands it, showing subdirectories indented 2 spaces with ▶ (collapsed) or ▼ (expanded) indicators. Pressing ← on an expanded node collapses it. A directory with no subdirectories shows ▷ and pressing → on it does nothing.
result: [pending]

### 5. Open Vault — Enter on File Confirms Selection
expected: Navigating to a directory with .abditum files, then pressing Tab to move focus to the files panel, then pressing Enter on a selected file confirms the selection and dismisses the modal. The vault opens (the flow proceeds). In Open mode, Enter does NOT copy the filename to a field — it directly confirms.
result: [pending]

### 6. Open Vault — Empty Directory Shows No-Files Message and Disables Action
expected: When the tree cursor is on a directory with no .abditum files, the files panel shows "Nenhum cofre neste diretório". The bottom action "Enter Abrir" appears dimmed/disabled (not in the active accent color). The action becomes active only when the cursor is on a dir with at least one .abditum file.
result: [pending]

### 7. Save Vault — File Picker Opens with Three Panels (Filename Field)
expected: When the "Salvar cofre" or "Criar cofre" flow triggers the file picker, the modal appears with title "Salvar cofre". Below the tree and files panels there is a separator and a "Arquivo:" filename input field. Tab now cycles through 3 stops: Tree → Files → Campo Nome (filename field) → Tree.
result: [pending]

### 8. Save Vault — Enter on File Copies Name to Field (Does Not Confirm)
expected: In Save mode, with focus on the files panel, pressing Enter on a highlighted .abditum file copies the filename (without the .abditum extension) into the filename field and moves focus to that field. The modal stays open — no vault is saved yet. The user must press Enter again in the filename field to confirm.
result: [pending]

### 9. Save Vault — Enter on Filename Field Saves
expected: In Save mode, with the filename field filled and focused (Campo Nome panel), pressing Enter confirms and saves the vault with that name (.abditum extension is added automatically if not typed). The modal dismisses and the save flow proceeds. Pressing Enter when the field is empty does nothing.
result: [pending]

### 10. Esc Cancels the Picker from Any Panel
expected: Pressing Esc from any panel (tree, files, or filename field) closes the file picker modal without saving or opening anything. The app returns to the previous screen/state.
result: [pending]

### 11. Path Header Shows Current Tree Cursor Directory
expected: The path header at the top of the modal content (below the title border) always shows the directory currently under the tree cursor. As you move the cursor in the tree, the header updates. If the path is very long, it is truncated from the left with "…" (e.g., "…/projetos/abditum"), not from the right.
result: [pending]

### 12. File Metadata Format — Absolute Date (dd/mm/aa HH:MM)
expected: Files listed in the Arquivos panel show file size and modification date. The date format is absolute (e.g., "07/04/26 13:25"), not relative (e.g., "2 hours ago" or "just now"). File sizes use a space before the unit: "1.2 KB", "25.8 MB", "512 B" — not "1.2KB" without space.
result: [pending]

### 13. Command Bar Shows Only Tab and F1 While Picker is Active
expected: While the file picker modal is open, the command bar at the bottom of the screen shows only "Tab Painel" and "F1 Ajuda". All other shortcuts that appear in normal application mode are hidden. When the picker is closed, the normal shortcuts return.
result: [pending]

### 14. All Tests Pass (go test)
expected: Running `go test ./internal/tui/ -run "TestFilePicker|TestGolden" -count=1` produces all PASS with no failures. The 38 file picker tests (20 unit/behavioral + 8 golden + prior baseline) all pass. Pre-existing failures on TestRenderCommandBar_Golden and TestDecisionDialog_Golden (CRLF issue, unrelated) are the only non-passing tests.
result: pass
note: "All TestFilePicker* and TestGolden* tests pass. Golden files regenerated after renderTreeContent, renderBottomBorder, renderFieldSeparator color/highlight fixes."

## Summary

total: 14
passed: 1
fixed: 2
issues: 0
skipped: 0
pending: 11

## Gaps

- truth: "All TestFilePicker* and TestGolden* tests pass"
  status: passed
  test: 14

- truth: "buildTreeChain cross-platform: cursor posicionado no pwd correto no Windows"
  status: fixed
  fix: "filepath.VolumeName + filepath.Separator como raiz em vez de / hardcoded; depth contador separado; isLeaf por comparação de path"
  test: 2
